basePath: /default/default
consumes:
- application/json
definitions:
  AcceptConsentRequest:
    properties:
      account_ids:
        description: List of accounts that user agreed to share on the consent page.
        items:
          type: string
        type: array
        x-nullable: false
      granted_scopes:
        $ref: '#/definitions/GrantedScopes'
      login_state:
        description: |-
          Random string generated by Cloudentity used to mitigate Cross-site request forgery (CSRF) attacks.
          Cloudentity sends state as login_state query param when redirecting to the consent page.
        example: '"cauq8fonbud6q8806bf0"'
        format: loginState
        type: string
        x-nullable: false
    type: object
  AuthenticationContext:
    additionalProperties:
      type: object
    type: object
  AuthorizationDetailType:
    type: string
  BrazilConsent:
    properties:
      account_ids:
        items:
          type: string
        type: array
        x-nullable: false
      client_id:
        description: Client application identifier.
        example: '"cauqo9c9vpbs0aj2b2v0"'
        format: clientID
        type: string
        x-nullable: false
      cnpj:
        type: string
        x-nullable: false
      consent_id:
        format: consentID
        type: string
        x-nullable: false
      cpf:
        type: string
        x-nullable: false
      created_at:
        format: date-time
        type: string
        x-nullable: false
      customer_data_access_consent:
        $ref: '#/definitions/BrazilCustomerDataAccessConsentV1'
      customer_data_access_consent_v2:
        $ref: '#/definitions/BrazilCustomerDataAccessConsentV2'
      customer_insurance_data_access_consent:
        $ref: '#/definitions/BrazilInsuranceCustomerDataAccessConsent'
      customer_payment_consent:
        $ref: '#/definitions/BrazilCustomerPaymentConsent'
      customer_payment_consent_v2:
        $ref: '#/definitions/BrazilCustomerPaymentConsentV2'
      customer_payment_consent_v3:
        $ref: '#/definitions/BrazilCustomerPaymentConsentV3'
      idempotency_key:
        type: string
        x-nullable: false
      request_hash:
        type: string
        x-nullable: false
      server_id:
        description: Server / Workspace identifier.
        example: '"server"'
        format: serverID
        type: string
        x-nullable: false
      spec:
        type: string
        x-nullable: false
      spec_version:
        $ref: '#/definitions/SpecVersion'
      status:
        type: string
        x-nullable: false
      tenant_id:
        description: Tenant identifier.
        example: '"tenant"'
        format: tenantID
        type: string
        x-nullable: false
      type:
        $ref: '#/definitions/ConsentType'
    type: object
  BrazilConsentExtension:
    properties:
      expiration_time:
        format: date-time
        type: string
        x-nullable: false
      logged_user:
        type: string
        x-nullable: false
      request_time:
        format: date-time
        type: string
        x-nullable: false
    type: object
  BrazilConsentExtensions:
    items:
      $ref: '#/definitions/BrazilConsentExtension'
    type: array
  BrazilCreateConsentExtensionResponse:
    properties:
      data:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Data3'
      links:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Links'
      meta:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Meta'
    required:
    - data
    type: object
  BrazilCreateCustomerDataAccessConsentV2Response:
    properties:
      data:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Data2'
      links:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Links'
      meta:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Meta'
    required:
    - data
    type: object
  BrazilCustomerCreatePaymentConsentRequest:
    properties:
      aud:
        type: string
        x-nullable: false
      data:
        $ref: '#/definitions/OpenbankingBrasilPaymentData'
      iat:
        format: int64
        type: integer
        x-nullable: false
      iss:
        type: string
        x-nullable: false
      jti:
        type: string
        x-nullable: false
    required:
    - data
    type: object
  BrazilCustomerCreatePaymentConsentRequestV2:
    properties:
      aud:
        type: string
        x-nullable: false
      data:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Data'
      iat:
        format: int64
        type: integer
        x-nullable: false
      iss:
        type: string
        x-nullable: false
      jti:
        type: string
        x-nullable: false
    required:
    - data
    type: object
  BrazilCustomerCreatePaymentConsentRequestV3:
    properties:
      aud:
        type: string
        x-nullable: false
      data:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Data'
      iat:
        format: int64
        type: integer
        x-nullable: false
      iss:
        type: string
        x-nullable: false
      jti:
        type: string
        x-nullable: false
    required:
    - data
    type: object
  BrazilCustomerDataAccessConsentRequestV1:
    properties:
      data:
        $ref: '#/definitions/OpenbankingBrasilConsentData'
    required:
    - data
    type: object
  BrazilCustomerDataAccessConsentRequestV2:
    properties:
      data:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Data'
    required:
    - data
    type: object
  BrazilCustomerDataAccessConsentResponse:
    properties:
      data:
        $ref: '#/definitions/OpenbankingBrasilConsentData1'
      links:
        $ref: '#/definitions/OpenbankingBrasilConsentLinks'
      meta:
        $ref: '#/definitions/OpenbankingBrasilConsentMeta'
    required:
    - data
    type: object
  BrazilCustomerDataAccessConsentV1:
    properties:
      consentId:
        description: |-
          O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      document:
        $ref: '#/definitions/OpenbankingBrasilConsentDocument1'
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      permissions:
        description: Especifica os tipos de permisses de acesso s APIs no escopo do
          Open Banking Brasil - Fase 2, de acordo com os blocos de consentimento fornecidos
          pelo usurio e necessrios ao acesso a cada endpoint das APIs.
        example:
        - ACCOUNTS_READ
        - ACCOUNTS_OVERDRAFT_LIMITS_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentPermission1'
        maxItems: 30
        minItems: 1
        type: array
        x-nullable: false
      status:
        $ref: '#/definitions/OpenbankingBrasilConsentStatus'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionFromDateTime:
        description: Data e hora da transao inicial. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a primeira transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-01-01T00:00:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionToDateTime:
        description: Data e hora final da transao. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a ultima transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-02-01T23:59:59Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - expirationDateTime
    - permissions
    - status
    - statusUpdateDateTime
    - document
    type: object
  BrazilCustomerDataAccessConsentV2:
    properties:
      consentId:
        description: |-
          O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      document:
        $ref: '#/definitions/OpenbankingBrasilConsentV2BusinessEntityDocument'
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC (UTC time format). Para consentimentos com prazo indeterminado,  esperado
          preenchimento com `2300-01-01T00:00:00Z`.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      extensions:
        $ref: '#/definitions/BrazilConsentExtensions'
      permissions:
        description: Especifica os tipos de permisses de acesso s APIs no escopo do
          Open Finance Brasil - Dados cadastrais e transacionais, de acordo com os
          blocos de consentimento fornecidos pelo usurio e necessrios ao acesso a
          cada endpoint das APIs. Esse array no deve ter duplicidade de itens.
        example:
        - ACCOUNTS_READ
        - ACCOUNTS_OVERDRAFT_LIMITS_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentV2Permission1'
        minItems: 1
        type: array
        x-nullable: false
      rejection:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Rejection'
      status:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Status'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - expirationDateTime
    - permissions
    - status
    - statusUpdateDateTime
    - document
    type: object
  BrazilCustomerPatchPaymentConsentRequest:
    properties:
      aud:
        type: string
        x-nullable: false
      data:
        $ref: '#/definitions/OpenbankingBrasilPaymentPatchPaymentsConsentData'
      iat:
        format: int64
        type: integer
        x-nullable: false
      iss:
        type: string
        x-nullable: false
      jti:
        type: string
        x-nullable: false
    required:
    - data
    type: object
  BrazilCustomerPaymentConsent:
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentBusinessEntity'
      consentId:
        description: |-
          Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o consentimento foi criado. Uma string com
          data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentIdentification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentDebtorAccount'
      expirationDateTime:
        description: |-
          Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
          O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
          Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
          O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentLoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentPaymentConsent'
      revocation:
        $ref: '#/definitions/OpenbankingBrasilPaymentRevocation'
      status:
        $ref: '#/definitions/OpenbankingBrasilPaymentEnumAuthorisationStatusType'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - creditor
    - expirationDateTime
    - loggedUser
    - payment
    - status
    - statusUpdateDateTime
    type: object
  BrazilCustomerPaymentConsentResponse:
    properties:
      aud:
        type: string
        x-nullable: false
      data:
        $ref: '#/definitions/OpenbankingBrasilPaymentData1'
      iat:
        format: int64
        type: integer
        x-nullable: false
      iss:
        type: string
        x-nullable: false
      jti:
        type: string
        x-nullable: false
      links:
        $ref: '#/definitions/OpenbankingBrasilPaymentLinks'
      meta:
        $ref: '#/definitions/OpenbankingBrasilPaymentMeta'
    required:
    - data
    - links
    - meta
    type: object
  BrazilCustomerPaymentConsentResponseV2:
    properties:
      aud:
        type: string
        x-nullable: false
      data:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Data1'
      iat:
        format: int64
        type: integer
        x-nullable: false
      iss:
        type: string
        x-nullable: false
      jti:
        type: string
        x-nullable: false
      links:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2LinkSingle'
      meta:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Meta'
    required:
    - data
    - links
    - meta
    type: object
  BrazilCustomerPaymentConsentResponseV3:
    properties:
      aud:
        type: string
        x-nullable: false
      data:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Data1'
      iat:
        format: int64
        type: integer
        x-nullable: false
      iss:
        type: string
        x-nullable: false
      jti:
        type: string
        x-nullable: false
      links:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3LinkSingle'
      meta:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Meta'
    required:
    - data
    - links
    - meta
    type: object
  BrazilCustomerPaymentConsentV2:
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2BusinessEntity'
      consentId:
        description: |-
          Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9\-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o consentimento foi criado. Uma string com
          data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Identification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2DebtorAccount'
      expirationDateTime:
        description: |-
          Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
          O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
          Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
          O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2LoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2PaymentConsent'
      status:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2EnumAuthorisationStatusType'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - creditor
    - expirationDateTime
    - loggedUser
    - payment
    - status
    - statusUpdateDateTime
    type: object
  BrazilCustomerPaymentConsentV3:
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3BusinessEntity'
      consentId:
        description: |-
          Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9\-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o consentimento foi criado. Uma string com
          data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Identification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3ConsentsDebtorAccount'
      expirationDateTime:
        description: |-
          Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
          O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
          Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
          O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3LoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3PaymentConsent'
      rejectionReason:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3ConsentRejectionReason'
      status:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumAuthorisationStatusType'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - creditor
    - expirationDateTime
    - loggedUser
    - payment
    - status
    - statusUpdateDateTime
    type: object
  BrazilExtensionConsentRequest:
    properties:
      data:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Data1'
    required:
    - data
    type: object
  BrazilGetConsentExtensionsResponse:
    properties:
      data:
        description: data
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentV2Data5'
        type: array
        x-nullable: false
      links:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Links'
      meta:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Meta'
    required:
    - data
    type: object
  BrazilGetCustomerDataAccessConsentV2Response:
    properties:
      data:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Data4'
      links:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Links'
      meta:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Meta'
    required:
    - data
    type: object
  BrazilInsuranceCustomerDataAccessConsent:
    properties:
      consentId:
        description: |-
          O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:prudential:C1DD93123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (prudential)
          o identificador especfico dentro do namespace (C1DD93123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:prudential:C1DD93123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      document:
        $ref: '#/definitions/OPINDocument1'
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      permissions:
        description: Especifica os tipos de permisses de acesso  s APIs no escopo
          do Open Insurance Brasil - Fase 2, de acordo com os blocos de consentimento
          fornecidos pelo usurio e necessrios ao acesso a cada endpoint das APIs.
        example:
        - PENSION_RISK_READ
        - CAPITALIZATION_TITLES_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OPINPermission1'
        minItems: 1
        type: array
        x-nullable: false
      status:
        $ref: '#/definitions/OPINStatus'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionFromDateTime:
        description: Data e hora da transao inicial. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a primeira transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-01-01T00:00:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionToDateTime:
        description: Data e hora final da transao. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a ultima transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-02-01T23:59:59Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - expirationDateTime
    - permissions
    - status
    - statusUpdateDateTime
    - document
    type: object
  BrazilInsuranceCustomerDataAccessConsentRequest:
    properties:
      data:
        $ref: '#/definitions/OPINData'
    required:
    - data
    type: object
  BrazilInsuranceCustomerDataAccessConsentResponse:
    properties:
      data:
        $ref: '#/definitions/OPINData1'
      links:
        $ref: '#/definitions/OPINLinks'
      meta:
        $ref: '#/definitions/OPINMeta'
    required:
    - data
    type: object
  ClientInfo:
    properties:
      client_name:
        description: Human-readable name of a client application.
        example: My app
        type: string
        x-nullable: false
      client_uri:
        description: URI of a client application.
        type: string
        x-nullable: false
      description:
        description: Description of the client application.
        type: string
        x-nullable: false
      logo_uri:
        description: Logo URI.
        type: string
        x-nullable: false
      organisation_id:
        description: |-
          External organization identifier. It is a unique string assigned by the CDR Register to identify an Accredited
          Data Recipient Brand.

          The value obtained is used as the `aud` claim for message signing, for example, when a JSON Web Token (JWT) is
          required for authorization, and represents the audience(s) the JWT is intended for.
        example: 5647fe90-f6bc-11eb-9a03-0242ac130003
        type: string
        x-nullable: false
      policy_uri:
        description: Policy URL to read about how the profile data is used.
        type: string
        x-nullable: false
      tos_uri:
        description: Terms of Service URL.
        type: string
        x-nullable: false
    type: object
  ClientJWK:
    properties:
      alg:
        description: |-
          The "alg" (algorithm) parameter identifies the algorithm intended for
          use with the key.  The values used should either be registered in the
          IANA "JSON Web Signature and Encryption Algorithms" registry
          established by [JWA] or be a value that contains a Collision-
          Resistant Name.
        example: RS256
        type: string
        x-nullable: false
      crv:
        example: P-256
        type: string
        x-nullable: false
      d:
        example: T_N8I-6He3M8a7X1vWt6TGIx4xB_GP3Mb4SsZSA4v-orvJzzRiQhLlRR81naWYxfQAYt5isDI6_C2L9bdWo4FFPjGQFvNoRX-_sBJyBI_rl-TBgsZYoUlAj3J92WmY2inbA-PwyJfsaIIDceYBC-eX-xiCu6qMqkZi3MwQAFL6bMdPEM0z4JBcwFT3VdiWAIRUuACWQwrXMq672x7fMuaIaHi7XDGgt1ith23CLfaREmJku9PQcchbt_uEY-hqrFY6ntTtS4paWWQj86xLL94S-Tf6v6xkL918PfLSOTq6XCzxvlFwzBJqApnAhbwqLjpPhgUG04EDRrqrSBc5Y1BLevn6Ip5h1AhessBp3wLkQgz_roeckt-ybvzKTjESMuagnpqLvOT7Y9veIug2MwPJZI2VjczRc1vzMs25XrFQ8DpUy-bNdp89TmvAXwctUMiJdgHloJw23Cv03gIUAkDnsTqZmkpbIf-crpgNKFmQP_EDKoe8p_PXZZgfbRri3NoEVGP7Mk6yEu8LjJhClhZaBNjuWw2-KlBfOA3g79mhfBnkInee5KO9mGR50qPk1V-MorUYNTFMZIm0kFE6eYVWFBwJHLKYhHU34DoiK1VP-svZpC2uAMFNA_UJEwM9CQ2b8qe4-5e9aywMvwcuArRkAB5mBIfOaOJao3mfukKAE
        type: string
        x-nullable: false
      dp:
        example: G4sPXkc6Ya9y8oJW9_ILj4xuppu0lzi_H7VTkS8xj5SdX3coE0oimYwxIi2emTAue0UOa5dpgFGyBJ4c8tQ2VF402XRugKDTP8akYhFo5tAA77Qe_NmtuYZc3C3m3I24G2GvR5sSDxUyAN2zq8Lfn9EUms6rY3Ob8YeiKkTiBj0
        type: string
        x-nullable: false
      dq:
        example: s9lAH9fggBsoFR8Oac2R_E2gw282rT2kGOAhvIllETE1efrA6huUUvMfBcMpn8lqeW6vzznYY5SSQF7pMdC_agI3nG8Ibp1BUb0JUiraRNqUfLhcQb_d9GF4Dh7e74WbRsobRonujTYN1xCaP6TO61jvWrX-L18txXw494Q_cgk
        type: string
        x-nullable: false
      e:
        example: AQAB
        type: string
        x-nullable: false
      k:
        example: GawgguFyGrWKav7AX4VKUg
        type: string
        x-nullable: false
      kid:
        description: |-
          The "kid" (key ID) parameter is used to match a specific key.  This
          is used, for instance, to choose among a set of keys within a JWK Set
          during key rollover.  The structure of the "kid" value is
          unspecified.  When "kid" values are used within a JWK Set, different
          keys within the JWK Set SHOULD use distinct "kid" values.  (One
          example in which different keys might use the same "kid" value is if
          they have different "kty" (key type) values but are considered to be
          equivalent alternatives by the application using them.)  The "kid"
          value is a case-sensitive string.
        example: 1603dfe0af8f4596
        type: string
        x-nullable: false
      kty:
        description: |-
          The "kty" (key type) parameter identifies the cryptographic algorithm
          family used with the key, such as "RSA" or "EC". "kty" values should
          either be registered in the IANA "JSON Web Key Types" registry
          established by [JWA] or be a value that contains a Collision-
          Resistant Name.  The "kty" value is a case-sensitive string.
        example: RSA
        type: string
        x-nullable: false
      "n":
        example: vTqrxUyQPl_20aqf5kXHwDZrel-KovIp8s7ewJod2EXHl8tWlRB3_Rem34KwBfqlKQGp1nqah-51H4Jzruqe0cFP58hPEIt6WqrvnmJCXxnNuIB53iX_uUUXXHDHBeaPCSRoNJzNysjoJ30TIUsKBiirhBa7f235PXbKiHducLevV6PcKxJ5cY8zO286qJLBWSPm-OIevwqsIsSIH44Qtm9sioFikhkbLwoqwWORGAY0nl6XvVOlhADdLjBSqSAeT1FPuCDCnXwzCDR8N9IFB_IjdStFkC-rVt2K5BYfPd0c3yFp_vHR15eRd0zJ8XQ7woBC8Vnsac6Et1pKS59pX6256DPWu8UDdEOolKAPgcd_g2NpA76cAaF_jcT80j9KrEzw8Tv0nJBGesuCjPNjGs_KzdkWTUXt23Hn9QJsdc1MZuaW0iqXBepHYfYoqNelzVte117t4BwVp0kUM6we0IqyXClaZgOI8S-WDBw2_Ovdm8e5NmhYAblEVoygcX8Y46oH6bKiaCQfKCFDMcRgChme7AoE1yZZYsPbaG_3IjPrC4LBMHQw8rM9dWjJ8ImjicvZ1pAm0dx-KHCP3y5PVKrxBDf1zSOsBRkOSjB8TPODnJMz6-jd5hTtZxpZPwPoIdCanTZ3ZD6uRBpTmDwtpRGm63UQs1m5FWPwb0T2IF0
        type: string
        x-nullable: false
      p:
        example: 6NbkXwDWUhi-eR55Cgbf27FkQDDWIamOaDr0rj1q0f1fFEz1W5A_09YvG09Fiv1AO2-D8Rl8gS1Vkz2i0zCSqnyy8A025XOcRviOMK7nIxE4OH_PEsko8dtIrb3TmE2hUXvCkmzw9EsTF1LQBOGC6iusLTXepIC1x9ukCKFZQvdgtEObQ5kzd9Nhq-cdqmSeMVLoxPLd1blviVT9Vm8-y12CtYpeJHOaIDtVPLlBhJiBoPKWg3vxSm4XxIliNOefqegIlsmTIa3MpS6WWlCK3yHhat0Q-rRxDxdyiVdG_wzJvp0Iw_2wms7pe-PgNPYvUWH9JphWP5K38YqEBiJFXQ
        type: string
        x-nullable: false
      q:
        example: 0A1FmpOWR91_RAWpqreWSavNaZb9nXeKiBo0DQGBz32DbqKqQ8S4aBJmbRhJcctjCLjain-ivut477tAUMmzJwVJDDq2MZFwC9Q-4VYZmFU4HJityQuSzHYe64RjN-E_NQ02TWhG3QGW6roq6c57c99rrUsETwJJiwS8M5p15Miuz53DaOjv-uqqFAFfywN5WkxHbraBcjHtMiQuyQbQqkCFh-oanHkwYNeytsNhTu2mQmwR5DR2roZ2nPiFjC6nsdk-A7E3S3wMzYYFw7jvbWWoYWo9vB40_MY2Y0FYQSqcDzcBIcq_0tnnasf3VW4Fdx6m80RzOb2Fsnln7vKXAQ
        type: string
        x-nullable: false
      qi:
        example: GyM_p6JrXySiz1toFgKbWV-JdI3jQ4ypu9rbMWx3rQJBfmt0FoYzgUIZEVFEcOqwemRN81zoDAaa-Bk0KWNGDjJHZDdDmFhW3AN7lI-puxk_mHZGJ11rxyR8O55XLSe3SPmRfKwZI6yU24ZxvQKFYItdldUKGzO6Ia6zTKhAVRU
        type: string
        x-nullable: false
      use:
        description: |-
          Use ("public key use") identifies the intended use of
          the public key. The "use" parameter is employed to indicate whether
          a public key is used for encrypting data or verifying the signature
          on data. Values are commonly "sig" (signature) or "enc" (encryption).
        example: sig
        type: string
        x-nullable: false
      x:
        example: f83OJ3D2xF1Bg8vub9tLe1gHMzV76e8Tus9uPHvRVEU
        type: string
        x-nullable: false
      x5c:
        description: |-
          The "x5c" (X.509 certificate chain) parameter contains a chain of one
          or more PKIX certificates [RFC5280].  The certificate chain is
          represented as a JSON array of certificate value strings.  Each
          string in the array is a base64-encoded (Section 4 of [RFC4648] --
          not base64url-encoded) DER [ITU.X690.1994] PKIX certificate value.
          The PKIX certificate containing the key value MUST be the first
          certificate.
        items:
          type: string
        type: array
        x-nullable: false
      x5t:
        example: GawgguFyGrWKav7AX4VKUg
        type: string
        x-nullable: false
      x5t#S256:
        example: GawgguFyGrWKav7AX4VKUg
        type: string
        x-nullable: false
      "y":
        example: x_FEzRu9m36HLN_tue659LNpXW6pCyStikYjKIWI5a0
        type: string
        x-nullable: false
    required:
    - kty
    type: object
  ClientJWKs:
    properties:
      keys:
        example: []
        items:
          $ref: '#/definitions/ClientJWK'
        type: array
        x-nullable: false
    type: object
  ClientPrivacy:
    properties:
      scopes:
        additionalProperties:
          $ref: '#/definitions/ScopePrivacyInformation'
        description: optional privacy information mapping for scopes
        type: object
        x-nullable: false
    type: object
  ClientTokenExchangeConfiguration:
    properties:
      actor_claims:
        description: |-
          Additional actor token claims

          Claims from the actor token that will be injected into the exchanged token under the `act` claim.

          Applies for the token exchange delegation flow only.
        items:
          type: string
        type: array
        x-nullable: false
    type: object
  Confirmation:
    properties:
      jkt:
        type: string
        x-nullable: false
      x5t#S256:
        type: string
        x-nullable: false
    type: object
  ConsentAccepted:
    properties:
      redirect_to:
        description: |-
          A URL to redirect the user.
          It applies for the redirect flow only, i.e the consent page.
        example: https://authorization.cloudentity.com:8443/tenant/server/oauth2/authorize?client_id=bugkgm23g9kregtu051g&consent_verified=true&login_id=cavai7d8s9nelp7k792g&login_state=cauq8fonbud6q8806bf0
        type: string
        x-nullable: false
    type: object
  ConsentRejected:
    properties:
      redirect_to:
        description: |-
          A URL to redirect the user.
          It applies for the redirect flow only, i.e the consent page.
        example: https://authorization.cloudentity.com:8443/tenant/server/oauth2/authorize?client_id=bugkgm23g9kregtu051g&consent_verified=true&login_id=cavai7d8s9nelp7k792g&login_state=cauq8fonbud6q8806bf0
        type: string
        x-nullable: false
    type: object
  ConsentType:
    type: string
  ConsentsRemovedResponse:
    properties:
      number_of_consents_removed:
        format: int64
        type: integer
        x-nullable: false
    type: object
  DynamicClientRegistrationJSONRequest:
    properties:
      application_type:
        default: web
        description: |-
          The client application type.

          Client applications can be either of a `web` or `native` types.

          Web applications include clients like server web applications or service apps.

          Native applications include single-page applications (SPAs) and mobile or desktop
          applications.

          Apply security measures according to the type of your application.
        example: web
        type: string
        x-nullable: false
      application_types:
        default:
        - server_web
        description: An array of dynamically calculated application types that can
          be used for filtering
        example:
        - single_page
        - server_web
        - mobile_desktop
        - service
        - legacy
        - dcr
        items:
          enum:
          - single_page
          - server_web
          - mobile_desktop
          - service
          - legacy
          - dcr
          type: string
        readOnly: true
        type: array
        x-nullable: false
      audience:
        default:
        - client_id
        description: |-
          Identity of the intended recipients (the audience).

          Typically, the audience is a single resource server or a list of resource servers.

          It is considered a good practice to limit the audience of the token for security purposes.
        items:
          type: string
        type: array
        x-nullable: false
      authorization_details_types:
        description: |-
          Authorization details types

          Indicates what authorization details types the client can use.
        items:
          $ref: '#/definitions/AuthorizationDetailType'
        type: array
        x-nullable: false
      authorization_encrypted_response_alg:
        description: |-
          Algorithm used for encrypting authorization responses.

          If both signing and encryption are requested, the response is first signed, and then encrypted.
          As a result, a Nested JWT is obtained, as defined in JWT [RFC7519].

          If omitted, no encryption is applied by default.
        enum:
        - RSA-OAEP
        - RSA-OAEP-256
        example: RSA-OAEP-256
        type: string
        x-nullable: false
      authorization_encrypted_response_enc:
        description: |-
          Algorithm used for encrypting authorization responses.

          With `authorization_encrypted_response_alg` specified, the default value is `A128CBC-HS256`.
          When `authorization_encrypted_response_enc` is included, `authorization_encrypted_response_alg`
          MUST also be provided in a request.
        enum:
        - A256GCM
        - A128CBC-HS256
        example: A128CBC-HS256
        type: string
        x-nullable: false
      authorization_signed_response_alg:
        description: |-
          Algorithm used for signing authorization responses.

          With this parameter specified, the response is signed using JWS and according to the configured algorithm.

          `none` isn't allowed.
        example: RS256
        type: string
        x-nullable: false
      backchannel_authentication_request_signing_alg:
        description: |-
          OPTIONAL. The JWS alg algorithm value used by the client application to sign authentication requests.

          When omitted, the client application doesn't send signed authentication requests.
        type: string
        x-nullable: false
      backchannel_client_notification_endpoint:
        description: |-
          REQUIRED for requests when the client application uses CIBA as an authorization grant type, and the token
          delivery mode is set to `ping` or `push`.

          This parameter is the endpoint where an OP (OpenID Provider) posts a notification after end-user authentication.

          Input: an HTTPS URL.
        type: string
        x-nullable: false
      backchannel_token_delivery_mode:
        description: |-
          REQUIRED for requests when the client application uses CIBA as an authorization grant type.

          Input: `poll`, `ping`, or `push`.
        type: string
        x-nullable: false
      backchannel_user_code_parameter:
        description: |-
          OPTIONAL. A boolean value indicating the `user_code` parameter support by the client application.

          If omitted, the default value is `false`.

          This applies only when the `backchannel_user_code_parameter_supported` OP parameter is `true`.
        type: boolean
        x-nullable: false
      client_id_issued_at:
        description: |-
          The client identifier time of issue.

          The value is the number of seconds between 1970-01-01T00:00:00Z (UTC) and the date/time of issue.
        format: int64
        type: integer
        x-nullable: false
      client_name:
        description: Human-readable name of a client application.
        example: My app
        type: string
        x-nullable: false
      client_secret_expires_at:
        description: |-
          The client secret expiration time.

          If the client secret does not expire, `client_secret_expires_at` = `0`.
        format: int64
        type: integer
        x-nullable: false
      client_uri:
        description: URI of a client application.
        type: string
        x-nullable: false
      created_at:
        description: Date when the client application was created.
        example: "2022-04-07T19:17:31.323187Z"
        format: date-time
        type: string
        x-nullable: false
      description:
        description: Description of the client application.
        type: string
        x-nullable: false
      dpop_bound_access_tokens:
        description: |-
          Boolean value specifying whether the client always uses DPoP for token requests
          If true, the authorization server will reject token requests from this client that do not contain the DPoP header.
        type: boolean
        x-nullable: false
      grant_types:
        default:
        - authorization_code
        description: |-
          An array of allowed OAuth client grant types.

          The `grantTypes` array stores OAuth flows that are allowed for a given client application.

          [Read more](https://cloudentity.com/developers/basics/oauth-grant-types/grant-types-overview/) about grant types.
        example:
        - password
        - refresh_token
        - client_credentials
        - implicit
        - authorization_code
        items:
          enum:
          - authorization_code
          - implicit
          - client_credentials
          - refresh_token
          - password
          - urn:ietf:params:oauth:grant-type:jwt-bearer
          - urn:openid:params:grant-type:ciba
          - urn:ietf:params:oauth:grant-type:token-exchange
          - urn:ietf:params:oauth:grant-type:device_code
          type: string
        type: array
        x-nullable: false
      id_token_encrypted_response_alg:
        description: JWE alg algorithm for encrypting the ID token issued to this
          client application.
        enum:
        - RSA-OAEP
        - RSA-OAEP-256
        type: string
        x-nullable: false
      id_token_encrypted_response_enc:
        description: JWE enc algorithm for encrypting the ID token issued to this
          client application.
        enum:
        - A256GCM
        - A128CBC-HS256
        type: string
        x-nullable: false
      id_token_signed_response_alg:
        description: |-
          Algorithm for signing ID tokens issued for a client application.

          The default value depends on authorization server configuration.
        enum:
        - RS256
        - ES256
        - PS256
        example: ES256
        type: string
        x-nullable: false
      introspection_endpoint_auth_method:
        default: client_secret_basic
        description: |-
          An introspection endpoint authentication method configured for the client application (read-only).

          If empty, the `token_endpoint_auth_method` is used.

          Cloudentity supports the following client authentication methods:
          `client_secret_basic`, `client_secret_post`, `client_secret_jwt`, `private_key_jwt`,
          `self_signed_tls_client_auth`, `tls_client_auth`, `none`.

          [Read more](https://cloudentity.com/developers/basics/oauth-client-authentication/client-authentication-overview/) about client authentication.
        enum:
        - client_secret_basic
        - client_secret_post
        - client_secret_jwt
        - private_key_jwt
        - self_signed_tls_client_auth
        - tls_client_auth
        - none
        example: client_secret_basic
        type: string
        x-nullable: false
      jwks:
        $ref: '#/definitions/ClientJWKs'
      jwks_uri:
        description: A URL of JSON Web Key Set with the public keys used by a client
          application to authenticate to Cloudentity.
        type: string
        x-nullable: false
      logo_uri:
        description: Logo URI.
        type: string
        x-nullable: false
      organisation_id:
        description: |-
          External organization identifier. It is a unique string assigned by the CDR Register to identify an Accredited
          Data Recipient Brand.

          The value obtained is used as the `aud` claim for message signing, for example, when a JSON Web Token (JWT) is
          required for authorization, and represents the audience(s) the JWT is intended for.
        example: 5647fe90-f6bc-11eb-9a03-0242ac130003
        type: string
        x-nullable: false
      policy_uri:
        description: Policy URL to read about how the profile data is used.
        type: string
        x-nullable: false
      post_logout_redirect_uris:
        description: Array of URLs to which a relying party may request that the user
          be redirected after a logout has been performed.
        items:
          type: string
        type: array
        x-nullable: false
      privacy:
        $ref: '#/definitions/ClientPrivacy'
      redirect_uris:
        $ref: '#/definitions/RedirectURIs'
      request_object_encryption_alg:
        description: Optional JWE alg algorithm the client is declaring that it may
          use for encrypting Request Objects
        enum:
        - RSA-OAEP
        - RSA-OAEP-256
        example: RSA-OAEP
        type: string
        x-nullable: false
      request_object_encryption_enc:
        description: |-
          Optional JWE enc algorithm the client is declaring that it may use for encrypting Request Objects
          When `request_object_encryption_enc` is included, `request_object_encryption_alg` MUST also be provided.
        enum:
        - A256GCM
        - A128CBC-HS256
        example: A256GCM
        type: string
        x-nullable: false
      request_object_signing_alg:
        default: any
        description: |-
          Request object signing algorithm for the token endpoint

          Cloudentity supports signing tokens with the RS256, ES256, and PS256 algorithms. If you do not want
          to use a signing algorithm, set the value of this parameter to `none`.
        enum:
        - any
        - none
        - RS256
        - ES256
        - PS256
        example: none
        type: string
        x-nullable: false
      request_uris:
        description: Array of absolute URIs that points to the Request Object that
          holds authorization request parameters.
        items:
          type: string
        type: array
        x-nullable: false
      require_pushed_authorization_requests:
        description: Boolean parameter indicating whether the only means of initiating
          an authorization request the client is allowed to use is PAR.
        type: boolean
        x-nullable: false
      response_types:
        $ref: '#/definitions/ResponseTypes'
      revocation_endpoint_auth_method:
        default: client_secret_basic
        description: |-
          A revocation endpoint authentication method configured for the client application (read-only).
          If empty, the `token_endpoint_auth_method` is used.

          Cloudentity supports the following client authentication methods:
          `client_secret_basic`, `client_secret_post`, `client_secret_jwt`, `private_key_jwt`,
          `self_signed_tls_client_auth`, `tls_client_auth`, `none`.

          [Read more](https://cloudentity.com/developers/basics/oauth-client-authentication/client-authentication-overview/) about client authentication.
        enum:
        - client_secret_basic
        - client_secret_post
        - client_secret_jwt
        - private_key_jwt
        - self_signed_tls_client_auth
        - tls_client_auth
        - none
        example: client_secret_basic
        type: string
        x-nullable: false
      scope:
        description: Space-separated list of scopes for compatibility with the OAuth
          specification.
        example: email offline_access openid
        type: string
        x-nullable: false
      scopes:
        description: An array of string represented scopes assigned to a client application
        example:
        - email
        - offline_access
        - openid
        items:
          type: string
        type: array
        x-nullable: false
      sector_identifier_uri:
        description: |-
          OPTIONAL. [A URL using the HTTPS scheme](https://openid.net/specs/openid-connect-registration-1_0.html#SectorIdentifierValidation).
          It must reference a JSON file with the array of `redirect_uri` values.

          Pass this parameter when you use multiple domains in your `redirect_uris` or need a mechanism to allow changes in
          domain without affecting consumer consent.

          [Read more](https://openid.net/specs/openid-connect-core-1_0.html)
        type: string
        x-nullable: false
      software_id:
        description: |-
          A unique identifier string (e.g., a Universally Unique Identifier
          (UUID)) assigned by the client developer or software publisher.

          The `software_id` MUST remain the same across
          multiple updates or versions of the same software package. The
          value of this field is not intended to be human-readable and is
          usually opaque to the client and authorization server.
        type: string
        x-nullable: false
      software_statement:
        description: |-
          A digitally signed or MACed JSON Web Token (JWT) [RFC7519] that
          asserts metadata values about the client software.  In some cases,
          a software statement is issued directly by the client
          developer.  In other cases, a software statement is issued by
          a third-party organization for use by the client developer.

          In both cases, the trust relationship the authorization server has
          with the issuer of the software statement is intended to be used
          as an input to the evaluation of whether the registration request
          is accepted.

          A software statement can be presented to an
          authorization server as part of the client registration request.
        type: string
        x-nullable: false
      software_version:
        description: |-
          A version identifier string for the client software identified by
          `software_id`. The value of the `software_version` MUST be changed
          with any update of the client software identified by the same
          `software_id`.
        type: string
        x-nullable: false
      subject_type:
        default: public
        description: |-
          Subject identifier type

          Stores information if the subject identifier is of the `public` or the `pairwise` type.

          Subject identifiers identify an end-user. They are locally unique and never reassigned within the Issuer,
          and are intended to be consumed by client applications. There are two types
          of subject identifiers: `public` and `pairwise`.

          For the `public` type, the value of the `sub` (subject) token claim is the same for all clients.

          For the `pairwise` type, a different `sub` (subject) token claim is provided for each client.
          Using the `pairwise` subject identifier makes it impossible for client applications to correlate the end-user's
          activity without their permission.
        enum:
        - public
        - pairwise
        example: public
        type: string
        x-nullable: false
      tls_client_auth_san_dns:
        description: A string containing the value of an expected dNSName SAN entry
          in the certificate.
        type: string
        x-nullable: false
      tls_client_auth_san_email:
        description: A string containing the value of an expected rfc822Name SAN entry
          in the certificate.
        type: string
        x-nullable: false
      tls_client_auth_san_ip:
        description: A string representation of an IP address in either dotted decimal
          notation (for IPv4) or colon-delimited hexadecimal (for IPv6, as defined
          in [RFC5952]) that is expected to be present as an iPAddress SAN entry in
          the certificate.
        type: string
        x-nullable: false
      tls_client_auth_san_uri:
        description: A string containing the value of an expected uniformResourceIdentifier
          SAN entry in the certificate.
        type: string
        x-nullable: false
      tls_client_auth_subject_dn:
        description: An [RFC4514] string representation of the expected subject distinguished
          name of the certificate.
        type: string
        x-nullable: false
      tls_client_certificate_bound_access_tokens:
        description: Boolean value indicating server support for mutual TLS client
          certificate-bound access tokens. If omitted, the default value is "false".
        type: boolean
        x-nullable: false
      token_endpoint_auth_method:
        default: client_secret_basic
        description: |-
          Token endpoint authentication method configured for a client application

          Cloudentity supports the following client authentication methods:
          `client_secret_basic`, `client_secret_post`, `client_secret_jwt`, `private_key_jwt`,
          `self_signed_tls_client_auth`, `tls_client_auth`, `none`.

          To learn more, go to the Authorization Basics > Client Authentication section of this guide.
        enum:
        - client_secret_basic
        - client_secret_post
        - client_secret_jwt
        - private_key_jwt
        - self_signed_tls_client_auth
        - tls_client_auth
        - none
        example: client_secret_basic
        type: string
        x-nullable: false
      token_endpoint_auth_signing_alg:
        default: ES256
        description: |-
          Signing algorithm for the token endpoint

          This field is optional. If empty, a client can use any algorithm supported by the server (see `token_endpoint_auth_signing_alg_values_supported` in the well-known endpoing).

          If provided, depending on the server configuration, client can use of one: HS256, RS256, ES256, PS256 algorithms.

          If your token endpoint authentication is set to the `private_key_jwt` method, the
          `token_endpoint_auth_signing_alg` parameter must be either RS256, ES256, or PS256.

          If your token endpoint authentication is set to the `client_secret_jwt` method,
          the `token_endpoint_auth_signing_alg` parameter must be HS256.
        enum:
        - RS256
        - ES256
        - PS256
        - HS256
        - ""
        example: ES256
        type: string
        x-nullable: false
      token_exchange:
        $ref: '#/definitions/ClientTokenExchangeConfiguration'
      tos_uri:
        description: Terms of Service URL.
        type: string
        x-nullable: false
      updated_at:
        description: Date when the client application was updated.
        example: "2022-05-08T01:11:51.1262916Z"
        format: date-time
        type: string
        x-nullable: false
      userinfo_signed_response_alg:
        default: none
        description: |-
          JWS alg algorithm REQUIRED for signing UserInfo Responses.

          If specified, the response is a JWT serialized and signed with JWS.

          If omitted, then by default, UserInfo Response returns the Claims
          as an UTF-8 encoded JSON object using the application/json content-type.
        enum:
        - none
        - RS256
        - ES256
        example: none
        type: string
        x-nullable: false
      webhook_uris:
        items:
          type: string
        type: array
        x-nullable: false
    type: object
  DynamicClientRegistrationResponse:
    properties:
      application_type:
        default: web
        description: |-
          The client application type.

          Client applications can be either of a `web` or `native` types.

          Web applications include clients like server web applications or service apps.

          Native applications include single-page applications (SPAs) and mobile or desktop
          applications.

          Apply security measures according to the type of your application.
        example: web
        type: string
        x-nullable: false
      application_types:
        default:
        - server_web
        description: An array of dynamically calculated application types that can
          be used for filtering
        example:
        - single_page
        - server_web
        - mobile_desktop
        - service
        - legacy
        - dcr
        items:
          enum:
          - single_page
          - server_web
          - mobile_desktop
          - service
          - legacy
          - dcr
          type: string
        readOnly: true
        type: array
        x-nullable: false
      audience:
        default:
        - client_id
        description: |-
          Identity of the intended recipients (the audience).

          Typically, the audience is a single resource server or a list of resource servers.

          It is considered a good practice to limit the audience of the token for security purposes.
        items:
          type: string
        type: array
        x-nullable: false
      authorization_details_types:
        description: |-
          Authorization details types

          Indicates what authorization details types the client can use.
        items:
          $ref: '#/definitions/AuthorizationDetailType'
        type: array
        x-nullable: false
      authorization_encrypted_response_alg:
        description: |-
          Algorithm used for encrypting authorization responses.

          If both signing and encryption are requested, the response is first signed, and then encrypted.
          As a result, a Nested JWT is obtained, as defined in JWT [RFC7519].

          If omitted, no encryption is applied by default.
        enum:
        - RSA-OAEP
        - RSA-OAEP-256
        example: RSA-OAEP-256
        type: string
        x-nullable: false
      authorization_encrypted_response_enc:
        description: |-
          Algorithm used for encrypting authorization responses.

          With `authorization_encrypted_response_alg` specified, the default value is `A128CBC-HS256`.
          When `authorization_encrypted_response_enc` is included, `authorization_encrypted_response_alg`
          MUST also be provided in a request.
        enum:
        - A256GCM
        - A128CBC-HS256
        example: A128CBC-HS256
        type: string
        x-nullable: false
      authorization_signed_response_alg:
        description: |-
          Algorithm used for signing authorization responses.

          With this parameter specified, the response is signed using JWS and according to the configured algorithm.

          `none` isn't allowed.
        example: RS256
        type: string
        x-nullable: false
      backchannel_authentication_request_signing_alg:
        description: |-
          OPTIONAL. The JWS alg algorithm value used by the client application to sign authentication requests.

          When omitted, the client application doesn't send signed authentication requests.
        type: string
        x-nullable: false
      backchannel_client_notification_endpoint:
        description: |-
          REQUIRED for requests when the client application uses CIBA as an authorization grant type, and the token
          delivery mode is set to `ping` or `push`.

          This parameter is the endpoint where an OP (OpenID Provider) posts a notification after end-user authentication.

          Input: an HTTPS URL.
        type: string
        x-nullable: false
      backchannel_token_delivery_mode:
        description: |-
          REQUIRED for requests when the client application uses CIBA as an authorization grant type.

          Input: `poll`, `ping`, or `push`.
        type: string
        x-nullable: false
      backchannel_user_code_parameter:
        description: |-
          OPTIONAL. A boolean value indicating the `user_code` parameter support by the client application.

          If omitted, the default value is `false`.

          This applies only when the `backchannel_user_code_parameter_supported` OP parameter is `true`.
        type: boolean
        x-nullable: false
      client_id:
        description: |-
          OAuth client application identifier

          If not provided, a random client ID is generated.
        format: clientID
        type: string
        x-nullable: false
      client_id_issued_at:
        description: |-
          The client identifier time of issue.

          The value is the number of seconds between 1970-01-01T00:00:00Z (UTC) and the date/time of issue.
        format: int64
        type: integer
        x-nullable: false
      client_name:
        description: Human-readable name of a client application.
        example: My app
        type: string
        x-nullable: false
      client_secret:
        description: |-
          OAuth client secret

          If not provided, a random client secret is generated.
        minLength: 32
        type: string
        x-nullable: false
      client_secret_expires_at:
        description: |-
          The client secret expiration time.

          If the client secret does not expire, `client_secret_expires_at` = `0`.
        format: int64
        type: integer
        x-nullable: false
      client_uri:
        description: URI of a client application.
        type: string
        x-nullable: false
      created_at:
        description: Date when the client application was created.
        example: "2022-04-07T19:17:31.323187Z"
        format: date-time
        type: string
        x-nullable: false
      description:
        description: Description of the client application.
        type: string
        x-nullable: false
      dpop_bound_access_tokens:
        description: |-
          Boolean value specifying whether the client always uses DPoP for token requests
          If true, the authorization server will reject token requests from this client that do not contain the DPoP header.
        type: boolean
        x-nullable: false
      dynamically_registered:
        type: boolean
        x-nullable: false
      grant_types:
        default:
        - authorization_code
        description: |-
          An array of allowed OAuth client grant types.

          The `grantTypes` array stores OAuth flows that are allowed for a given client application.

          [Read more](https://cloudentity.com/developers/basics/oauth-grant-types/grant-types-overview/) about grant types.
        example:
        - password
        - refresh_token
        - client_credentials
        - implicit
        - authorization_code
        items:
          enum:
          - authorization_code
          - implicit
          - client_credentials
          - refresh_token
          - password
          - urn:ietf:params:oauth:grant-type:jwt-bearer
          - urn:openid:params:grant-type:ciba
          - urn:ietf:params:oauth:grant-type:token-exchange
          - urn:ietf:params:oauth:grant-type:device_code
          type: string
        type: array
        x-nullable: false
      hashed_rotated_secrets:
        description: An array of hashed rotated client secrets
        items:
          type: string
        type: array
        x-nullable: false
      hashed_secret:
        description: |-
          Hashed client secret

          Hashing client secrets provides additional security for your secrets storage as it hides
          plaintext secrets from being viewed both in the UI and the database.
        type: string
        x-nullable: false
      id_token_encrypted_response_alg:
        description: JWE alg algorithm for encrypting the ID token issued to this
          client application.
        enum:
        - RSA-OAEP
        - RSA-OAEP-256
        type: string
        x-nullable: false
      id_token_encrypted_response_enc:
        description: JWE enc algorithm for encrypting the ID token issued to this
          client application.
        enum:
        - A256GCM
        - A128CBC-HS256
        type: string
        x-nullable: false
      id_token_signed_response_alg:
        description: |-
          Algorithm for signing ID tokens issued for a client application.

          The default value depends on authorization server configuration.
        enum:
        - RS256
        - ES256
        - PS256
        example: ES256
        type: string
        x-nullable: false
      introspection_endpoint_auth_method:
        default: client_secret_basic
        description: |-
          An introspection endpoint authentication method configured for the client application (read-only).

          If empty, the `token_endpoint_auth_method` is used.

          Cloudentity supports the following client authentication methods:
          `client_secret_basic`, `client_secret_post`, `client_secret_jwt`, `private_key_jwt`,
          `self_signed_tls_client_auth`, `tls_client_auth`, `none`.

          [Read more](https://cloudentity.com/developers/basics/oauth-client-authentication/client-authentication-overview/) about client authentication.
        enum:
        - client_secret_basic
        - client_secret_post
        - client_secret_jwt
        - private_key_jwt
        - self_signed_tls_client_auth
        - tls_client_auth
        - none
        example: client_secret_basic
        type: string
        x-nullable: false
      jwks:
        $ref: '#/definitions/ClientJWKs'
      jwks_uri:
        description: A URL of JSON Web Key Set with the public keys used by a client
          application to authenticate to Cloudentity.
        type: string
        x-nullable: false
      logo_uri:
        description: Logo URI.
        type: string
        x-nullable: false
      organisation_id:
        description: |-
          External organization identifier. It is a unique string assigned by the CDR Register to identify an Accredited
          Data Recipient Brand.

          The value obtained is used as the `aud` claim for message signing, for example, when a JSON Web Token (JWT) is
          required for authorization, and represents the audience(s) the JWT is intended for.
        example: 5647fe90-f6bc-11eb-9a03-0242ac130003
        type: string
        x-nullable: false
      policy_uri:
        description: Policy URL to read about how the profile data is used.
        type: string
        x-nullable: false
      post_logout_redirect_uris:
        description: Array of URLs to which a relying party may request that the user
          be redirected after a logout has been performed.
        items:
          type: string
        type: array
        x-nullable: false
      privacy:
        $ref: '#/definitions/ClientPrivacy'
      redirect_uris:
        $ref: '#/definitions/RedirectURIs'
      registration_access_token:
        description: Registration token that can be used for client management
        type: string
        x-nullable: false
      registration_access_token_expires_in:
        description: Registration token expiration time in seconds. If 0 value is
          returned, token never expires.
        format: int64
        type: integer
        x-nullable: false
      registration_client_uri:
        description: URL for client management
        type: string
        x-nullable: false
      request_object_encryption_alg:
        description: Optional JWE alg algorithm the client is declaring that it may
          use for encrypting Request Objects
        enum:
        - RSA-OAEP
        - RSA-OAEP-256
        example: RSA-OAEP
        type: string
        x-nullable: false
      request_object_encryption_enc:
        description: |-
          Optional JWE enc algorithm the client is declaring that it may use for encrypting Request Objects
          When `request_object_encryption_enc` is included, `request_object_encryption_alg` MUST also be provided.
        enum:
        - A256GCM
        - A128CBC-HS256
        example: A256GCM
        type: string
        x-nullable: false
      request_object_signing_alg:
        default: any
        description: |-
          Request object signing algorithm for the token endpoint

          Cloudentity supports signing tokens with the RS256, ES256, and PS256 algorithms. If you do not want
          to use a signing algorithm, set the value of this parameter to `none`.
        enum:
        - any
        - none
        - RS256
        - ES256
        - PS256
        example: none
        type: string
        x-nullable: false
      request_uris:
        description: Array of absolute URIs that points to the Request Object that
          holds authorization request parameters.
        items:
          type: string
        type: array
        x-nullable: false
      require_pushed_authorization_requests:
        description: Boolean parameter indicating whether the only means of initiating
          an authorization request the client is allowed to use is PAR.
        type: boolean
        x-nullable: false
      response_types:
        $ref: '#/definitions/ResponseTypes'
      revocation_endpoint_auth_method:
        default: client_secret_basic
        description: |-
          A revocation endpoint authentication method configured for the client application (read-only).
          If empty, the `token_endpoint_auth_method` is used.

          Cloudentity supports the following client authentication methods:
          `client_secret_basic`, `client_secret_post`, `client_secret_jwt`, `private_key_jwt`,
          `self_signed_tls_client_auth`, `tls_client_auth`, `none`.

          [Read more](https://cloudentity.com/developers/basics/oauth-client-authentication/client-authentication-overview/) about client authentication.
        enum:
        - client_secret_basic
        - client_secret_post
        - client_secret_jwt
        - private_key_jwt
        - self_signed_tls_client_auth
        - tls_client_auth
        - none
        example: client_secret_basic
        type: string
        x-nullable: false
      rotated_secrets:
        description: An array of rotated OAuth client secrets
        items:
          type: string
        minLength: 32
        type: array
        x-nullable: false
      scope:
        description: Space-separated list of scopes for compatibility with the OAuth
          specification.
        example: email offline_access openid
        type: string
        x-nullable: false
      scopes:
        description: An array of string represented scopes assigned to a client application
        example:
        - email
        - offline_access
        - openid
        items:
          type: string
        type: array
        x-nullable: false
      sector_identifier_uri:
        description: |-
          OPTIONAL. [A URL using the HTTPS scheme](https://openid.net/specs/openid-connect-registration-1_0.html#SectorIdentifierValidation).
          It must reference a JSON file with the array of `redirect_uri` values.

          Pass this parameter when you use multiple domains in your `redirect_uris` or need a mechanism to allow changes in
          domain without affecting consumer consent.

          [Read more](https://openid.net/specs/openid-connect-core-1_0.html)
        type: string
        x-nullable: false
      software_id:
        description: |-
          A unique identifier string (e.g., a Universally Unique Identifier
          (UUID)) assigned by the client developer or software publisher.

          The `software_id` MUST remain the same across
          multiple updates or versions of the same software package. The
          value of this field is not intended to be human-readable and is
          usually opaque to the client and authorization server.
        type: string
        x-nullable: false
      software_statement:
        type: string
        x-nullable: false
      software_version:
        description: |-
          A version identifier string for the client software identified by
          `software_id`. The value of the `software_version` MUST be changed
          with any update of the client software identified by the same
          `software_id`.
        type: string
        x-nullable: false
      subject_type:
        default: public
        description: |-
          Subject identifier type

          Stores information if the subject identifier is of the `public` or the `pairwise` type.

          Subject identifiers identify an end-user. They are locally unique and never reassigned within the Issuer,
          and are intended to be consumed by client applications. There are two types
          of subject identifiers: `public` and `pairwise`.

          For the `public` type, the value of the `sub` (subject) token claim is the same for all clients.

          For the `pairwise` type, a different `sub` (subject) token claim is provided for each client.
          Using the `pairwise` subject identifier makes it impossible for client applications to correlate the end-user's
          activity without their permission.
        enum:
        - public
        - pairwise
        example: public
        type: string
        x-nullable: false
      tls_client_auth_san_dns:
        description: A string containing the value of an expected dNSName SAN entry
          in the certificate.
        type: string
        x-nullable: false
      tls_client_auth_san_email:
        description: A string containing the value of an expected rfc822Name SAN entry
          in the certificate.
        type: string
        x-nullable: false
      tls_client_auth_san_ip:
        description: A string representation of an IP address in either dotted decimal
          notation (for IPv4) or colon-delimited hexadecimal (for IPv6, as defined
          in [RFC5952]) that is expected to be present as an iPAddress SAN entry in
          the certificate.
        type: string
        x-nullable: false
      tls_client_auth_san_uri:
        description: A string containing the value of an expected uniformResourceIdentifier
          SAN entry in the certificate.
        type: string
        x-nullable: false
      tls_client_auth_subject_dn:
        description: An [RFC4514] string representation of the expected subject distinguished
          name of the certificate.
        type: string
        x-nullable: false
      tls_client_certificate_bound_access_tokens:
        description: Boolean value indicating server support for mutual TLS client
          certificate-bound access tokens. If omitted, the default value is "false".
        type: boolean
        x-nullable: false
      token_endpoint_auth_method:
        default: client_secret_basic
        description: |-
          Token endpoint authentication method configured for a client application

          Cloudentity supports the following client authentication methods:
          `client_secret_basic`, `client_secret_post`, `client_secret_jwt`, `private_key_jwt`,
          `self_signed_tls_client_auth`, `tls_client_auth`, `none`.

          To learn more, go to the Authorization Basics > Client Authentication section of this guide.
        enum:
        - client_secret_basic
        - client_secret_post
        - client_secret_jwt
        - private_key_jwt
        - self_signed_tls_client_auth
        - tls_client_auth
        - none
        example: client_secret_basic
        type: string
        x-nullable: false
      token_endpoint_auth_signing_alg:
        default: ES256
        description: |-
          Signing algorithm for the token endpoint

          This field is optional. If empty, a client can use any algorithm supported by the server (see `token_endpoint_auth_signing_alg_values_supported` in the well-known endpoing).

          If provided, depending on the server configuration, client can use of one: HS256, RS256, ES256, PS256 algorithms.

          If your token endpoint authentication is set to the `private_key_jwt` method, the
          `token_endpoint_auth_signing_alg` parameter must be either RS256, ES256, or PS256.

          If your token endpoint authentication is set to the `client_secret_jwt` method,
          the `token_endpoint_auth_signing_alg` parameter must be HS256.
        enum:
        - RS256
        - ES256
        - PS256
        - HS256
        - ""
        example: ES256
        type: string
        x-nullable: false
      token_exchange:
        $ref: '#/definitions/ClientTokenExchangeConfiguration'
      tos_uri:
        description: Terms of Service URL.
        type: string
        x-nullable: false
      updated_at:
        description: Date when the client application was updated.
        example: "2022-05-08T01:11:51.1262916Z"
        format: date-time
        type: string
        x-nullable: false
      userinfo_signed_response_alg:
        default: none
        description: |-
          JWS alg algorithm REQUIRED for signing UserInfo Responses.

          If specified, the response is a JWT serialized and signed with JWS.

          If omitted, then by default, UserInfo Response returns the Claims
          as an UTF-8 encoded JSON object using the application/json content-type.
        enum:
        - none
        - RS256
        - ES256
        example: none
        type: string
        x-nullable: false
    type: object
  Error:
    description: HTTP error response
    properties:
      details:
        type: object
        x-nullable: false
      error:
        type: string
        x-nullable: false
      status_code:
        format: int64
        type: integer
        x-nullable: false
    type: object
  GetOBBRCustomerDataAccessConsentResponse:
    properties:
      account_ids:
        description: List of account identifiers
        items:
          type: string
        type: array
        x-nullable: false
      authentication_context:
        $ref: '#/definitions/AuthenticationContext'
      client_id:
        description: Client application identifier.
        example: '"cauqo9c9vpbs0aj2b2v0"'
        format: clientID
        type: string
        x-nullable: false
      client_info:
        $ref: '#/definitions/ClientInfo'
      consent_id:
        format: consentID
        type: string
        x-nullable: false
      created_at:
        description: Consent creation time
        format: date-time
        type: string
        x-nullable: false
      customer_data_access_consent:
        $ref: '#/definitions/BrazilCustomerDataAccessConsentV1'
      requested_scopes:
        description: List of requested scopes
        items:
          $ref: '#/definitions/RequestedScope'
        type: array
        x-nullable: false
      server_id:
        description: Server / Workspace identifier.
        example: '"server"'
        format: serverID
        type: string
        x-nullable: false
      status:
        description: Consent status
        type: string
        x-nullable: false
      subject:
        description: Subject
        format: subject
        type: string
        x-nullable: false
      tenant_id:
        description: Tenant identifier.
        example: '"tenant"'
        format: tenantID
        type: string
        x-nullable: false
      type:
        $ref: '#/definitions/ConsentType'
    type: object
  GetOBBRCustomerPaymentConsentResponse:
    properties:
      account_ids:
        description: List of account identifiers
        items:
          type: string
        type: array
        x-nullable: false
      authentication_context:
        $ref: '#/definitions/AuthenticationContext'
      client_id:
        description: Client application identifier.
        example: '"cauqo9c9vpbs0aj2b2v0"'
        format: clientID
        type: string
        x-nullable: false
      client_info:
        $ref: '#/definitions/ClientInfo'
      consent_id:
        format: consentID
        type: string
        x-nullable: false
      created_at:
        description: Consent creation time
        format: date-time
        type: string
        x-nullable: false
      customer_payment_consent:
        $ref: '#/definitions/BrazilCustomerPaymentConsent'
      requested_scopes:
        description: List of requested scopes
        items:
          $ref: '#/definitions/RequestedScope'
        type: array
        x-nullable: false
      server_id:
        description: Server / Workspace identifier.
        example: '"server"'
        format: serverID
        type: string
        x-nullable: false
      status:
        description: Consent status
        type: string
        x-nullable: false
      subject:
        description: Subject
        format: subject
        type: string
        x-nullable: false
      tenant_id:
        description: Tenant identifier.
        example: '"tenant"'
        format: tenantID
        type: string
        x-nullable: false
      type:
        $ref: '#/definitions/ConsentType'
    type: object
  GetOBBRCustomerPaymentConsentResponseV2:
    properties:
      account_ids:
        description: List of account identifiers
        items:
          type: string
        type: array
        x-nullable: false
      authentication_context:
        $ref: '#/definitions/AuthenticationContext'
      client_id:
        description: Client application identifier.
        example: '"cauqo9c9vpbs0aj2b2v0"'
        format: clientID
        type: string
        x-nullable: false
      client_info:
        $ref: '#/definitions/ClientInfo'
      consent_id:
        format: consentID
        type: string
        x-nullable: false
      created_at:
        description: Consent creation time
        format: date-time
        type: string
        x-nullable: false
      customer_payment_consent_v2:
        $ref: '#/definitions/BrazilCustomerPaymentConsentV2'
      requested_scopes:
        description: List of requested scopes
        items:
          $ref: '#/definitions/RequestedScope'
        type: array
        x-nullable: false
      server_id:
        description: Server / Workspace identifier.
        example: '"server"'
        format: serverID
        type: string
        x-nullable: false
      status:
        description: Consent status
        type: string
        x-nullable: false
      subject:
        description: Subject
        format: subject
        type: string
        x-nullable: false
      tenant_id:
        description: Tenant identifier.
        example: '"tenant"'
        format: tenantID
        type: string
        x-nullable: false
      type:
        $ref: '#/definitions/ConsentType'
    type: object
  GetOBBRCustomerPaymentConsentResponseV3:
    properties:
      account_ids:
        description: List of account identifiers
        items:
          type: string
        type: array
        x-nullable: false
      authentication_context:
        $ref: '#/definitions/AuthenticationContext'
      client_id:
        description: Client application identifier.
        example: '"cauqo9c9vpbs0aj2b2v0"'
        format: clientID
        type: string
        x-nullable: false
      client_info:
        $ref: '#/definitions/ClientInfo'
      consent_id:
        format: consentID
        type: string
        x-nullable: false
      created_at:
        description: Consent creation time
        format: date-time
        type: string
        x-nullable: false
      customer_payment_consent_v3:
        $ref: '#/definitions/BrazilCustomerPaymentConsentV3'
      requested_scopes:
        description: List of requested scopes
        items:
          $ref: '#/definitions/RequestedScope'
        type: array
        x-nullable: false
      server_id:
        description: Server / Workspace identifier.
        example: '"server"'
        format: serverID
        type: string
        x-nullable: false
      status:
        description: Consent status
        type: string
        x-nullable: false
      subject:
        description: Subject
        format: subject
        type: string
        x-nullable: false
      tenant_id:
        description: Tenant identifier.
        example: '"tenant"'
        format: tenantID
        type: string
        x-nullable: false
      type:
        $ref: '#/definitions/ConsentType'
    type: object
  GrantedScopes:
    items:
      format: scopeName
      type: string
    type: array
  HTTPError:
    properties:
      code:
        type: string
        x-nullable: false
      detail:
        type: string
        x-nullable: false
      title:
        type: string
        x-nullable: false
    type: object
  IntrospectOBBRDataAccessConsentResponse:
    allOf:
    - $ref: '#/definitions/IntrospectResponse'
    - properties:
        AccountIDs:
          items:
            type: string
          type: array
        consentId:
          description: |-
            O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
            Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
            Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
            seja um identificador de recurso persistente e independente da localizao.
            Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
            o namespace(urn)
            o identificador associado ao namespace da instituio transnmissora (bancoex)
            o identificador especfico dentro do namespace (C1DD33123).
            Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
          example: urn:bancoex:C1DD33123
          maxLength: 256
          pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
          type: string
        creationDateTime:
          description: Data e hora em que o recurso foi criado. Uma string com data
            e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        document:
          $ref: '#/definitions/OpenbankingBrasilConsentDocument1'
        expirationDateTime:
          description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
            reflete a data limite de validade do consentimento. Uma string com data
            e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        permissions:
          description: Especifica os tipos de permisses de acesso s APIs no escopo
            do Open Banking Brasil - Fase 2, de acordo com os blocos de consentimento
            fornecidos pelo usurio e necessrios ao acesso a cada endpoint das APIs.
          example:
          - ACCOUNTS_READ
          - ACCOUNTS_OVERDRAFT_LIMITS_READ
          - RESOURCES_READ
          items:
            $ref: '#/definitions/OpenbankingBrasilConsentPermission1'
          maxItems: 30
          minItems: 1
          type: array
        status:
          $ref: '#/definitions/OpenbankingBrasilConsentStatus'
        statusUpdateDateTime:
          description: Data e hora em que o recurso foi atualizado. Uma string com
            data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        transactionFromDateTime:
          description: Data e hora da transao inicial. Se no for preenchido, a transao
            ter a data aberta e a data ser retornada com a primeira transao disponvel.
            Uma string com data e hora conforme especificao RFC-3339, sempre com a
            utilizao de timezone UTC(UTC time format).
          example: "2021-01-01T00:00:00Z"
          format: date-time
          type: string
        transactionToDateTime:
          description: Data e hora final da transao. Se no for preenchido, a transao
            ter a data aberta e a data ser retornada com a ultima transao disponvel.
            Uma string com data e hora conforme especificao RFC-3339, sempre com a
            utilizao de timezone UTC(UTC time format).
          example: "2021-02-01T23:59:59Z"
          format: date-time
          type: string
      required:
      - consentId
      - creationDateTime
      - expirationDateTime
      - permissions
      - status
      - statusUpdateDateTime
      - document
      type: object
  IntrospectOBBRDataAccessConsentV2Response:
    allOf:
    - $ref: '#/definitions/IntrospectResponse'
    - properties:
        AccountIDs:
          items:
            type: string
          type: array
        consentId:
          description: |-
            O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
            Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
            Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
            seja um identificador de recurso persistente e independente da localizao.
            Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
            o namespace(urn)
            o identificador associado ao namespace da instituio transnmissora (bancoex)
            o identificador especfico dentro do namespace (C1DD33123).
            Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
          example: urn:bancoex:C1DD33123
          maxLength: 256
          pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
          type: string
        creationDateTime:
          description: Data e hora em que o recurso foi criado. Uma string com data
            e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        document:
          $ref: '#/definitions/OpenbankingBrasilConsentV2BusinessEntityDocument'
        expirationDateTime:
          description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
            reflete a data limite de validade do consentimento. Uma string com data
            e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC (UTC time format). Para consentimentos com prazo indeterminado,  esperado
            preenchimento com `2300-01-01T00:00:00Z`.
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        extensions:
          $ref: '#/definitions/BrazilConsentExtensions'
        permissions:
          description: Especifica os tipos de permisses de acesso s APIs no escopo
            do Open Finance Brasil - Dados cadastrais e transacionais, de acordo com
            os blocos de consentimento fornecidos pelo usurio e necessrios ao acesso
            a cada endpoint das APIs. Esse array no deve ter duplicidade de itens.
          example:
          - ACCOUNTS_READ
          - ACCOUNTS_OVERDRAFT_LIMITS_READ
          - RESOURCES_READ
          items:
            $ref: '#/definitions/OpenbankingBrasilConsentV2Permission1'
          minItems: 1
          type: array
        rejection:
          $ref: '#/definitions/OpenbankingBrasilConsentV2Rejection'
        status:
          $ref: '#/definitions/OpenbankingBrasilConsentV2Status'
        statusUpdateDateTime:
          description: Data e hora em que o recurso foi atualizado. Uma string com
            data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
      required:
      - consentId
      - creationDateTime
      - expirationDateTime
      - permissions
      - status
      - statusUpdateDateTime
      - document
      type: object
  IntrospectOBBRPaymentConsentResponse:
    allOf:
    - $ref: '#/definitions/IntrospectResponse'
    - properties:
        AccountIDs:
          items:
            type: string
          type: array
        businessEntity:
          $ref: '#/definitions/OpenbankingBrasilPaymentBusinessEntity'
        consentId:
          description: |-
            Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
            Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
            Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
            seja um identificador de recurso persistente e independente da localizao.
            Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
            o namespace(urn)
            o identificador associado ao namespace da instituio transnmissora (bancoex)
            o identificador especfico dentro do namespace (C1DD33123).
            Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
          example: urn:bancoex:C1DD33123
          maxLength: 256
          pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
          type: string
        creationDateTime:
          description: Data e hora em que o consentimento foi criado. Uma string com
            data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        creditor:
          $ref: '#/definitions/OpenbankingBrasilPaymentIdentification'
        debtorAccount:
          $ref: '#/definitions/OpenbankingBrasilPaymentDebtorAccount'
        expirationDateTime:
          description: |-
            Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
            O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
            Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
            O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        loggedUser:
          $ref: '#/definitions/OpenbankingBrasilPaymentLoggedUser'
        payment:
          $ref: '#/definitions/OpenbankingBrasilPaymentPaymentConsent'
        revocation:
          $ref: '#/definitions/OpenbankingBrasilPaymentRevocation'
        status:
          $ref: '#/definitions/OpenbankingBrasilPaymentEnumAuthorisationStatusType'
        statusUpdateDateTime:
          description: Data e hora em que o recurso foi atualizado. Uma string com
            data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
      required:
      - consentId
      - creationDateTime
      - creditor
      - expirationDateTime
      - loggedUser
      - payment
      - status
      - statusUpdateDateTime
      type: object
  IntrospectOBBRPaymentConsentResponseV2:
    allOf:
    - $ref: '#/definitions/IntrospectResponse'
    - properties:
        AccountIDs:
          items:
            type: string
          type: array
        businessEntity:
          $ref: '#/definitions/OpenbankingBrasilPaymentV2BusinessEntity'
        consentId:
          description: |-
            Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
            Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
            Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
            seja um identificador de recurso persistente e independente da localizao.
            Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
            o namespace(urn)
            o identificador associado ao namespace da instituio transnmissora (bancoex)
            o identificador especfico dentro do namespace (C1DD33123).
            Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
          example: urn:bancoex:C1DD33123
          maxLength: 256
          pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9\-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
          type: string
        creationDateTime:
          description: Data e hora em que o consentimento foi criado. Uma string com
            data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        creditor:
          $ref: '#/definitions/OpenbankingBrasilPaymentV2Identification'
        debtorAccount:
          $ref: '#/definitions/OpenbankingBrasilPaymentV2DebtorAccount'
        expirationDateTime:
          description: |-
            Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
            O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
            Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
            O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        loggedUser:
          $ref: '#/definitions/OpenbankingBrasilPaymentV2LoggedUser'
        payment:
          $ref: '#/definitions/OpenbankingBrasilPaymentV2PaymentConsent'
        status:
          $ref: '#/definitions/OpenbankingBrasilPaymentV2EnumAuthorisationStatusType'
        statusUpdateDateTime:
          description: Data e hora em que o recurso foi atualizado. Uma string com
            data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
      required:
      - consentId
      - creationDateTime
      - creditor
      - expirationDateTime
      - loggedUser
      - payment
      - status
      - statusUpdateDateTime
      type: object
  IntrospectOBBRPaymentConsentResponseV3:
    allOf:
    - $ref: '#/definitions/IntrospectResponse'
    - properties:
        AccountIDs:
          items:
            type: string
          type: array
        businessEntity:
          $ref: '#/definitions/OpenbankingBrasilPaymentV3BusinessEntity'
        consentId:
          description: |-
            Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
            Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
            Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
            seja um identificador de recurso persistente e independente da localizao.
            Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
            o namespace(urn)
            o identificador associado ao namespace da instituio transnmissora (bancoex)
            o identificador especfico dentro do namespace (C1DD33123).
            Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
          example: urn:bancoex:C1DD33123
          maxLength: 256
          pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9\-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
          type: string
        creationDateTime:
          description: Data e hora em que o consentimento foi criado. Uma string com
            data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        creditor:
          $ref: '#/definitions/OpenbankingBrasilPaymentV3Identification'
        debtorAccount:
          $ref: '#/definitions/OpenbankingBrasilPaymentV3ConsentsDebtorAccount'
        expirationDateTime:
          description: |-
            Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
            O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
            Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
            O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
        loggedUser:
          $ref: '#/definitions/OpenbankingBrasilPaymentV3LoggedUser'
        payment:
          $ref: '#/definitions/OpenbankingBrasilPaymentV3PaymentConsent'
        rejectionReason:
          $ref: '#/definitions/OpenbankingBrasilPaymentV3ConsentRejectionReason'
        status:
          $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumAuthorisationStatusType'
        statusUpdateDateTime:
          description: Data e hora em que o recurso foi atualizado. Uma string com
            data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
            UTC(UTC time format).
          example: "2021-05-21T08:30:00Z"
          format: date-time
          type: string
      required:
      - consentId
      - creationDateTime
      - creditor
      - expirationDateTime
      - loggedUser
      - payment
      - status
      - statusUpdateDateTime
      type: object
  IntrospectResponse:
    properties:
      acr:
        description: Authentication context class reference
        type: string
        x-nullable: false
      act:
        additionalProperties:
          type: object
        description: Actor claims used in the Token Exchange flow.
        type: object
        x-nullable: false
      active:
        description: |-
          Active is a boolean indicator of whether or not the presented token
          is currently active. The specifics of a token's `active` state
          varies depending on the implementation of an authorization
          server and the information it keeps about its token. Still, the `true`
          value returned for the `active` property generally indicates
          that a given token has been issued by this authorization server,
          has not been revoked by the resource owner, and is within its
          given time window of validity (e.g., between its issuance and
          expiration time).
        type: boolean
        x-nullable: false
      amr:
        description: Authentication method references
        items:
          type: string
        type: array
        x-nullable: false
      aud:
        description: Audience contains the list of the audiences the token is intended
          for.
        items:
          type: string
        type: array
        x-nullable: false
      client_id:
        description: |-
          A client application identifier for the OAuth 2.0 client that
          requested this token.
        type: string
        x-nullable: false
      cnf:
        $ref: '#/definitions/Confirmation'
      exp:
        description: |-
          ExpiredAt is the integer timestamp measured in the number of seconds
          since January 1 1970 UTC (1970-01-01T00:00:00Z). It indicates when this token will expire.
        format: int64
        type: integer
        x-nullable: false
      ext:
        additionalProperties:
          type: object
        description: Extra is arbitrary data set by the session.
        type: object
        x-nullable: false
      iat:
        description: |-
          IssuedAt is the integer timestamp measured in the number of seconds
          since January 1 1970 UTC. It indicates when this token was
          originally issued.
        format: int64
        type: integer
        x-nullable: false
      idp:
        description: The identifier of an identity provider that user authenticated
          with.
        type: string
        x-nullable: false
      idp_sub:
        description: IDP subject
        type: string
        x-nullable: false
      iss:
        description: Issuer URL is a string representing the issuer of this token.
        type: string
        x-nullable: false
      may_act:
        additionalProperties:
          type: object
        description: May act claims used in the Token Exchange flow.s
        type: object
        x-nullable: false
      nbf:
        description: |-
          NotBefore is an integer timestamp measured in the number of seconds
          since January 1 1970 UTC. It indicates this token was not
          used before the specified time.
        format: int64
        type: integer
        x-nullable: false
      scope:
        description: |-
          Scope is a JSON string containing a space-separated list of
          scopes associated with this token.
        type: string
        x-nullable: false
      server_id:
        description: |-
          The OAuth 2.0 authorization server identifier that
          issued this token.
        type: string
        x-nullable: false
      sub:
        description: |-
          Subject of the token, as defined in JWT [RFC7519].
          Usually a machine-readable identifier of the resource owner who
          authorized this token.
        type: string
        x-nullable: false
      tenant_id:
        description: |-
          TenantID identifies a tenant holding the authorization server that
          issued this token.
        type: string
        x-nullable: false
      token_type:
        description: TokenType is the type of the introspected token. For example,
          `access_token` or `refresh_token`.
        type: string
        x-nullable: false
      username:
        description: |-
          Username is a human-readable identifier for the resource owner who
          authorized this token.
        type: string
        x-nullable: false
    type: object
  Metadata:
    additionalProperties:
      type: object
    type: object
  OBBRConsentWithClient:
    properties:
      Client:
        $ref: '#/definitions/OpenbankingClient'
      account_ids:
        items:
          type: string
        type: array
        x-nullable: false
      client_id:
        description: Client application identifier.
        example: '"cauqo9c9vpbs0aj2b2v0"'
        format: clientID
        type: string
        x-nullable: false
      cnpj:
        type: string
        x-nullable: false
      consent_id:
        format: consentID
        type: string
        x-nullable: false
      cpf:
        type: string
        x-nullable: false
      created_at:
        format: date-time
        type: string
        x-nullable: false
      customer_data_access_consent:
        $ref: '#/definitions/BrazilCustomerDataAccessConsentV1'
      customer_data_access_consent_v2:
        $ref: '#/definitions/BrazilCustomerDataAccessConsentV2'
      customer_insurance_data_access_consent:
        $ref: '#/definitions/BrazilInsuranceCustomerDataAccessConsent'
      customer_payment_consent:
        $ref: '#/definitions/BrazilCustomerPaymentConsent'
      customer_payment_consent_v2:
        $ref: '#/definitions/BrazilCustomerPaymentConsentV2'
      customer_payment_consent_v3:
        $ref: '#/definitions/BrazilCustomerPaymentConsentV3'
      idempotency_key:
        type: string
        x-nullable: false
      request_hash:
        type: string
        x-nullable: false
      server_id:
        description: Server / Workspace identifier.
        example: '"server"'
        format: serverID
        type: string
        x-nullable: false
      spec:
        type: string
        x-nullable: false
      spec_version:
        $ref: '#/definitions/SpecVersion'
      status:
        type: string
        x-nullable: false
      tenant_id:
        description: Tenant identifier.
        example: '"tenant"'
        format: tenantID
        type: string
        x-nullable: false
      type:
        $ref: '#/definitions/ConsentType'
    type: object
  OBBRConsents:
    properties:
      Consents:
        items:
          $ref: '#/definitions/OBBRConsentWithClient'
        type: array
        x-nullable: false
    type: object
  OBBRConsentsRequest:
    properties:
      accounts:
        description: |-
          List of accounts.

          It can refer to user bank accounts the client application is allowed to access.
        items:
          type: string
        type: array
        x-nullable: false
      after_consent_id:
        description: |-
          A consent identifier.

          Use it to navigate through the request pagination when the number of consents is greater than
          the `limit` set for results in the response.

          With `after_consent_id`, the list you obtain starts from the subsequent consent after the specified one. Also,
          the response depends on the `sort` and `order` parameters, if any are passed.
        type: string
        x-nullable: false
      before_consent_id:
        description: |-
          A consent identifier.

          Use it to navigate through the request pagination when the number of consents is greater than
          the limit set for results in the response.

          With `before_consent_id`, the list you obtain comprises consents up to the specified one. The specified consent
          isn't included. Also, the response depends on the `sort` and `order` parameters, if any are passed.
        type: string
        x-nullable: false
      client_id:
        description: A client application identifier.
        type: string
        x-nullable: false
      cnpj:
        description: |-
          Optional. A
          [14-digit identification number](https://openbanking-brasil.github.io/specs-seguranca/open-banking-brasil-financial-api-1_ID3.html#section-5.2.2.4)
          of a company in Cadastro Nacional da Pessoa Jurídica (Brazilian National Registry of Legal Entities).
        type: string
        x-nullable: false
      cpf:
        description: |-
          Optional. An
          [11-digit taxpayer identification number](https://openbanking-brasil.github.io/specs-seguranca/open-banking-brasil-financial-api-1_ID3.html#name-requesting-the-cpf-claim)
          in Cadastro de Pessoas Físicas (Natural Persons Register).
        type: string
        x-nullable: false
      date_from:
        description: |-
          Optional. Consent creation date.

          Sets the start date of the consent retrieval date range.
        type: string
        x-nullable: false
      date_to:
        description: |-
          Optional. Consent creation date.

          Sets the end date of the consent retrieval date range.
        type: string
        x-nullable: false
      limit:
        default: 20
        description: Limit the number of results returned in the response.
        format: int64
        maximum: 100
        minimum: 1
        type: integer
        x-nullable: false
      order:
        default: desc
        description: |-
          Input: `acs` or `desc`.

          Set the order of results returned in the response.
        type: string
        x-nullable: false
      sort:
        description: Sort results returned in the response.
        type: string
        x-nullable: false
      status:
        description: List of the consent statuses.
        items:
          type: string
        type: array
        x-nullable: false
      types:
        description: Consent types.
        items:
          type: string
        type: array
        x-nullable: false
    type: object
  OBBRErrorResponse:
    properties:
      errors:
        items:
          $ref: '#/definitions/HTTPError'
        type: array
        x-nullable: false
      meta:
        $ref: '#/definitions/OpenbankingBrasilPaymentMeta'
    type: object
  OPINBusinessEntity:
    description: Titular, pessoa jurdica a quem se referem os dados que so objeto
      de compartilhamento.
    properties:
      document:
        $ref: '#/definitions/OPINDocument1'
    required:
    - document
    title: OPINBusinessEntity BusinessEntity
    type: object
  OPINData:
    description: OPINData Data
    properties:
      businessEntity:
        $ref: '#/definitions/OPINBusinessEntity'
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OPINLoggedUser'
      permissions:
        description: permissions
        example:
        - PENSION_RISK_READ
        - CAPITALIZATION_TITLES_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OPINPermission'
        minItems: 1
        type: array
        x-nullable: false
      transactionFromDateTime:
        description: Data e hora da transao inicial. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a primeira transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-01-01T00:00:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionToDateTime:
        description: Data e hora final da transao. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a ultima transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-02-01T23:59:59Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - expirationDateTime
    - loggedUser
    - permissions
    type: object
  OPINData1:
    description: OPINData1 Data1
    properties:
      consentId:
        description: |-
          O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:prudential:C1DD93123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (prudential)
          o identificador especfico dentro do namespace (C1DD93123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:prudential:C1DD93123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      permissions:
        description: Especifica os tipos de permisses de acesso  s APIs no escopo
          do Open Insurance Brasil - Fase 2, de acordo com os blocos de consentimento
          fornecidos pelo usurio e necessrios ao acesso a cada endpoint das APIs.
        example:
        - PENSION_RISK_READ
        - CAPITALIZATION_TITLES_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OPINPermission1'
        minItems: 1
        type: array
        x-nullable: false
      status:
        $ref: '#/definitions/OPINStatus'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionFromDateTime:
        description: Data e hora da transao inicial. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a primeira transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-01-01T00:00:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionToDateTime:
        description: Data e hora final da transao. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a ultima transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-02-01T23:59:59Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - expirationDateTime
    - permissions
    - status
    - statusUpdateDateTime
    type: object
  OPINDocument:
    description: OPINDocument Document
    properties:
      identification:
        description: Nmero do documento de identificao oficial do usurio.
        example: "11111111111"
        maxLength: 11
        pattern: ^\d{11}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do usurio.
        example: CPF
        maxLength: 3
        pattern: ^[A-Z]{3}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OPINDocument1:
    description: OPINDocument1 Document1
    properties:
      identification:
        description: Nmero do documento de identificao oficial do titular pessoa jurdica.
        example: "11111111111111"
        maxLength: 14
        pattern: ^\d{14}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do titular pessoa jurdica.
        example: CNPJ
        maxLength: 4
        pattern: ^[A-Z]{4}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OPINLinks:
    description: Referncias para outros recusos da API requisitada.
    properties:
      first:
        description: URI da primeira pgina que originou essa lista de resultados.
          Restrio - Obrigatrio quando no for a primeira pgina da resposta
        example: https://api.seguro.com.br/open-insurance/consents/v1
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      last:
        description: URI da ltima pgina dessa lista de resultados. Restrio - Obrigatrio
          quando no for a ltima pgina da resposta
        example: https://api.seguro.com.br/open-insurance/consents/v1
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      next:
        description: URI da prxima pgina dessa lista de resultados. Restrio - Obrigatrio
          quando no for a ltima pgina da resposta
        example: https://api.seguro.com.br/open-insurance/consents/v1
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      prev:
        description: "URI da pgina anterior dessa lista de resultados. Restrio - \tObrigatrio
          quando no for a primeira pgina da resposta"
        example: https://api.seguro.com.br/open-insurance/consents/v1
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      self:
        description: URI completo que gerou a resposta atual.
        example: https://api.seguro.com.br/open-insurance/consents/v1
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
    required:
    - self
    title: OPINLinks Links
    type: object
  OPINLoggedUser:
    description: Usurio (pessoa natural) que encontra-se logado na instituio receptora
      e que iniciar o processo de consentimento para compartilhamento de dados.
    properties:
      document:
        $ref: '#/definitions/OPINDocument'
    required:
    - document
    title: OPINLoggedUser LoggedUser
    type: object
  OPINMeta:
    description: Meta informaes referente   API requisitada.
    properties:
      requestDateTime:
        description: Data e hora da consulta, conforme especificao RFC-3339, formato
          UTC.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      totalPages:
        description: Nmero total de pginas no resultado
        example: 1
        format: int32
        type: integer
        x-nullable: false
      totalRecords:
        description: Nmero total de registros no resultado
        example: 1
        format: int32
        type: integer
        x-nullable: false
    required:
    - requestDateTime
    - totalPages
    - totalRecords
    title: OPINMeta Meta
    type: object
  OPINPermission:
    description: Especifica os tipos de permisses de acesso  s APIs no escopo do Open
      Insurance Brasil - Fase 2, de acordo com os blocos de consentimento fornecidos
      pelo usurio e necessrios ao acesso a cada endpoint das APIs.
    title: OPINPermission Permission
    type: string
  OPINPermission1:
    description: OPINPermission1 Permission1
    type: string
  OPINStatus:
    description: |-
      Estado atual do consentimento cadastrado.
      Example: AWAITING_AUTHORISATION
    title: OPINStatus Status
    type: string
  OpenbankingBrasilConsentBusinessEntity:
    description: Titular, pessoa jurdica a quem se referem os dados que so objeto
      de compartilhamento.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilConsentDocument1'
    required:
    - document
    title: OpenbankingBrasilConsentBusinessEntity BusinessEntity
    type: object
  OpenbankingBrasilConsentData:
    description: OpenbankingBrasilConsentData Data
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilConsentBusinessEntity'
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilConsentLoggedUser'
      permissions:
        description: permissions
        example:
        - ACCOUNTS_READ
        - ACCOUNTS_OVERDRAFT_LIMITS_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentPermission'
        maxItems: 30
        minItems: 1
        type: array
        x-nullable: false
      transactionFromDateTime:
        description: Data e hora da transao inicial. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a primeira transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-01-01T00:00:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionToDateTime:
        description: Data e hora final da transao. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a ultima transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-02-01T23:59:59Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - expirationDateTime
    - loggedUser
    - permissions
    type: object
  OpenbankingBrasilConsentData1:
    description: OpenbankingBrasilConsentData1 Data1
    properties:
      consentId:
        description: |-
          O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      permissions:
        description: Especifica os tipos de permisses de acesso s APIs no escopo do
          Open Banking Brasil - Fase 2, de acordo com os blocos de consentimento fornecidos
          pelo usurio e necessrios ao acesso a cada endpoint das APIs.
        example:
        - ACCOUNTS_READ
        - ACCOUNTS_OVERDRAFT_LIMITS_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentPermission1'
        maxItems: 30
        minItems: 1
        type: array
        x-nullable: false
      status:
        $ref: '#/definitions/OpenbankingBrasilConsentStatus'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionFromDateTime:
        description: Data e hora da transao inicial. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a primeira transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-01-01T00:00:00Z"
        format: date-time
        type: string
        x-nullable: false
      transactionToDateTime:
        description: Data e hora final da transao. Se no for preenchido, a transao
          ter a data aberta e a data ser retornada com a ultima transao disponvel.
          Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao
          de timezone UTC(UTC time format).
        example: "2021-02-01T23:59:59Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - expirationDateTime
    - permissions
    - status
    - statusUpdateDateTime
    type: object
  OpenbankingBrasilConsentDocument:
    description: OpenbankingBrasilConsentDocument Document
    properties:
      identification:
        description: Nmero do documento de identificao oficial do usurio.
        example: "11111111111"
        maxLength: 11
        pattern: ^\d{11}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do usurio.
        example: CPF
        maxLength: 3
        pattern: ^[A-Z]{3}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilConsentDocument1:
    description: OpenbankingBrasilConsentDocument1 Document1
    properties:
      identification:
        description: Nmero do documento de identificao oficial do titular pessoa jurdica.
        example: "11111111111111"
        maxLength: 14
        pattern: ^\d{14}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do titular pessoa jurdica.
        example: CNPJ
        maxLength: 4
        pattern: ^[A-Z]{4}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilConsentLinks:
    description: Referncias para outros recusos da API requisitada.
    properties:
      first:
        description: URI da primeira pgina que originou essa lista de resultados.
          Restrio - Obrigatrio quando no for a primeira pgina da resposta
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      last:
        description: URI da ltima pgina dessa lista de resultados. Restrio - Obrigatrio
          quando no for a ltima pgina da resposta
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      next:
        description: URI da prxima pgina dessa lista de resultados. Restrio - Obrigatrio
          quando no for a ltima pgina da resposta
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      prev:
        description: "URI da pgina anterior dessa lista de resultados. Restrio - \tObrigatrio
          quando no for a primeira pgina da resposta"
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      self:
        description: URI completo que gerou a resposta atual.
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
    required:
    - self
    title: OpenbankingBrasilConsentLinks Links
    type: object
  OpenbankingBrasilConsentLoggedUser:
    description: Usurio (pessoa natural) que encontra-se logado na instituio receptora
      e que iniciar o processo de consentimento para compartilhamento de dados.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilConsentDocument'
    required:
    - document
    title: OpenbankingBrasilConsentLoggedUser LoggedUser
    type: object
  OpenbankingBrasilConsentMeta:
    description: Meta informaes referente a API requisitada.
    properties:
      requestDateTime:
        description: Data e hora da consulta, conforme especificao RFC-3339, formato
          UTC.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      totalPages:
        description: Nmero total de pginas no resultado
        example: 1
        format: int32
        type: integer
        x-nullable: false
      totalRecords:
        description: Nmero total de registros no resultado
        example: 1
        format: int32
        type: integer
        x-nullable: false
    required:
    - requestDateTime
    - totalPages
    - totalRecords
    title: OpenbankingBrasilConsentMeta Meta
    type: object
  OpenbankingBrasilConsentPermission:
    description: Especifica os tipos de permisses de acesso s APIs no escopo do Open
      Banking Brasil - Fase 2, de acordo com os blocos de consentimento fornecidos
      pelo usurio e necessrios ao acesso a cada endpoint das APIs.
    title: OpenbankingBrasilConsentPermission Permission
    type: string
  OpenbankingBrasilConsentPermission1:
    description: OpenbankingBrasilConsentPermission1 Permission1
    type: string
  OpenbankingBrasilConsentStatus:
    description: Estado atual do consentimento cadastrado.
    title: OpenbankingBrasilConsentStatus Status
    type: string
  OpenbankingBrasilConsentV2BusinessEntity:
    description: Titular, pessoa jurdica a quem se referem os dados que so objeto
      de compartilhamento.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilConsentV2BusinessEntityDocument'
    required:
    - document
    title: OpenbankingBrasilConsentV2BusinessEntity BusinessEntity
    type: object
  OpenbankingBrasilConsentV2BusinessEntityDocument:
    description: OpenbankingBrasilConsentV2BusinessEntityDocument BusinessEntityDocument
    properties:
      identification:
        description: Nmero do documento de identificao oficial do titular pessoa jurdica.
        example: "11111111111111"
        maxLength: 14
        pattern: ^\d{14}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do titular pessoa jurdica.
        example: CNPJ
        maxLength: 4
        pattern: ^[A-Z]{4}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilConsentV2BusinessEntityExtends:
    description: |-
      Titular, pessoa jurdica a quem se referem os dados que so objeto de compartilhamento.
      Deve ser informado apenas para casos de consentimento pessoa jurdica.
      No precisa ser armazenado separadamente. Para fins de renovao de consentimento, ser utilizado apenas para verificao do consentimento vigente, pois  um atributo imutvel.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilConsentV2BusinessEntityDocument'
    required:
    - document
    title: OpenbankingBrasilConsentV2BusinessEntityExtends BusinessEntityExtends
    type: object
  OpenbankingBrasilConsentV2Data:
    description: OpenbankingBrasilConsentV2Data Data
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilConsentV2BusinessEntity'
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC (UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilConsentV2LoggedUser'
      permissions:
        description: permissions
        example:
        - ACCOUNTS_READ
        - ACCOUNTS_OVERDRAFT_LIMITS_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentV2Permission'
        minItems: 1
        type: array
        x-nullable: false
    required:
    - expirationDateTime
    - loggedUser
    - permissions
    type: object
  OpenbankingBrasilConsentV2Data1:
    description: OpenbankingBrasilConsentV2Data1 Data1
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilConsentV2BusinessEntityExtends'
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format). Para consentimentos com prazo indeterminado, preencher
          com valor '2300-01-01T00:00:00Z'. Esse valor deve ser refletido no expirationDateTime
          do consentimento relacionado.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilConsentV2LoggedUserExtends'
    required:
    - expirationDateTime
    - loggedUser
    type: object
  OpenbankingBrasilConsentV2Data2:
    description: OpenbankingBrasilConsentV2Data2 Data2
    properties:
      consentId:
        description: |-
          O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC (UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      permissions:
        description: Especifica os tipos de permisses de acesso s APIs no escopo do
          Open Finance Brasil - Dados cadastrais e transacionais, de acordo com os
          blocos de consentimento fornecidos pelo usurio e necessrios ao acesso a
          cada endpoint das APIs. Esse array no deve ter duplicidade de itens.
        example:
        - ACCOUNTS_READ
        - ACCOUNTS_OVERDRAFT_LIMITS_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentV2Permission1'
        minItems: 1
        type: array
        x-nullable: false
      status:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Status'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - expirationDateTime
    - permissions
    - status
    - statusUpdateDateTime
    type: object
  OpenbankingBrasilConsentV2Data3:
    description: OpenbankingBrasilConsentV2Data3 Data3
    properties:
      consentId:
        description: |-
          O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      expirationDateTime:
        description: |-
          Data e hora de expirao da permisso. Deve ser preenchido caso o consentimento tenha data limite de validade. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC(UTC time format).

          [Restrio] Aceitar apenas data no futuro em relao  data de requisio.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      permissions:
        description: Especifica os tipos de permisses de acesso s APIs no escopo do
          Open Finance Brasil - Dados cadastrais e transacionais, de acordo com os
          blocos de consentimento fornecidos pelo usurio e necessrios ao acesso a
          cada endpoint das APIs. Esse array no deve ter duplicidade de itens.
        example:
        - ACCOUNTS_READ
        - ACCOUNTS_OVERDRAFT_LIMITS_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentV2Permission1'
        minItems: 1
        type: array
        x-nullable: false
      status:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Status'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - permissions
    - status
    - statusUpdateDateTime
    type: object
  OpenbankingBrasilConsentV2Data4:
    description: OpenbankingBrasilConsentV2Data4 Data4
    properties:
      consentId:
        description: |-
          O consentId  o identificador nico do consentimento e dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC (UTC time format). Para consentimentos com prazo indeterminado,  esperado
          preenchimento com `2300-01-01T00:00:00Z`.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      permissions:
        description: Especifica os tipos de permisses de acesso s APIs no escopo do
          Open Finance Brasil - Dados cadastrais e transacionais, de acordo com os
          blocos de consentimento fornecidos pelo usurio e necessrios ao acesso a
          cada endpoint das APIs. Esse array no deve ter duplicidade de itens.
        example:
        - ACCOUNTS_READ
        - ACCOUNTS_OVERDRAFT_LIMITS_READ
        - RESOURCES_READ
        items:
          $ref: '#/definitions/OpenbankingBrasilConsentV2Permission1'
        minItems: 1
        type: array
        x-nullable: false
      rejection:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Rejection'
      status:
        $ref: '#/definitions/OpenbankingBrasilConsentV2Status'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - expirationDateTime
    - permissions
    - status
    - statusUpdateDateTime
    type: object
  OpenbankingBrasilConsentV2Data5:
    description: OpenbankingBrasilConsentV2Data5 Data5
    properties:
      expirationDateTime:
        description: Data e hora de expirao da permisso. De preenchimento obrigatrio,
          reflete a data limite de validade do consentimento. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format), utilizado apenas para consulta de alteraes histricas
          de extenso do consentimento.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilConsentV2LoggedUserExtends'
      requestDateTime:
        description: Data e hora em que o recurso foi criado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - loggedUser
    - requestDateTime
    type: object
  OpenbankingBrasilConsentV2EnumReasonCode:
    description: |-
      Define o cdigo da razo pela qual o consentimento foi rejeitado.

      CONSENT_EXPIRED  consentimento que ultrapassou o tempo limite para autorizao.
      CUSTOMER_MANUALLY_REJECTED  cliente efetuou a rejeio do consentimento manualmente atravs de interao nas instituies participantes.
      CUSTOMER_MANUALLY_REVOKED  cliente efetuou a revogao aps a autorizao do consentimento.
      CONSENT_MAX_DATE_REACHED  consentimento que ultrapassou o tempo limite de compartilhamento.
      CONSENT_TECHNICAL_ISSUE  consentimento que foi rejeitado devido a um problema tcnico que impossibilita seu uso pela instituio receptora, por exemplo: falha associada a troca do AuthCode pelo AccessToken, durante o processo de Hybid Flow.
      INTERNAL_SECURITY_REASON  consentimento que foi rejeitado devido as polticas de segurana aplicada pela instituio transmissora.
      Example: CONSENT_EXPIRED
    title: OpenbankingBrasilConsentV2EnumReasonCode EnumReasonCode
    type: string
  OpenbankingBrasilConsentV2EnumRejectedBy:
    description: |-
      Informar usurio responsvel pela rejeio.
      1. USER usurio
      2. ASPSP instituio transmissora
      3. TPP instituio receptora
      Example: USER
    title: OpenbankingBrasilConsentV2EnumRejectedBy EnumRejectedBy
    type: string
  OpenbankingBrasilConsentV2Links:
    description: Referncias para outros recusos da API requisitada.
    properties:
      first:
        description: URI da primeira pgina que originou essa lista de resultados.
          Restrio - Obrigatrio quando no for a primeira pgina da resposta
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      last:
        description: URI da ltima pgina dessa lista de resultados. Restrio - Obrigatrio
          quando no for a ltima pgina da resposta
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      next:
        description: URI da prxima pgina dessa lista de resultados. Restrio - Obrigatrio
          quando no for a ltima pgina da resposta
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      prev:
        description: "URI da pgina anterior dessa lista de resultados. Restrio - \tObrigatrio
          quando no for a primeira pgina da resposta"
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
      self:
        description: URI completo que gerou a resposta atual.
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
    required:
    - self
    title: OpenbankingBrasilConsentV2Links Links
    type: object
  OpenbankingBrasilConsentV2LoggedUser:
    description: Usurio (pessoa natural) que encontra-se logado na instituio receptora
      e que iniciar o processo de consentimento para compartilhamento de dados.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilConsentV2LoggedUserDocument'
    required:
    - document
    title: OpenbankingBrasilConsentV2LoggedUser LoggedUser
    type: object
  OpenbankingBrasilConsentV2LoggedUserDocument:
    description: OpenbankingBrasilConsentV2LoggedUserDocument LoggedUserDocument
    properties:
      identification:
        description: Nmero do documento de identificao oficial do usurio.
        example: "11111111111"
        maxLength: 11
        pattern: ^\d{11}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do usurio.
        example: CPF
        maxLength: 3
        pattern: ^[A-Z]{3}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilConsentV2LoggedUserExtends:
    description: |-
      Usurio (pessoa natural) que encontra-se logado na instituio receptora e que iniciar o processo de consentimento para compartilhamento de dados.
      Deve ser armazenado como novo usurio logado responsvel pela renovao do consentimento atual.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilConsentV2LoggedUserDocument'
    required:
    - document
    title: OpenbankingBrasilConsentV2LoggedUserExtends LoggedUserExtends
    type: object
  OpenbankingBrasilConsentV2Meta:
    description: Meta informaes referente  API requisitada.
    properties:
      requestDateTime:
        description: Data e hora da consulta, conforme especificao RFC-3339, formato
          UTC.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      totalPages:
        description: Nmero total de pginas no resultado
        example: 1
        format: int32
        type: integer
        x-nullable: false
      totalRecords:
        description: Nmero total de registros no resultado
        example: 1
        format: int32
        type: integer
        x-nullable: false
    required:
    - requestDateTime
    - totalPages
    - totalRecords
    title: OpenbankingBrasilConsentV2Meta Meta
    type: object
  OpenbankingBrasilConsentV2Permission:
    description: Especifica os tipos de permisses de acesso s APIs no escopo do Open
      Finance Brasil - Dados cadastrais e transacionais, de acordo com os blocos de
      consentimento fornecidos pelo usurio e necessrios ao acesso a cada endpoint
      das APIs. Esse array no deve ter duplicidade de itens.
    title: OpenbankingBrasilConsentV2Permission Permission
    type: string
  OpenbankingBrasilConsentV2Permission1:
    description: OpenbankingBrasilConsentV2Permission1 Permission1
    type: string
  OpenbankingBrasilConsentV2RejectedReason:
    description: Define a razo pela qual o consentimento foi rejeitado.
    properties:
      additionalInformation:
        description: Contm informaes adicionais a critrio da transmissora.
        example: Tempo de confirmao da mltipla alada excedido.
        maxLength: 140
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
      code:
        $ref: '#/definitions/OpenbankingBrasilConsentV2EnumReasonCode'
    required:
    - code
    title: OpenbankingBrasilConsentV2RejectedReason RejectedReason
    type: object
  OpenbankingBrasilConsentV2Rejection:
    description: Objeto a ser retornado caso o consentimento seja rejeitado.
    properties:
      reason:
        $ref: '#/definitions/OpenbankingBrasilConsentV2RejectedReason'
      rejectedBy:
        $ref: '#/definitions/OpenbankingBrasilConsentV2EnumRejectedBy'
    required:
    - reason
    - rejectedBy
    title: OpenbankingBrasilConsentV2Rejection Rejection
    type: object
  OpenbankingBrasilConsentV2Status:
    description: |-
      Estado atual do consentimento cadastrado.
      Example: AWAITING_AUTHORISATION
    title: OpenbankingBrasilConsentV2Status Status
    type: string
  OpenbankingBrasilPaymentBusinessEntity:
    description: Usurio (pessoa jurdica) que encontra-se logado na instituio Iniciadora
      de Pagamento. [Restrio] Preenchimento obrigatrio se usurio logado na instituio
      Iniciadora de Pagamento for um CNPJ (pessoa jurdica).
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilPaymentDocument'
    required:
    - document
    title: OpenbankingBrasilPaymentBusinessEntity BusinessEntity
    type: object
  OpenbankingBrasilPaymentCreditorAccount:
    description: Objeto que contm a identificao da conta de destino do beneficirio/recebedor.
    properties:
      accountType:
        $ref: '#/definitions/OpenbankingBrasilPaymentEnumAccountPaymentsType'
      ispb:
        description: Deve ser preenchido com o ISPB (Identificador do Sistema de Pagamentos
          Brasileiros) do participante do SPI (Sistema de pagamentos instantneos)
          somente com nmeros.
        example: "12345678"
        maxLength: 8
        minLength: 8
        pattern: ^[0-9]{8}$
        type: string
        x-nullable: false
      issuer:
        description: |-
          Cdigo da Agncia emissora da conta sem dgito.
          (Agncia  a dependncia destinada ao atendimento aos clientes, ao pblico em geral e aos associados de cooperativas de crdito,
          no exerccio de atividades da instituio, no podendo ser mvel ou transitria).
          [Restrio] Preenchimento obrigatrio para os seguintes tipos de conta: CACC (CONTA_DEPOSITO_A_VISTA), SVGS (CONTA_POUPANCA) e SLRY (CONTA_SALARIO).
        example: "1774"
        maxLength: 4
        pattern: ^\d{4}$
        type: string
        x-nullable: false
      number:
        description: |-
          Deve ser preenchido com o nmero da conta do usurio recebedor, com dgito verificador (se este existir),
          se houver valor alfanumrico, este deve ser convertido para 0.
        example: "1234567890"
        maxLength: 20
        minLength: 3
        pattern: ^\d{3,20}$
        type: string
        x-nullable: false
    required:
    - accountType
    - ispb
    - number
    title: OpenbankingBrasilPaymentCreditorAccount CreditorAccount
    type: object
  OpenbankingBrasilPaymentData:
    description: Objeto contendo as informaes de consentimento para a iniciao de pagamento
      individual.
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentBusinessEntity'
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentIdentification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentDebtorAccount'
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentLoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentPaymentConsent'
    required:
    - creditor
    - loggedUser
    - payment
    title: OpenbankingBrasilPaymentData Data
    type: object
  OpenbankingBrasilPaymentData1:
    description: Objeto contendo as informaes de resposta do consentimento para a
      iniciao de pagamento individual.
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentBusinessEntity'
      consentId:
        description: |-
          Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o consentimento foi criado. Uma string com
          data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentIdentification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentDebtorAccount'
      expirationDateTime:
        description: |-
          Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
          O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
          Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
          O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentLoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentPaymentConsent'
      revocation:
        $ref: '#/definitions/OpenbankingBrasilPaymentRevocation'
      status:
        $ref: '#/definitions/OpenbankingBrasilPaymentEnumAuthorisationStatusType'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - creditor
    - expirationDateTime
    - loggedUser
    - payment
    - status
    - statusUpdateDateTime
    title: OpenbankingBrasilPaymentData1 Data1
    type: object
  OpenbankingBrasilPaymentDebtorAccount:
    description: |-
      Objeto que contm a identificao da conta de origem do pagador.
      As informaes quanto  conta de origem do pagador podero ser trazidas no consentimento para a detentora, caso a iniciadora tenha coletado essas informaes do cliente. Do contrrio, ser coletada na detentora e trazida para a iniciadora como resposta  criao do pagamento.
    properties:
      accountType:
        $ref: '#/definitions/OpenbankingBrasilPaymentEnumAccountPaymentsType'
      ispb:
        description: Deve ser preenchido com o ISPB (Identificador do Sistema de Pagamentos
          Brasileiros) do participante do SPI (Sistema de pagamentos instantneos)
          somente com nmeros.
        example: "12345678"
        maxLength: 8
        minLength: 8
        pattern: ^[0-9]{8}$
        type: string
        x-nullable: false
      issuer:
        description: |-
          Cdigo da Agncia emissora da conta sem dgito.
          (Agncia  a dependncia destinada ao atendimento aos clientes, ao pblico em geral e aos associados de cooperativas de crdito,
          no exerccio de atividades da instituio, no podendo ser mvel ou transitria).
          [Restrio] Preenchimento obrigatrio para os seguintes tipos de conta: CACC (CONTA_DEPOSITO_A_VISTA), SVGS (CONTA_POUPANCA) e SLRY (CONTA_SALARIO).
        example: "1774"
        maxLength: 4
        pattern: ^\d{4}$
        type: string
        x-nullable: false
      number:
        description: |-
          Deve ser preenchido com o nmero da conta transacional do usurio pagador, com dgito verificador (se este existir),
          se houver valor alfanumrico, este deve ser convertido para 0.
        example: "1234567890"
        maxLength: 20
        minLength: 3
        pattern: ^\d{3,20}$
        type: string
        x-nullable: false
    required:
    - accountType
    - ispb
    - number
    title: OpenbankingBrasilPaymentDebtorAccount DebtorAccount
    type: object
  OpenbankingBrasilPaymentDetails:
    description: Objeto contendo os detalhes do pagamento.
    properties:
      creditorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentCreditorAccount'
      localInstrument:
        $ref: '#/definitions/OpenbankingBrasilPaymentEnumLocalInstrument'
      proxy:
        description: |-
          Chave cadastrada no DICT pertencente ao recebedor. Os tipos de chaves podem ser: telefone, e-mail, cpf/cnpj ou chave aleatria.
          No caso de telefone celular deve ser informado no padro E.1641.
          Para e-mail deve ter o formato xxxxxxxx@xxxxxxx.xxx(.xx) e no mximo 77 caracteres.
          No caso de CPF dever ser informado com 11 nmeros, sem pontos ou traos.
          Para o caso de CNPJ dever ser informado com 14 nmeros, sem pontos ou traos.
          No caso de chave aleatria deve ser informado o UUID gerado pelo DICT, conforme formato especificado na RFC41223.
          Se informado, a detentora da conta deve validar o proxy no DICT quando localInstrument for igual a DICT, QRDN ou QRES e validar o campo creditorAccount.
          Esta validao  opcional caso o localInstrument for igual a INIC.
          [Restrio]
          Se localInstrument for igual a MANU, o campo proxy no deve ser preenchido.
          Se localInstrument for igual INIC, DICT, QRDN ou QRES, o campo proxy deve ser sempre preenchido com a chave Pix.
        example: "12345678901"
        maxLength: 77
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
      qrCode:
        description: |-
          Sequncia de caracteres que corresponde ao QR Code disponibilizado para o pagador.

          a sequncia de caracteres que seria lida pelo leitor de QR Code, e deve propiciar o retorno dos dados do pagador aps consulta na DICT.

          Essa funcionalidade  possvel tanto para QR Code esttico quanto para QR Code dinmico.
          No arranjo do Pix esta  a mesma sequncia gerada e/ou lida pela funcionalidade Pix Copia e Cola.
          Este campo dever ser no formato UTF-8.
          [Restrio] Preenchimento obrigatrio para pagamentos por QR Code, observado o tamanho mximo de 512 bytes.
        example: 00020104141234567890123426660014BR.GOV.BCB.PIX014466756C616E6F32303139406578616D706C652E636F6D27300012\nBR.COM.OUTRO011001234567895204000053039865406123.455802BR5915NOMEDORECEBEDOR6008BRASILIA61087007490062\n530515RP12345678-201950300017BR.GOV.BCB.BRCODE01051.0.080450014BR.GOV.BCB.PIX0123PADRAO.URL.PIX/0123AB\nCD81390012BR.COM.OUTRO01190123.ABCD.3456.WXYZ6304EB76\n
        maxLength: 512
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
    required:
    - creditorAccount
    - localInstrument
    title: OpenbankingBrasilPaymentDetails Details
    type: object
  OpenbankingBrasilPaymentDocument:
    description: OpenbankingBrasilPaymentDocument Document
    properties:
      identification:
        description: Nmero do documento de identificao oficial do titular pessoa jurdica.
        example: "11111111111111"
        maxLength: 14
        pattern: ^\d{14}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do titular pessoa jurdica.
        example: CNPJ
        maxLength: 4
        pattern: ^[A-Z]{4}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilPaymentDocument1:
    description: Objeto que contm os dados de identificao do usurio.
    properties:
      identification:
        description: Nmero do documento de identificao oficial do usurio.
        example: "11111111111"
        maxLength: 11
        pattern: ^\d{11}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do usurio.
        example: CPF
        maxLength: 3
        pattern: ^[A-Z]{3}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    title: OpenbankingBrasilPaymentDocument1 Document1
    type: object
  OpenbankingBrasilPaymentEnumAccountPaymentsType:
    description: |-
      Tipos de contas usadas para pagamento via Pix.
      Modalidades tradicionais previstas pela Resoluo 4.753, no contemplando contas vinculadas,
      conta de domiciliados no exterior, contas em moedas estrangeiras e conta correspondente moeda eletrnica.
      Segue descrio de cada valor do ENUM para o escopo do Pix.
      CACC - Current - Conta Corrente.
      SLRY - Salary - Conta-Salrio.
      SVGS - Savings - Conta de Poupana.
      TRAN - TransactingAccount - Conta de Pagamento pr-paga.
      Example: CACC
    title: OpenbankingBrasilPaymentEnumAccountPaymentsType EnumAccountPaymentsType
    type: string
  OpenbankingBrasilPaymentEnumAuthorisationStatusType:
    description: |-
      Retorna o estado do consentimento, o qual no momento de sua criao ser AWAITING_AUTHORISATION.
      Este estado ser alterado depois da autorizao do consentimento na detentora da conta do pagador (Debtor) para AUTHORISED, REJECTED ou REVOKED.
      O consentimento fica no estado CONSUMED aps ocorrer a iniciao do pagamento referente ao consentimento.
      Em caso de consentimento expirado a detentora dever retornar o status REJECTED.
      Em caso de revogao do consentimento a detentora dever retornar o status REVOKED.
      Estados possveis:
      AWAITING_AUTHORISATION - Aguardando autorizao
      AUTHORISED - Autorizado
      REJECTED - Rejeitado
      CONSUMED - Consumido
      REVOKED - Revogado
      Example: REVOKED
    title: OpenbankingBrasilPaymentEnumAuthorisationStatusType EnumAuthorisationStatusType
    type: string
  OpenbankingBrasilPaymentEnumLocalInstrument:
    description: |-
      Especifica a forma de iniciao do pagamento:
      MANU - Insero manual de dados da conta transacional
      DICT - Insero manual de chave Pix
      QRDN - QR code dinmico
      QRES - QR code esttico
      INIC - Indica que o recebedor (creditor) contratou o Iniciador de Pagamentos especificamente para realizar iniciaes de pagamento em que o beneficirio  previamente conhecido.
      Example: DICT
    title: OpenbankingBrasilPaymentEnumLocalInstrument EnumLocalInstrument
    type: string
  OpenbankingBrasilPaymentEnumPaymentPersonType:
    description: |-
      Titular, pessoa natural ou juridica a quem se referem os dados de recebedor (creditor).
      Example: PESSOA_NATURAL
    title: OpenbankingBrasilPaymentEnumPaymentPersonType EnumPaymentPersonType
    type: string
  OpenbankingBrasilPaymentEnumRevocationReason:
    description: |-
      Define o cdigo da razo pela qual o consentimento foi revogado.
      Valores possveis:
      FRAUD- Indica suspeita de fraude
      ACCOUNT_CLOSURE - Indica que a conta do usurio foi encerrada
      OTHER- Indica que motivo do cancelamento est fora dos motivos pr-estabelecidos.
      Example: OTHER
    title: OpenbankingBrasilPaymentEnumRevocationReason EnumRevocationReason
    type: string
  OpenbankingBrasilPaymentEnumRevokedBy:
    description: |-
      Define qual das partes envolvidas na transao est realizando a revogao. Valores possveis:
      USER (Revogado pelo usurio)
      ASPSP (Provedor de servios de pagamento para servios de conta - Detentora de conta)
      TPP (Instituies Provedoras - iniciadora de pagamentos)
      Example: USER
    title: OpenbankingBrasilPaymentEnumRevokedBy EnumRevokedBy
    type: string
  OpenbankingBrasilPaymentIdentification:
    description: Objeto contendo os dados do recebedor (creditor).
    properties:
      cpfCnpj:
        description: |-
          Identificao da pessoa envolvida na transao.
          Preencher com o CPF ou CNPJ, de acordo com o valor escolhido no campo type.
          O CPF ser utilizado com 11 nmeros e dever ser informado sem pontos ou traos.
          O CNPJ ser utilizado com 14 nmeros e dever ser informado sem pontos ou traos.
        example: "58764789000137"
        maxLength: 14
        minLength: 11
        pattern: ^\d{11}$|^\d{14}$
        type: string
        x-nullable: false
      name:
        description: |-
          Em caso de pessoa natural deve ser informado o nome completo do titular da conta do recebedor.
          Em caso de pessoa jurdica deve ser informada a razo social ou o nome fantasia da conta do recebedor.
        example: Marco Antonio de Brito
        maxLength: 140
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
      personType:
        $ref: '#/definitions/OpenbankingBrasilPaymentEnumPaymentPersonType'
    required:
    - cpfCnpj
    - name
    - personType
    title: OpenbankingBrasilPaymentIdentification Identification
    type: object
  OpenbankingBrasilPaymentLinks:
    description: Referncias para outros recusos da API requisitada.
    properties:
      self:
        description: URI completo que gerou a resposta atual.
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https?:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
    required:
    - self
    title: OpenbankingBrasilPaymentLinks Links
    type: object
  OpenbankingBrasilPaymentLoggedUser:
    description: |-
      Usurio (pessoa natural) que encontra-se logado na instituio Iniciadora de Pagamento.
      [Restrio] Deve obrigatoriamente ser enviado quando a revogao for feita pelo usurio final, ou seja, se o campo revokedBy estiver com o valor 'USER'.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilPaymentDocument1'
    required:
    - document
    title: OpenbankingBrasilPaymentLoggedUser LoggedUser
    type: object
  OpenbankingBrasilPaymentMeta:
    description: Meta informaes referente  API requisitada.
    properties:
      requestDateTime:
        description: Data e hora da consulta, conforme especificao RFC-3339, formato
          UTC.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      totalPages:
        description: Nmero total de pginas no resultado
        example: 1
        format: int32
        type: integer
        x-nullable: false
      totalRecords:
        description: Nmero total de registros no resultado
        example: 1
        format: int32
        type: integer
        x-nullable: false
    required:
    - requestDateTime
    - totalPages
    - totalRecords
    title: OpenbankingBrasilPaymentMeta Meta
    type: object
  OpenbankingBrasilPaymentPatchPaymentsConsentData:
    description: Objeto contendo dados do pagamento e do recebedor (creditor).
    properties:
      revocation:
        $ref: '#/definitions/OpenbankingBrasilPaymentRevocation'
      status:
        description: |-
          Estado para o qual deve ir.

          [Restrio] O nico valor vlido  REVOKED. (caso contrrio a detentora deve retornar erro HTTP 400)
        example: REVOKED
        type: string
        x-nullable: false
    required:
    - revocation
    - status
    title: OpenbankingBrasilPaymentPatchPaymentsConsentData PatchPaymentsConsentData
    type: object
  OpenbankingBrasilPaymentPaymentConsent:
    description: Objeto contendo dados de pagamento para consentimento.
    properties:
      amount:
        description: Valor da transao com 2 casas decimais.
        example: "100000.12"
        maxLength: 19
        minLength: 4
        pattern: ^((\d{1,16}\.\d{2}))$
        type: string
        x-nullable: false
      currency:
        description: |-
          Cdigo da moeda nacional segundo modelo ISO-4217, ou seja, 'BRL'.
          Todos os valores monetrios informados esto representados com a moeda vigente do Brasil.
        example: BRL
        maxLength: 3
        pattern: ^([A-Z]{3})$
        type: string
        x-nullable: false
      date:
        description: Mutuamente exclusivo com o objeto schedule. Este campo  obrigatrio
          no caso de pagamento nico. Neste caso, o objeto schedule no deve ser informado.
        example: "2021-01-01"
        format: date
        type: string
        x-nullable: false
      details:
        $ref: '#/definitions/OpenbankingBrasilPaymentDetails'
      ibgeTownCode:
        description: |-
          Traz o cdigo da cidade segundo o IBGE (Instituto Brasileiro de Geografia e Estatstica).
          Para o preenchimento deste campo, o Iniciador de Pagamentos deve seguir a orientao do arranjo da forma de pagamento.
          O preenchimento do campo tanto em pix/payments quanto /consents deve ser igual. Caso haja divergncia dos valores, a instituio deve retornar HTTP 422 com o cdigo de erro PAGAMENTO_DIVERGENTE_DO_CONSENTIMENTO.
          Este campo faz referncia ao campo CodMun do arranjo Pix.
        example: "5300108"
        maxLength: 7
        minLength: 7
        pattern: ^\d{7}$
        type: string
        x-nullable: false
      schedule:
        $ref: '#/definitions/OpenbankingBrasilPaymentSchedule'
      type:
        description: Este campo define o tipo de pagamento que ser iniciado aps a
          autorizao do consentimento.
        example: PIX
        type: string
        x-nullable: false
    required:
    - amount
    - currency
    - details
    - type
    title: OpenbankingBrasilPaymentPaymentConsent PaymentConsent
    type: object
  OpenbankingBrasilPaymentReason:
    description: Define a razo pela qual o consentimento foi revogado.
    properties:
      additionalInformation:
        description: |-
          Contm informaes adicionais definidas pelo requisitante da revogao.

          [Restrio] Dever ser obrigatoriamente preenchido quando a revogao for feita pela iniciadora ou pela detentora unilateralmente, ou seja, quando o campo revokedBy for igual a TPP ou ASPSP e o motivo de revogao for OTHER.
        example: No quero mais o servio
        maxLength: 140
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
      code:
        $ref: '#/definitions/OpenbankingBrasilPaymentEnumRevocationReason'
    required:
    - code
    title: OpenbankingBrasilPaymentReason Reason
    type: object
  OpenbankingBrasilPaymentRevocation:
    description: |-
      Objeto que contm as informaes das circunstncia da revogao.

      [Restrio] O campo loggedUser deve ser preenchido quando a revogao for feita pelo usurio final, ou seja, se o campo data.revocation.revokedBy estiver com o valor 'USER'.
    properties:
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentLoggedUser'
      reason:
        $ref: '#/definitions/OpenbankingBrasilPaymentReason'
      revokedBy:
        $ref: '#/definitions/OpenbankingBrasilPaymentEnumRevokedBy'
    required:
    - reason
    - revokedBy
    title: OpenbankingBrasilPaymentRevocation Revocation
    type: object
  OpenbankingBrasilPaymentSchedule:
    description: Mutuamente exclusivo com o campo date. Este campo  obrigatrio no
      caso de agendamento. Neste caso, o campo date no deve ser informado.
    properties:
      single:
        $ref: '#/definitions/OpenbankingBrasilPaymentSingle'
    required:
    - single
    title: OpenbankingBrasilPaymentSchedule Schedule
    type: object
  OpenbankingBrasilPaymentSingle:
    description: Define a poltica de agendamento nico.
    properties:
      date:
        description: |-
          Define a data alvo da liquidao do pagamento. O fuso horrio de Brasilia deve ser utilizado para criao e racionalizao sobre os dados deste campo.
          OBS:Esse campo dever sempre ser no mnimo D+1 corrido, ou seja, a data imediatamente posterior em relao a data do consentimento considerando o fuso horrio de Braslia e dever ser no mximo um ano corrido a partir da data do consentimento considerando o fuso horrio de Braslia.
        example: "2021-01-01"
        format: date
        type: string
        x-nullable: false
    required:
    - date
    title: OpenbankingBrasilPaymentSingle Single
    type: object
  OpenbankingBrasilPaymentV2BusinessEntity:
    description: Usurio (pessoa jurdica) que encontra-se logado na instituio Iniciadora
      de Pagamento. [Restrio] Preenchimento obrigatrio se usurio logado na instituio
      Iniciadora de Pagamento for um CNPJ (pessoa jurdica).
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Document'
    required:
    - document
    title: OpenbankingBrasilPaymentV2BusinessEntity BusinessEntity
    type: object
  OpenbankingBrasilPaymentV2CreditorAccount:
    description: Objeto que contm a identificao da conta de destino do beneficirio/recebedor.
    properties:
      accountType:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2EnumAccountPaymentsType'
      ispb:
        description: Deve ser preenchido com o ISPB (Identificador do Sistema de Pagamentos
          Brasileiros) do participante do SPI (Sistema de pagamentos instantneos)
          somente com nmeros.
        example: "12345678"
        maxLength: 8
        minLength: 8
        pattern: ^[0-9]{8}$
        type: string
        x-nullable: false
      issuer:
        description: |-
          Cdigo da Agncia emissora da conta sem dgito.
          (Agncia  a dependncia destinada ao atendimento aos clientes, ao pblico em geral e aos associados de cooperativas de crdito,
          no exerccio de atividades da instituio, no podendo ser mvel ou transitria).
          [Restrio] Preenchimento obrigatrio para os seguintes tipos de conta: CACC (CONTA_DEPOSITO_A_VISTA), SVGS (CONTA_POUPANCA) e SLRY (CONTA_SALARIO).
        example: "1774"
        maxLength: 4
        minLength: 1
        pattern: ^[0-9]{1,4}$
        type: string
        x-nullable: false
      number:
        description: |-
          Deve ser preenchido com o nmero da conta do usurio recebedor, com dgito verificador (se este existir),
          se houver valor alfanumrico, este deve ser convertido para 0.
        example: "1234567890"
        maxLength: 20
        minLength: 1
        pattern: ^[0-9]{1,20}$
        type: string
        x-nullable: false
    required:
    - accountType
    - ispb
    - number
    title: OpenbankingBrasilPaymentV2CreditorAccount CreditorAccount
    type: object
  OpenbankingBrasilPaymentV2Data:
    description: Objeto contendo as informaes de consentimento para a iniciao de pagamento
      individual.
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2BusinessEntity'
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Identification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2DebtorAccount'
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2LoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Payment'
    required:
    - creditor
    - loggedUser
    - payment
    title: OpenbankingBrasilPaymentV2Data Data
    type: object
  OpenbankingBrasilPaymentV2Data1:
    description: Objeto contendo as informaes de resposta do consentimento para a
      iniciao de pagamento individual.
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2BusinessEntity'
      consentId:
        description: |-
          Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9\-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o consentimento foi criado. Uma string com
          data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Identification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2DebtorAccount'
      expirationDateTime:
        description: |-
          Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
          O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
          Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
          O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2LoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2PaymentConsent'
      status:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2EnumAuthorisationStatusType'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - creditor
    - expirationDateTime
    - loggedUser
    - payment
    - status
    - statusUpdateDateTime
    title: OpenbankingBrasilPaymentV2Data1 Data1
    type: object
  OpenbankingBrasilPaymentV2DebtorAccount:
    description: |-
      Objeto que contm a identificao da conta de origem do pagador.
      As informaes quanto  conta de origem do pagador podero ser trazidas no consentimento para a detentora, caso a iniciadora tenha coletado essas informaes do cliente. Do contrrio, ser coletada na detentora e trazida para a iniciadora como resposta  criao do pagamento.
    properties:
      accountType:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2EnumAccountPaymentsType'
      ispb:
        description: Deve ser preenchido com o ISPB (Identificador do Sistema de Pagamentos
          Brasileiros) do participante do SPI (Sistema de pagamentos instantneos)
          somente com nmeros.
        example: "12345678"
        maxLength: 8
        minLength: 8
        pattern: ^[0-9]{8}$
        type: string
        x-nullable: false
      issuer:
        description: |-
          Cdigo da Agncia emissora da conta sem dgito.
          (Agncia  a dependncia destinada ao atendimento aos clientes, ao pblico em geral e aos associados de cooperativas de crdito,
          no exerccio de atividades da instituio, no podendo ser mvel ou transitria).
          [Restrio] Preenchimento obrigatrio para os seguintes tipos de conta: CACC (CONTA_DEPOSITO_A_VISTA), SVGS (CONTA_POUPANCA) e SLRY (CONTA_SALARIO).
        example: "1774"
        maxLength: 4
        minLength: 1
        pattern: ^[0-9]{1,4}$
        type: string
        x-nullable: false
      number:
        description: |-
          Deve ser preenchido com o nmero da conta transacional do usurio pagador, com dgito verificador (se este existir),
          se houver valor alfanumrico, este deve ser convertido para 0.
        example: "1234567890"
        maxLength: 20
        minLength: 1
        pattern: ^[0-9]{1,20}$
        type: string
        x-nullable: false
    required:
    - accountType
    - ispb
    - number
    title: OpenbankingBrasilPaymentV2DebtorAccount DebtorAccount
    type: object
  OpenbankingBrasilPaymentV2Details:
    description: Objeto contendo os detalhes do pagamento.
    properties:
      creditorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2CreditorAccount'
      localInstrument:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2EnumLocalInstrument'
      proxy:
        description: |-
          Chave cadastrada no DICT pertencente ao recebedor. Os tipos de chaves podem ser: telefone, e-mail, cpf/cnpj ou chave aleatria.
          No caso de telefone celular deve ser informado no padro E.1641.
          Para e-mail deve ter o formato xxxxxxxx@xxxxxxx.xxx(.xx) e no mximo 77 caracteres.
          No caso de CPF dever ser informado com 11 nmeros, sem pontos ou traos.
          Para o caso de CNPJ dever ser informado com 14 nmeros, sem pontos ou traos.
          No caso de chave aleatria deve ser informado o UUID gerado pelo DICT, conforme formato especificado na RFC41223.
          Se informado, a detentora da conta deve validar o proxy no DICT quando localInstrument for igual a DICT, QRDN ou QRES e validar o campo creditorAccount.
          Esta validao  opcional caso o localInstrument for igual a INIC.
          [Restrio]
          Se localInstrument for igual a MANU, o campo proxy no deve ser preenchido.
          Se localInstrument for igual INIC, DICT, QRDN ou QRES, o campo proxy deve ser sempre preenchido com a chave Pix.
        example: "12345678901"
        maxLength: 77
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
      qrCode:
        description: |-
          Sequncia de caracteres que corresponde ao QR Code disponibilizado para o pagador.

          a sequncia de caracteres que seria lida pelo leitor de QR Code, e deve propiciar o retorno dos dados do pagador aps consulta na DICT.

          Essa funcionalidade  possvel tanto para QR Code esttico quanto para QR Code dinmico.
          No arranjo do Pix esta  a mesma sequncia gerada e/ou lida pela funcionalidade Pix Copia e Cola.
          Este campo dever ser no formato UTF-8.
          [Restrio] Preenchimento obrigatrio para pagamentos por QR Code, observado o tamanho mximo de 512 bytes.
        example: 00020104141234567890123426660014BR.GOV.BCB.PIX014466756C616E6F32303139406578616D706C652E636F6D27300012\nBR.COM.OUTRO011001234567895204000053039865406123.455802BR5915NOMEDORECEBEDOR6008BRASILIA61087007490062\n530515RP12345678-201950300017BR.GOV.BCB.BRCODE01051.0.080450014BR.GOV.BCB.PIX0123PADRAO.URL.PIX/0123AB\nCD81390012BR.COM.OUTRO01190123.ABCD.3456.WXYZ6304EB76\n
        maxLength: 512
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
    required:
    - creditorAccount
    - localInstrument
    title: OpenbankingBrasilPaymentV2Details Details
    type: object
  OpenbankingBrasilPaymentV2Document:
    description: OpenbankingBrasilPaymentV2Document Document
    properties:
      identification:
        description: Nmero do documento de identificao oficial do titular pessoa jurdica.
        example: "11111111111111"
        maxLength: 14
        pattern: ^\d{14}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do titular pessoa jurdica.
        example: CNPJ
        maxLength: 4
        pattern: ^[A-Z]{4}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilPaymentV2Document1:
    description: OpenbankingBrasilPaymentV2Document1 Document1
    properties:
      identification:
        description: Nmero do documento de identificao oficial do usurio.
        example: "11111111111"
        maxLength: 11
        pattern: ^\d{11}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do usurio.
        example: CPF
        maxLength: 3
        pattern: ^[A-Z]{3}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilPaymentV2EnumAccountPaymentsType:
    description: |-
      Tipos de contas usadas para pagamento via Pix.
      Modalidades tradicionais previstas pela Resoluo 4.753, no contemplando contas vinculadas,
      conta de domiciliados no exterior, contas em moedas estrangeiras e conta correspondente moeda eletrnica.
      Segue descrio de cada valor do ENUM para o escopo do Pix.
      CACC - Current - Conta Corrente.
      SLRY - Salary - Conta-Salrio.
      SVGS - Savings - Conta de Poupana.
      TRAN - TransactingAccount - Conta de Pagamento pr-paga.
      Example: CACC
    title: OpenbankingBrasilPaymentV2EnumAccountPaymentsType EnumAccountPaymentsType
    type: string
  OpenbankingBrasilPaymentV2EnumAuthorisationStatusType:
    description: |-
      Retorna o estado do consentimento, o qual no momento de sua criao ser AWAITING_AUTHORISATION.
      Este estado ser alterado depois da autorizao do consentimento na detentora da conta do pagador (Debtor) para AUTHORISED ou REJECTED.
      O consentimento fica no estado CONSUMED aps ocorrer a iniciao do pagamento referente ao consentimento.
      Em caso de consentimento expirado a detentora dever retornar o status REJECTED.
      Estados possveis:
      AWAITING_AUTHORISATION - Aguardando autorizao
      AUTHORISED - Autorizado
      REJECTED - Rejeitado
      CONSUMED - Consumido
      Example: AWAITING_AUTHORISATION
    title: OpenbankingBrasilPaymentV2EnumAuthorisationStatusType EnumAuthorisationStatusType
    type: string
  OpenbankingBrasilPaymentV2EnumLocalInstrument:
    description: |-
      Especifica a forma de iniciao do pagamento:
      MANU - Insero manual de dados da conta transacional
      DICT - Insero manual de chave Pix
      QRDN - QR code dinmico
      QRES - QR code esttico
      INIC - Indica que o recebedor (creditor) contratou o Iniciador de Pagamentos especificamente para realizar iniciaes de pagamento em que o beneficirio  previamente conhecido.
      Example: DICT
    title: OpenbankingBrasilPaymentV2EnumLocalInstrument EnumLocalInstrument
    type: string
  OpenbankingBrasilPaymentV2EnumPaymentPersonType:
    description: Titular, pessoa natural ou juridica a quem se referem os dados de
      recebedor (creditor).
    title: OpenbankingBrasilPaymentV2EnumPaymentPersonType EnumPaymentPersonType
    type: string
  OpenbankingBrasilPaymentV2Identification:
    description: Objeto contendo os dados do recebedor (creditor).
    properties:
      cpfCnpj:
        description: |-
          Identificao da pessoa envolvida na transao.
          Preencher com o CPF ou CNPJ, de acordo com o valor escolhido no campo type.
          O CPF ser utilizado com 11 nmeros e dever ser informado sem pontos ou traos.
          O CNPJ ser utilizado com 14 nmeros e dever ser informado sem pontos ou traos.
        example: "58764789000137"
        maxLength: 14
        minLength: 11
        pattern: ^\d{11}$|^\d{14}$
        type: string
        x-nullable: false
      name:
        description: |-
          Em caso de pessoa natural deve ser informado o nome completo do titular da conta do recebedor.
          Em caso de pessoa jurdica deve ser informada a razo social ou o nome fantasia da conta do recebedor.
        example: Marco Antonio de Brito
        maxLength: 120
        pattern: ^([A-Za-z---' -]+)$
        type: string
        x-nullable: false
      personType:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2EnumPaymentPersonType'
    required:
    - cpfCnpj
    - name
    - personType
    title: OpenbankingBrasilPaymentV2Identification Identification
    type: object
  OpenbankingBrasilPaymentV2LinkSingle:
    description: Referncias para outros recusos da API requisitada.
    properties:
      self:
        description: URI completo que gerou a resposta atual.
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
    required:
    - self
    title: OpenbankingBrasilPaymentV2LinkSingle LinkSingle
    type: object
  OpenbankingBrasilPaymentV2LoggedUser:
    description: Usurio (pessoa natural) que encontra-se logado na instituio Iniciadora
      de Pagamento.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Document1'
    required:
    - document
    title: OpenbankingBrasilPaymentV2LoggedUser LoggedUser
    type: object
  OpenbankingBrasilPaymentV2Meta:
    description: Meta informao referente a API requisitada.
    properties:
      requestDateTime:
        description: Data e hora da consulta, conforme especificao RFC-3339, formato
          UTC.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - requestDateTime
    title: OpenbankingBrasilPaymentV2Meta Meta
    type: object
  OpenbankingBrasilPaymentV2Payment:
    description: Objeto contendo dados de pagamento para consentimento.
    properties:
      amount:
        description: |-
          Valor da transao com 2 casas decimais. O valor deve ser o mesmo enviado no consentimento.

          Para QR Code esttico com valor pr-determinado no QR Code ou para QR Code dinmico com indicao de que o valor no pode ser alterado: O campo amount deve ser preenchido com o valor estabelecido no QR Code.
          Caso seja preenchido com valor divergente do QR Code, deve ser retornado um erro HTTP Status 422.
        example: "100000.12"
        maxLength: 19
        minLength: 4
        pattern: ^((\d{1,16}\.\d{2}))$
        type: string
        x-nullable: false
      currency:
        description: |-
          Cdigo da moeda nacional segundo modelo ISO-4217, ou seja, 'BRL'.
          Todos os valores monetrios informados esto representados com a moeda vigente do Brasil.
        example: BRL
        maxLength: 3
        pattern: ^([A-Z]{3})$
        type: string
        x-nullable: false
      date:
        description: |-
          [Restrio] Mutuamente excludente com o objeto schedule.

          Este campo  obrigatrio no caso de pagamento nico.

          Neste caso, o objeto schedule no deve ser informado.
        example: "2021-01-01"
        format: date
        type: string
        x-nullable: false
      details:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Details'
      ibgeTownCode:
        description: |-
          O campo ibgetowncode no arranjo PIX, tem o mesmo comportamento que o campo codMun descrito no item 1.6.6 do manual do PIX, conforme segue:

          1. Caso a informao referente ao municpio no seja enviada; o PSP do recebedor assumir que no existem feriados estaduais e municipais no perodo em questo;
        example: "5300108"
        maxLength: 7
        minLength: 7
        pattern: ^\d{7}$
        type: string
        x-nullable: false
      schedule:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Schedule'
      type:
        description: Este campo define o tipo de pagamento que ser iniciado aps a
          autorizao do consentimento.
        example: PIX
        type: string
        x-nullable: false
    required:
    - amount
    - currency
    - details
    - type
    title: OpenbankingBrasilPaymentV2Payment Payment
    type: object
  OpenbankingBrasilPaymentV2PaymentConsent:
    description: Objeto contendo dados de pagamento para consentimento.
    properties:
      amount:
        description: Valor da transao com 2 casas decimais.
        example: "100000.12"
        maxLength: 19
        minLength: 4
        pattern: ^((\d{1,16}\.\d{2}))$
        type: string
        x-nullable: false
      currency:
        description: |-
          Cdigo da moeda nacional segundo modelo ISO-4217, ou seja, 'BRL'.
          Todos os valores monetrios informados esto representados com a moeda vigente do Brasil.
        example: BRL
        maxLength: 3
        pattern: ^([A-Z]{3})$
        type: string
        x-nullable: false
      date:
        description: |-
          [Restrio] Mutuamente excludente com o objeto schedule.

          Este campo  obrigatrio no caso de pagamento nico.

          Neste caso, o objeto schedule no deve ser informado.
        example: "2021-01-01"
        format: date
        type: string
        x-nullable: false
      details:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Details'
      ibgeTownCode:
        description: |-
          O campo ibgetowncode no arranjo PIX, tem o mesmo comportamento que o campo codMun descrito no item 1.6.6 do manual do PIX, conforme segue:

          1. Caso a informao referente ao municpio no seja enviada; o PSP do recebedor assumir que no existem feriados estaduais e municipais no perodo em questo;
        example: "5300108"
        maxLength: 7
        minLength: 7
        pattern: ^\d{7}$
        type: string
        x-nullable: false
      schedule:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Schedule'
      type:
        description: Este campo define o tipo de pagamento que ser iniciado aps a
          autorizao do consentimento.
        example: PIX
        type: string
        x-nullable: false
    required:
    - amount
    - currency
    - details
    - type
    title: OpenbankingBrasilPaymentV2PaymentConsent PaymentConsent
    type: object
  OpenbankingBrasilPaymentV2Schedule:
    description: |-
      [Restrio] Mutuamente excludente com o campo date.

      Este campo  obrigatrio no caso de agendamento.

      Neste caso, o campo date no deve ser informado.
    properties:
      single:
        $ref: '#/definitions/OpenbankingBrasilPaymentV2Single'
    required:
    - single
    title: OpenbankingBrasilPaymentV2Schedule Schedule
    type: object
  OpenbankingBrasilPaymentV2Single:
    description: Define a poltica de agendamento nico.
    properties:
      date:
        description: |-
          Define a data alvo da liquidao do pagamento.

          O fuso horrio de Braslia deve ser utilizado para criao e racionalizao sobre os dados deste campo.

          Observao: Esse campo dever sempre ser no mnimo D+1 corrido, ou seja, a data imediatamente posterior em relao a data do consentimento considerando o fuso horrio de Braslia e dever ser no mximo D+365 corridos a partir da data do consentimento considerando o fuso horrio de Braslia
        example: "2021-01-01"
        format: date
        type: string
        x-nullable: false
    required:
    - date
    title: OpenbankingBrasilPaymentV2Single Single
    type: object
  OpenbankingBrasilPaymentV3BusinessEntity:
    description: Usurio (pessoa jurdica) que encontra-se logado na instituio Iniciadora
      de Pagamento. [Restrio] Preenchimento obrigatrio se usurio logado na instituio
      Iniciadora de Pagamento for um CNPJ (pessoa jurdica).
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Document'
    required:
    - document
    title: OpenbankingBrasilPaymentV3BusinessEntity BusinessEntity
    type: object
  OpenbankingBrasilPaymentV3ConsentRejectionReason:
    description: Motivo da rejeio do consentimento. Informaes complementares sobre
      o motivo do status
    properties:
      code:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumConsentRejectionReasonType'
      detail:
        description: |-
          Contm informaes adicionais ao consentimento rejeitado.
          VALOR_INVALIDO: O valor enviado no  vlido para o QR Code informado;
          NAO_INFORMADO: No informada pela detentora de conta;
          FALHA_INFRAESTRUTURA: [Descrio de qual falha na infraestrutura inviabilizou o processamento].
          TEMPO_EXPIRADO_AUTORIZACAO: Consentimento expirou antes que o usurio pudesse confirm-lo.
          TEMPO_EXPIRADO_CONSUMO: O usurio no finalizou o fluxo de pagamento e o consentimento expirou;
          REJEITADO_USUARIO: O usurio rejeitou a autorizao do consentimento
          CONTAS_ORIGEM_DESTINO_IGUAIS: A conta selecionada  igual  conta destino e no permite realizar esse pagamento.
          CONTA_NAO_PERMITE_PAGAMENTO: A conta selecionada  do tipo [salario/investimento/liquidao/outros] e no permite realizar esse pagamento.
          SALDO_INSUFICIENTE: A conta selecionada no possui saldo suficiente para realizar o pagamento.
          VALOR_ACIMA_LIMITE: O valor ultrapassa o limite estabelecido [na instituio/no arranjo/outro] para permitir a realizao de transaes pelo cliente.
          QRCODE_INVALIDO: O QRCode utilizado para a iniciao de pagamento no  vlido.
        example: O usurio rejeitou a autorizao do consentimento
        maxLength: 2048
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
    required:
    - code
    - detail
    title: OpenbankingBrasilPaymentV3ConsentRejectionReason ConsentRejectionReason
    type: object
  OpenbankingBrasilPaymentV3ConsentsDebtorAccount:
    description: |-
      Objeto que contm a identificao da conta de origem do pagador.
      As informaes quanto  conta de origem do pagador podero ser trazidas no consentimento para a detentora, caso a iniciadora tenha coletado essas informaes do cliente.
      No caso em que o cliente no preenche os dados na iniciadora, a detentora dever persistir as informaes da conta selecionada seguindo as condies abaixo.

      [Restrio]
      AUTHORISED e CONSUMED: Para esses dois status, o preenchimento do campo dever ser obrigatrio.
      REJECTED: Para este status o preenchimento  condicional, dado que h cenrios em que a detentora tambm no ter conhecimento da conta origem, pois a mesma no foi selecionada pelo usurio. Nos casos em que houver seleo, a conta deve ser preenchida obrigatoriamente.
    properties:
      accountType:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumAccountPaymentsType'
      ispb:
        description: Deve ser preenchido com o ISPB (Identificador do Sistema de Pagamentos
          Brasileiros) do participante do SPI (Sistema de pagamentos instantneos)
          somente com nmeros.
        example: "12345678"
        maxLength: 8
        minLength: 8
        pattern: ^[0-9]{8}$
        type: string
        x-nullable: false
      issuer:
        description: |-
          Cdigo da Agncia emissora da conta sem dgito.
          (Agncia  a dependncia destinada ao atendimento aos clientes, ao pblico em geral e aos associados de cooperativas de crdito,
          no exerccio de atividades da instituio, no podendo ser mvel ou transitria).

          [Restrio] Preenchimento obrigatrio para os seguintes tipos de conta: CACC (CONTA_DEPOSITO_A_VISTA) e SVGS (CONTA_POUPANCA).
        example: "1774"
        maxLength: 4
        minLength: 1
        pattern: ^[0-9]{1,4}$
        type: string
        x-nullable: false
      number:
        description: |-
          Deve ser preenchido com o nmero da conta transacional do usurio pagador, com dgito verificador (se este existir),
          se houver valor alfanumrico, este deve ser convertido para 0.
        example: "1234567890"
        maxLength: 20
        minLength: 1
        pattern: ^[0-9]{1,20}$
        type: string
        x-nullable: false
    required:
    - accountType
    - ispb
    - number
    title: OpenbankingBrasilPaymentV3ConsentsDebtorAccount ConsentsDebtorAccount
    type: object
  OpenbankingBrasilPaymentV3CreditorAccount:
    description: Objeto que contm a identificao da conta de destino do beneficirio/recebedor.
    properties:
      accountType:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumAccountPaymentsType'
      ispb:
        description: Deve ser preenchido com o ISPB (Identificador do Sistema de Pagamentos
          Brasileiros) do participante do SPI (Sistema de pagamentos instantneos)
          somente com nmeros.
        example: "12345678"
        maxLength: 8
        minLength: 8
        pattern: ^[0-9]{8}$
        type: string
        x-nullable: false
      issuer:
        description: |-
          Cdigo da Agncia emissora da conta sem dgito.
          (Agncia  a dependncia destinada ao atendimento aos clientes, ao pblico em geral e aos associados de cooperativas de crdito,
          no exerccio de atividades da instituio, no podendo ser mvel ou transitria).

          [Restrio] Preenchimento obrigatrio para os seguintes tipos de conta: CACC (CONTA_DEPOSITO_A_VISTA) e SVGS (CONTA_POUPANCA).
        example: "1774"
        maxLength: 4
        minLength: 1
        pattern: ^[0-9]{1,4}$
        type: string
        x-nullable: false
      number:
        description: |-
          Deve ser preenchido com o nmero da conta do usurio recebedor, com dgito verificador (se este existir),
          se houver valor alfanumrico, este deve ser convertido para 0.
        example: "1234567890"
        maxLength: 20
        minLength: 1
        pattern: ^[0-9]{1,20}$
        type: string
        x-nullable: false
    required:
    - accountType
    - ispb
    - number
    title: OpenbankingBrasilPaymentV3CreditorAccount CreditorAccount
    type: object
  OpenbankingBrasilPaymentV3Data:
    description: Objeto contendo as informaes de consentimento para a iniciao de pagamento
      individual.
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3BusinessEntity'
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Identification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3DebtorAccount'
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3LoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Payment'
    required:
    - creditor
    - loggedUser
    - payment
    title: OpenbankingBrasilPaymentV3Data Data
    type: object
  OpenbankingBrasilPaymentV3Data1:
    description: Objeto contendo as informaes de resposta do consentimento para a
      iniciao de pagamento individual.
    properties:
      businessEntity:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3BusinessEntity'
      consentId:
        description: |-
          Identificador nico do consentimento criado para a iniciao de pagamento solicitada. Dever ser um URN - Uniform Resource Name.
          Um URN, conforme definido na [RFC8141](https://tools.ietf.org/html/rfc8141)  um Uniform Resource
          Identifier - URI - que  atribudo sob o URI scheme "urn" e um namespace URN especfico, com a inteno de que o URN
          seja um identificador de recurso persistente e independente da localizao.
          Considerando a string urn:bancoex:C1DD33123 como exemplo para consentId temos:
          o namespace(urn)
          o identificador associado ao namespace da instituio transnmissora (bancoex)
          o identificador especfico dentro do namespace (C1DD33123).
          Informaes mais detalhadas sobre a construo de namespaces devem ser consultadas na [RFC8141](https://tools.ietf.org/html/rfc8141).
        example: urn:bancoex:C1DD33123
        maxLength: 256
        pattern: ^urn:[a-zA-Z0-9][a-zA-Z0-9\-]{0,31}:[a-zA-Z0-9()+,\-.:=@;$_!*'%\/?#]+$
        type: string
        x-nullable: false
      creationDateTime:
        description: Data e hora em que o consentimento foi criado. Uma string com
          data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      creditor:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Identification'
      debtorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3ConsentsDebtorAccount'
      expirationDateTime:
        description: |-
          Data e hora em que o consentimento da iniciao de pagamento expira, devendo ser sempre o creationDateTime mais 5 minutos. Uma string com data e hora conforme especificao RFC-3339, sempre com a utilizao de timezone UTC (UTC time format).
          O consentimento  criado com o status AWAITING_AUTHORISATION, e deve assumir o status AUTHORIZED ou REJECTED antes do tempo de expirao - 5 minutos. Caso o tempo seja expirado, o status deve assumir REJECTED.
          Para o cenrio em que o status assumiu AUTHORISED, o tempo mximo do expirationDateTime do consentimento deve assumir "now + 60 minutos". Este  o tempo para consumir o consentimento autorizado, mudando seu status para CONSUMED. No  possvel prorrogar este tempo e a criao de um novo consentimento ser necessria para os cenrios de insucesso.
          O tempo do expirationDateTime  garantido com os 15 minutos do access token, sendo possvel utilizar mais trs refresh tokens at totalizar 60 minutos.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
      loggedUser:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3LoggedUser'
      payment:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3PaymentConsent'
      rejectionReason:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3ConsentRejectionReason'
      status:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumAuthorisationStatusType'
      statusUpdateDateTime:
        description: Data e hora em que o recurso foi atualizado. Uma string com data
          e hora conforme especificao RFC-3339, sempre com a utilizao de timezone
          UTC(UTC time format).
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - consentId
    - creationDateTime
    - creditor
    - expirationDateTime
    - loggedUser
    - payment
    - status
    - statusUpdateDateTime
    title: OpenbankingBrasilPaymentV3Data1 Data1
    type: object
  OpenbankingBrasilPaymentV3DebtorAccount:
    description: |-
      Objeto que contm a identificao da conta de origem do pagador.
      As informaes quanto  conta de origem do pagador podero ser trazidas no consentimento para a detentora, caso a iniciadora tenha coletado essas informaes do cliente. Do contrrio, ser coletada na detentora e trazida para a iniciadora como resposta  criao do pagamento.
    properties:
      accountType:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumAccountPaymentsType'
      ispb:
        description: Deve ser preenchido com o ISPB (Identificador do Sistema de Pagamentos
          Brasileiros) do participante do SPI (Sistema de pagamentos instantneos)
          somente com nmeros.
        example: "12345678"
        maxLength: 8
        minLength: 8
        pattern: ^[0-9]{8}$
        type: string
        x-nullable: false
      issuer:
        description: |-
          Cdigo da Agncia emissora da conta sem dgito.
          (Agncia  a dependncia destinada ao atendimento aos clientes, ao pblico em geral e aos associados de cooperativas de crdito,
          no exerccio de atividades da instituio, no podendo ser mvel ou transitria).

          [Restrio] Preenchimento obrigatrio para os seguintes tipos de conta: CACC (CONTA_DEPOSITO_A_VISTA) e SVGS (CONTA_POUPANCA).
        example: "1774"
        maxLength: 4
        minLength: 1
        pattern: ^[0-9]{1,4}$
        type: string
        x-nullable: false
      number:
        description: |-
          Deve ser preenchido com o nmero da conta transacional do usurio pagador, com dgito verificador (se este existir),
          se houver valor alfanumrico, este deve ser convertido para 0.
        example: "1234567890"
        maxLength: 20
        minLength: 1
        pattern: ^[0-9]{1,20}$
        type: string
        x-nullable: false
    required:
    - accountType
    - ispb
    - number
    title: OpenbankingBrasilPaymentV3DebtorAccount DebtorAccount
    type: object
  OpenbankingBrasilPaymentV3Details:
    description: Objeto contendo os detalhes do pagamento.
    properties:
      creditorAccount:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3CreditorAccount'
      localInstrument:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumLocalInstrument'
      proxy:
        description: |-
          Chave cadastrada no DICT pertencente ao recebedor. Os tipos de chaves podem ser: telefone, e-mail, cpf/cnpj ou chave aleatria.
          No caso de telefone celular deve ser informado no padro E.1641.
          Para e-mail deve ter o formato xxxxxxxx@xxxxxxx.xxx(.xx) e no mximo 77 caracteres.
          No caso de CPF dever ser informado com 11 nmeros, sem pontos ou traos.
          Para o caso de CNPJ dever ser informado com 14 nmeros, sem pontos ou traos.
          No caso de chave aleatria deve ser informado o UUID gerado pelo DICT, conforme formato especificado na RFC41223.
          Se informado, a detentora da conta deve validar o proxy no DICT quando localInstrument for igual a DICT, QRDN ou QRES e validar o campo creditorAccount.
          Esta validao  opcional caso o localInstrument for igual a INIC.
          [Restrio]
          Se localInstrument for igual a MANU, o campo proxy no deve ser preenchido.
          Se localInstrument for igual INIC, DICT, QRDN ou QRES, o campo proxy deve ser sempre preenchido com a chave Pix.
        example: "12345678901"
        maxLength: 77
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
      qrCode:
        description: |-
          Sequncia de caracteres que corresponde ao QR Code disponibilizado para o pagador.

          a sequncia de caracteres que seria lida pelo leitor de QR Code, e deve propiciar o retorno dos dados do pagador aps consulta na DICT.

          Essa funcionalidade  possvel tanto para QR Code esttico quanto para QR Code dinmico.
          No arranjo do Pix esta  a mesma sequncia gerada e/ou lida pela funcionalidade Pix Copia e Cola.
          Este campo dever ser no formato UTF-8.
          [Restrio] Preenchimento obrigatrio para pagamentos por QR Code, observado o tamanho mximo de 512 bytes.
        example: 00020104141234567890123426660014BR.GOV.BCB.PIX014466756C616E6F32303139406578616D706C652E636F6D27300012\nBR.COM.OUTRO011001234567895204000053039865406123.455802BR5915NOMEDORECEBEDOR6008BRASILIA61087007490062\n530515RP12345678-201950300017BR.GOV.BCB.BRCODE01051.0.080450014BR.GOV.BCB.PIX0123PADRAO.URL.PIX/0123AB\nCD81390012BR.COM.OUTRO01190123.ABCD.3456.WXYZ6304EB76\n
        maxLength: 512
        pattern: '[\w\W\s]*'
        type: string
        x-nullable: false
    required:
    - creditorAccount
    - localInstrument
    title: OpenbankingBrasilPaymentV3Details Details
    type: object
  OpenbankingBrasilPaymentV3Document:
    description: OpenbankingBrasilPaymentV3Document Document
    properties:
      identification:
        description: Nmero do documento de identificao oficial do titular pessoa jurdica.
        example: "11111111111111"
        maxLength: 14
        pattern: ^\d{14}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do titular pessoa jurdica.
        example: CNPJ
        maxLength: 4
        pattern: ^[A-Z]{4}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilPaymentV3Document1:
    description: OpenbankingBrasilPaymentV3Document1 Document1
    properties:
      identification:
        description: Nmero do documento de identificao oficial do usurio.
        example: "11111111111"
        maxLength: 11
        pattern: ^\d{11}$
        type: string
        x-nullable: false
      rel:
        description: Tipo do documento de identificao oficial do usurio.
        example: CPF
        maxLength: 3
        pattern: ^[A-Z]{3}$
        type: string
        x-nullable: false
    required:
    - identification
    - rel
    type: object
  OpenbankingBrasilPaymentV3EnumAccountPaymentsType:
    description: |-
      Tipos de contas usadas para pagamento.
      Modalidades tradicionais previstas pela Resoluo 4.753, no contemplando contas vinculadas,
      conta de domiciliados no exterior, contas em moedas estrangeiras e conta correspondente moeda eletrnica.
      Segue descrio de cada valor do ENUM.

      CACC - Current - Conta Corrente.
      SVGS - Savings - Conta de Poupana.
      TRAN - TransactingAccount - Conta de Pagamento pr-paga.
      Example: CACC
    title: OpenbankingBrasilPaymentV3EnumAccountPaymentsType EnumAccountPaymentsType
    type: string
  OpenbankingBrasilPaymentV3EnumAuthorisationStatusType:
    description: |-
      Retorna o estado do consentimento, o qual no momento de sua criao ser AWAITING_AUTHORISATION.
      Este estado ser alterado depois da autorizao do consentimento na detentora da conta do pagador (Debtor) para AUTHORISED ou REJECTED.
      O consentimento fica no estado CONSUMED aps ocorrer a iniciao do pagamento referente ao consentimento.
      Em caso de consentimento expirado a detentora dever retornar o status REJECTED.
      Estados possveis:
      AWAITING_AUTHORISATION - Aguardando autorizao
      AUTHORISED - Autorizado
      REJECTED - Rejeitado
      CONSUMED - Consumido
      Example: AWAITING_AUTHORISATION
    title: OpenbankingBrasilPaymentV3EnumAuthorisationStatusType EnumAuthorisationStatusType
    type: string
  OpenbankingBrasilPaymentV3EnumConsentRejectionReasonType:
    description: |-
      Define o cdigo da razo pela qual o consentimento foi rejeitado
      VALOR_INVALIDO
      NAO_INFORMADO
      FALHA_INFRAESTRUTURA
      TEMPO_EXPIRADO_AUTORIZACAO
      TEMPO_EXPIRADO_CONSUMO
      REJEITADO_USUARIO
      CONTAS_ORIGEM_DESTINO_IGUAIS
      CONTA_NAO_PERMITE_PAGAMENTO
      SALDO_INSUFICIENTE
      VALOR_ACIMA_LIMITE
      QRCODE_INVALIDO
      Example: SALDO_INSUFICIENTE
    title: OpenbankingBrasilPaymentV3EnumConsentRejectionReasonType EnumConsentRejectionReasonType
    type: string
  OpenbankingBrasilPaymentV3EnumLocalInstrument:
    description: |-
      Especifica a forma de iniciao do pagamento:
      MANU - Insero manual de dados da conta transacional
      DICT - Insero manual de chave Pix
      QRDN - QR code dinmico
      QRES - QR code esttico
      INIC - Indica que o recebedor (creditor) contratou o Iniciador de Pagamentos especificamente para realizar iniciaes de pagamento em que o beneficirio  previamente conhecido.
      Example: DICT
    title: OpenbankingBrasilPaymentV3EnumLocalInstrument EnumLocalInstrument
    type: string
  OpenbankingBrasilPaymentV3EnumPaymentPersonType:
    description: Titular, pessoa natural ou juridica a quem se referem os dados de
      recebedor (creditor).
    title: OpenbankingBrasilPaymentV3EnumPaymentPersonType EnumPaymentPersonType
    type: string
  OpenbankingBrasilPaymentV3Identification:
    description: Objeto contendo os dados do recebedor (creditor).
    properties:
      cpfCnpj:
        description: |-
          Identificao da pessoa envolvida na transao.
          Preencher com o CPF ou CNPJ, de acordo com o valor escolhido no campo type.
          O CPF ser utilizado com 11 nmeros e dever ser informado sem pontos ou traos.
          O CNPJ ser utilizado com 14 nmeros e dever ser informado sem pontos ou traos.
        example: "58764789000137"
        maxLength: 14
        minLength: 11
        pattern: ^\d{11}$|^\d{14}$
        type: string
        x-nullable: false
      name:
        description: |-
          Em caso de pessoa natural deve ser informado o nome completo do titular da conta do recebedor.
          Em caso de pessoa jurdica deve ser informada a razo social ou o nome fantasia da conta do recebedor.
        example: Marco Antonio de Brito
        maxLength: 120
        pattern: ^([A-Za-z---,.@:&*+_<>()!?/\\$%\d' -]+)$
        type: string
        x-nullable: false
      personType:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3EnumPaymentPersonType'
    required:
    - cpfCnpj
    - name
    - personType
    title: OpenbankingBrasilPaymentV3Identification Identification
    type: object
  OpenbankingBrasilPaymentV3LinkSingle:
    description: Referncias para outros recusos da API requisitada.
    properties:
      self:
        description: URI completo que gerou a resposta atual.
        example: https://api.banco.com.br/open-banking/api/v1/resource
        maxLength: 2000
        pattern: ^(https:\/\/)?(www\.)?[-a-zA-Z0-9@:%._\+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_\+.~#?&\/\/=]*)$
        type: string
        x-nullable: false
    required:
    - self
    title: OpenbankingBrasilPaymentV3LinkSingle LinkSingle
    type: object
  OpenbankingBrasilPaymentV3LoggedUser:
    description: Usurio (pessoa natural) que encontra-se logado na instituio Iniciadora
      de Pagamento.
    properties:
      document:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Document1'
    required:
    - document
    title: OpenbankingBrasilPaymentV3LoggedUser LoggedUser
    type: object
  OpenbankingBrasilPaymentV3Meta:
    description: Meta informao referente a API requisitada.
    properties:
      requestDateTime:
        description: Data e hora da consulta, conforme especificao RFC-3339, formato
          UTC.
        example: "2021-05-21T08:30:00Z"
        format: date-time
        type: string
        x-nullable: false
    required:
    - requestDateTime
    title: OpenbankingBrasilPaymentV3Meta Meta
    type: object
  OpenbankingBrasilPaymentV3Payment:
    description: Objeto contendo dados de pagamento para consentimento.
    properties:
      amount:
        description: |-
          Valor da transao com 2 casas decimais. O valor deve ser o mesmo enviado no consentimento.

          Para QR Code esttico com valor pr-determinado no QR Code ou para QR Code dinmico com indicao de que o valor no pode ser alterado: O campo amount deve ser preenchido com o valor estabelecido no QR Code.
          Caso seja preenchido com valor divergente do QR Code, deve ser retornado um erro HTTP Status 422.
        example: "100000.12"
        maxLength: 19
        minLength: 4
        pattern: ^((\d{1,16}\.\d{2}))$
        type: string
        x-nullable: false
      currency:
        description: |-
          Cdigo da moeda nacional segundo modelo ISO-4217, ou seja, 'BRL'.
          Todos os valores monetrios informados esto representados com a moeda vigente do Brasil.
        example: BRL
        maxLength: 3
        pattern: ^([A-Z]{3})$
        type: string
        x-nullable: false
      date:
        description: |-
          [Restrio] Mutuamente excludente com o objeto schedule.

          Este campo  obrigatrio no caso de pagamento nico.

          Neste caso, o objeto schedule no deve ser informado.
        example: "2021-01-01"
        format: date
        type: string
        x-nullable: false
      details:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Details'
      ibgeTownCode:
        description: |-
          O campo ibgetowncode no arranjo PIX, tem o mesmo comportamento que o campo codMun descrito no item 1.6.6 do manual do PIX, conforme segue:

          1. Caso a informao referente ao municpio no seja enviada; o PSP do recebedor assumir que no existem feriados estaduais e municipais no perodo em questo;
        example: "5300108"
        maxLength: 7
        minLength: 7
        pattern: ^\d{7}$
        type: string
        x-nullable: false
      schedule:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Schedule'
      type:
        description: Este campo define o tipo de pagamento que ser iniciado aps a
          autorizao do consentimento.
        example: PIX
        type: string
        x-nullable: false
    required:
    - amount
    - currency
    - details
    - type
    title: OpenbankingBrasilPaymentV3Payment Payment
    type: object
  OpenbankingBrasilPaymentV3PaymentConsent:
    description: Objeto contendo dados de pagamento para consentimento.
    properties:
      amount:
        description: Valor da transao com 2 casas decimais.
        example: "100000.12"
        maxLength: 19
        minLength: 4
        pattern: ^((\d{1,16}\.\d{2}))$
        type: string
        x-nullable: false
      currency:
        description: |-
          Cdigo da moeda nacional segundo modelo ISO-4217, ou seja, 'BRL'.
          Todos os valores monetrios informados esto representados com a moeda vigente do Brasil.
        example: BRL
        maxLength: 3
        pattern: ^([A-Z]{3})$
        type: string
        x-nullable: false
      date:
        description: |-
          [Restrio] Mutuamente excludente com o objeto schedule.

          Este campo  obrigatrio no caso de pagamento nico.

          Neste caso, o objeto schedule no deve ser informado.
        example: "2021-01-01"
        format: date
        type: string
        x-nullable: false
      details:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Details'
      ibgeTownCode:
        description: |-
          O campo ibgetowncode no arranjo PIX, tem o mesmo comportamento que o campo codMun descrito no item 1.6.6 do manual do PIX, conforme segue:

          1. Caso a informao referente ao municpio no seja enviada; o PSP do recebedor assumir que no existem feriados estaduais e municipais no perodo em questo;
        example: "5300108"
        maxLength: 7
        minLength: 7
        pattern: ^\d{7}$
        type: string
        x-nullable: false
      schedule:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Schedule'
      type:
        description: Este campo define o tipo de pagamento que ser iniciado aps a
          autorizao do consentimento.
        example: PIX
        type: string
        x-nullable: false
    required:
    - amount
    - currency
    - details
    - type
    title: OpenbankingBrasilPaymentV3PaymentConsent PaymentConsent
    type: object
  OpenbankingBrasilPaymentV3Schedule:
    description: |-
      [Restrio] Mutuamente excludente com o campo date.

      Este campo  obrigatrio no caso de agendamento.

      Neste caso, o campo date no deve ser informado.
    properties:
      single:
        $ref: '#/definitions/OpenbankingBrasilPaymentV3Single'
    required:
    - single
    title: OpenbankingBrasilPaymentV3Schedule Schedule
    type: object
  OpenbankingBrasilPaymentV3Single:
    description: Define a poltica de agendamento nico.
    properties:
      date:
        description: |-
          Define a data alvo da liquidao do pagamento.

          O fuso horrio de Braslia deve ser utilizado para criao e racionalizao sobre os dados deste campo.

          Observao: Esse campo dever sempre ser no mnimo D+1 corrido, ou seja, a data imediatamente posterior em relao a data do consentimento considerando o fuso horrio de Braslia e dever ser no mximo D+365 corridos a partir da data do consentimento considerando o fuso horrio de Braslia
        example: "2021-01-01"
        format: date
        type: string
        x-nullable: false
    required:
    - date
    title: OpenbankingBrasilPaymentV3Single Single
    type: object
  OpenbankingClient:
    properties:
      client_uri:
        description: url to a client website
        example: https://example.com
        type: string
        x-nullable: false
      id:
        description: client id
        example: default
        type: string
        x-nullable: false
      logo_uri:
        description: url to a page where client logo is served
        example: https://example.com/logo.png
        type: string
        x-nullable: false
      name:
        description: client name
        example: My app
        type: string
        x-nullable: false
    type: object
  PIICategory:
    properties:
      name:
        description: name
        example: HIPAA
        type: string
        x-nullable: false
    type: object
  RFC6749Error:
    properties:
      cause:
        type: string
        x-nullable: false
      error:
        type: string
        x-nullable: false
      error_description:
        type: string
        x-nullable: false
      error_hint:
        type: string
        x-nullable: false
      status_code:
        format: int64
        type: integer
        x-nullable: false
    type: object
  RedirectURIs:
    description: |-
      Cloudentity uses redirect URIs to redirect users back to the application after they authorized the application. Along
      with the redirection, an authorization code or access token is added to the URL.

      For backward compatibility reasons, it's also acceptable to pass `redirect_uris` as a string, for example:
      https://example.com/callback.

      example: ["https://example.com/callback"]
    items:
      type: string
    title: OAuth-allowed redirect URIs
    type: array
  RejectConsentRequest:
    properties:
      error:
        description: Rejection error indication.
        example: Rejected
        type: string
        x-nullable: false
      error_cause:
        description: Rejection cause.
        example: Rejected by the user.
        type: string
        x-nullable: false
      error_description:
        description: Rejection error description.
        example: The user rejected authentication.
        type: string
        x-nullable: false
      id:
        description: Login identifier.
        format: loginID
        type: string
        x-nullable: false
      login_state:
        description: |-
          Random string generated by Cloudentity used to mitigate cross-site request forgery (CSRF) attacks.
          Cloudentity sends state as the `login_state` query parameter when redirecting to the consent page.
        example: cauq8fonbud6q8806bf0
        format: loginState
        type: string
        x-nullable: false
      rejection_reason:
        $ref: '#/definitions/RejectionReason'
      status_code:
        description: Rejection HTTP status code.
        example: 403
        format: int64
        type: integer
        x-nullable: false
    type: object
  RejectionReason:
    properties:
      code:
        type: string
        x-nullable: false
      details:
        type: string
        x-nullable: false
    type: object
  RequestedScope:
    properties:
      authorization_server_id:
        description: Authorization server identifier
        example: my-server
        format: serverID
        type: string
        x-nullable: false
      description:
        description: The scope description displayed as a hint on a consent page
        example: This scope value requests offline access using refresh token
        type: string
        x-nullable: false
      display_name:
        description: The scope name displayed on a consent page
        example: Offline Access
        type: string
        x-nullable: false
      id:
        description: Scope identifier
        example: scope-1
        format: scopeID
        type: string
        x-nullable: false
      implicit:
        description: Request this scope by default for all clients who subscribed
          to this scope
        type: boolean
        x-nullable: false
      metadata:
        $ref: '#/definitions/Metadata'
      name:
        description: Scope name
        example: offline_access
        format: scopeName
        type: string
        x-nullable: false
      params:
        items:
          type: string
        type: array
        x-nullable: false
      requested_name:
        format: scopeName
        type: string
        x-nullable: false
      service:
        $ref: '#/definitions/Service'
      tenant_id:
        description: Tenant identifier
        example: my-company
        format: tenantID
        type: string
        x-nullable: false
      transient:
        description: Disable storage of scope grants
        type: boolean
        x-nullable: false
      with_service:
        type: boolean
        x-nullable: false
    type: object
  ResponseTypes:
    description: |-
      The array may consist of the following arguments:

      `code` - when supplied as the value for the `response_type` parameter, a successful
      response includes an authorization code

      `code token` - when supplied as the value for the `response_type` parameter, a successful
      response includes an access token, an access token type, and an authorization code

      `id_token token` - when supplied as the value for the `response_type` parameter, a successful
      response includes an access token, an access token type, and an ID token

      `code id_token token` - when supplied as the value for the `response_type` parameter, a successful
      response includes an authorization code, an ID token, an access token, and an access token
      type.

      `token` - when supplied as the value for the `response_type` parameter, a successful
      response includes an access token and its type. This argument is used for the implicit grant
      flow, but is not recommended. Instead, you should use either the authorization code grant
      flow with PKCE or client authentication set to `none` and with the use of PKCE.

      example: ["token", "id_token", "code"]
      default: ["code"]
    items:
      enum:
      - token
      - id_token
      - code
      - code id_token
      - token id_token
      - token code
      - token id_token code
      - none
      type: string
    title: An array of allowed response types
    type: array
  ScopePrivacyInformation:
    properties:
      pii_categories:
        items:
          $ref: '#/definitions/PIICategory'
        type: array
        x-nullable: false
      purpose:
        format: purposeName
        type: string
        x-nullable: false
    type: object
  Service:
    properties:
      authorization_server_id:
        description: Authorization server identifier
        example: my-server
        format: serverID
        type: string
        x-nullable: false
      custom_audience:
        description: Custom service audience
        example: https://api.example.com
        type: string
        x-nullable: false
      description:
        description: Service description
        example: Service description
        type: string
        x-nullable: false
      gateway_id:
        description: Gateway identifier
        example: gateway-1
        format: gatewayID
        type: string
        x-nullable: false
      id:
        description: A unique identifier of a service
        example: service-1
        format: serviceID
        type: string
        x-nullable: false
      name:
        description: Service name
        example: My service
        type: string
        x-nullable: false
      system:
        description: '`true` when the service is a system service'
        example: false
        type: boolean
        x-nullable: false
      tenant_id:
        description: Tenant identifier
        example: my-company
        format: tenantID
        type: string
        x-nullable: false
      type:
        default: user
        description: Service type
        enum:
        - oauth2
        - oidc
        - system
        - user
        - openbanking
        type: string
        x-nullable: false
      updated_at:
        description: The date of service update
        format: date-time
        type: string
        x-nullable: false
      with_specification:
        description: '`true` when the service has the OpenAPI 3.0 specification'
        type: boolean
        x-nullable: false
    type: object
  SpecVersion:
    type: string
  genericError:
    description: Error response
    properties:
      error:
        example: The requested resource could not be found
        type: string
        x-nullable: false
      error_code:
        description: Code represents the error status code (404, 403, 401, ...).
        example: 404
        format: int64
        type: integer
        x-nullable: false
      error_hint:
        example: Object with ID 12345 does not exist
        type: string
        x-nullable: false
    required:
    - error
    type: object
host: localhost:8443
info:
  description: |-
    Cloudentity's Brazil [Open Banking](https://cloudentity.com/developers/basics/openfinance/open-banking-brazil-overview/)
    and [Open Insurance](https://cloudentity.com/developers/basics/openfinance/open-insurance-brazil/) API collections meet
    the guidelines for products and services operating within the Open Finance ecosystem in Brazil.

    These APIs must be called with the `Authorization: Bearer` header with a token as its value.
    To obtain the token, apply the
    [Authorization Code](https://cloudentity.com/developers/basics/oauth-grant-types/authorization-code-flow/) grant type
    for the introspection endpoints and
    [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) for the
    rest of the APIs.

    API requests must feature the explicitly defined scopes the client application can access. The required and
    optional scopes are listed under individual endpoints. To check scopes for an endpoint, unfold the **AUTHORIZATIONS:**
    block in the endpoint description.
    [Enable](https://cloudentity.com/developers/howtos/access-control/creating-and-configuring-dynamic-scopes/#add-scope)
    necessary scopes for your client application in advance.

    Check our
    [Open Banking Glossary](https://cloudentity.com/developers/basics/openfinance/glossary-ob/) to get familiar with terms and
    abbreviations.
  title: Cloudentity Brazil Open Banking and Open Insurance APIs
  version: latest
paths:
  /oauth2/register:
    post:
      description: |-
        Dynamic Client Registration endpoint allows to dynamically register OAuth 2.0 client applications
        with the Cloudentity Platform. When a request with all required registration metadata
        values reaches the Cloudentity authorization server, the server issues a client
        identifier and provides client metadata values registered for the client.
        Client applications can use their registration data to communicate with the Cloudentity
        platform using the OAuth 2.0 protocol.
      operationId: dynamicClientRegistration
      parameters:
      - in: body
        name: Client
        schema:
          $ref: '#/definitions/DynamicClientRegistrationJSONRequest'
      responses:
        "201":
          $ref: '#/responses/DynamicClientRegistrationResponse'
        "400":
          $ref: '#/responses/RFC6749Error'
        "401":
          $ref: '#/responses/RFC6749Error'
        "403":
          $ref: '#/responses/RFC6749Error'
        "404":
          $ref: '#/responses/genericError'
      security:
      - default:
        - dcr_register
      summary: OBBR Dynamic Client Registration
      tags:
      - DCR
  /oauth2/register/{cid}:
    delete:
      description: Delete a dynamically registered client application.
      operationId: dynamicClientRegistrationDeleteClient
      parameters:
      - default: default
        description: Client id
        in: path
        name: cid
        required: true
        type: string
      responses:
        "204":
          description: ' Client has been deleted'
        "400":
          $ref: '#/responses/RFC6749Error'
        "401":
          $ref: '#/responses/RFC6749Error'
        "403":
          $ref: '#/responses/RFC6749Error'
        "404":
          $ref: '#/responses/genericError'
      security:
      - default: []
      summary: Delete OBBR Client
      tags:
      - DCR
    get:
      description: Retrieve metadata values of a dynamically registered client application.
      operationId: dynamicClientRegistrationGetClient
      parameters:
      - default: default
        description: Client id
        in: path
        name: cid
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/DynamicClientRegistrationResponse'
        "400":
          $ref: '#/responses/RFC6749Error'
        "401":
          $ref: '#/responses/RFC6749Error'
        "403":
          $ref: '#/responses/RFC6749Error'
        "404":
          $ref: '#/responses/genericError'
      security:
      - default: []
      summary: Get OBBR Client Metadata
      tags:
      - DCR
    put:
      description: Update metadata values of a dynamically registered client application.
      operationId: dynamicClientRegistrationUpdateClient
      parameters:
      - default: default
        description: Client id
        in: path
        name: cid
        required: true
        type: string
      - in: body
        name: Client
        schema:
          $ref: '#/definitions/DynamicClientRegistrationJSONRequest'
      responses:
        "200":
          $ref: '#/responses/DynamicClientRegistrationResponse'
        "400":
          $ref: '#/responses/RFC6749Error'
        "401":
          $ref: '#/responses/RFC6749Error'
        "403":
          $ref: '#/responses/RFC6749Error'
        "404":
          $ref: '#/responses/genericError'
      security:
      - default: []
      summary: Update OBBR Client Metadata
      tags:
      - DCR
  /open-banking-brasil/consent/{login}:
    get:
      description: |-
        Retrieve information about data access consents per client application. This endpoint is used by a custom
        Open Banking consent page during the authorization process. It returns consents previously created with the
        `POST` **Create Data Access** endpoints.

        For authorization, pass the `Authorization: Bearer` with a token as the bearer value. To obtain the token, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/)
        grant type.
      operationId: getOBBRCustomerDataAccessConsentSystem
      parameters:
      - in: path
        name: login
        required: true
        type: string
      - in: query
        name: login_state
        type: string
      responses:
        "200":
          $ref: '#/responses/GetOBBRCustomerDataAccessConsentResponse'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Get Customer Data Access Consent
      tags:
      - CONSENT_PAGE
  /open-banking-brasil/consent/{login}/accept:
    post:
      description: |-
        Notify Cloudentity that a user allowed access to their data.

        Used by a custom Open Banking consent page.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain
        the token, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/)
        grant type.
      operationId: acceptOBBRCustomerDataAccessConsentSystem
      parameters:
      - in: path
        name: login
        required: true
        type: string
      - in: body
        name: AcceptConsent
        schema:
          $ref: '#/definitions/AcceptConsentRequest'
      responses:
        "200":
          $ref: '#/responses/ConsentAccepted'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Accept Customer Data Access Consent
      tags:
      - CONSENT_PAGE
  /open-banking-brasil/consent/{login}/reject:
    post:
      description: |-
        Notify Cloudentity that a user rejected access to their data.

        Used by a custom Open Banking consent page.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain
        the token, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/)
        grant type.
      operationId: rejectOBBRCustomerDataAccessConsentSystem
      parameters:
      - in: path
        name: login
        required: true
        type: string
      - in: body
        name: RejectConsent
        schema:
          $ref: '#/definitions/RejectConsentRequest'
      responses:
        "200":
          $ref: '#/responses/ConsentRejected'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Reject Customer Data Access Consent
      tags:
      - CONSENT_PAGE
  /open-banking-brasil/open-banking/consents/v1/consents:
    post:
      deprecated: true
      description: |-
        This API allows an Account Information Service Provider (AISP) to create a consent to access the registration data
        of a Payment Service User (PSU), along with information about transactions in PSU's accounts, credit cards, and
        contracted credit products.

        For authorization, pass the `Authorization: Bearer` header with an access token as the bearer value. To obtain the
        access token with all the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: CreateDataAccessConsentDeprecated
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: |-
          Every request will be processed only once per x-idempotency-key.
          The Idempotency Key will be valid for 24 hours
        in: header
        name: x-idempotency-key
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilCustomerDataAccessConsentRequestV1'
      responses:
        "201":
          $ref: '#/responses/BrazilCustomerDataAccessConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Create Data Access Consent v1
      tags:
      - OBBR
  /open-banking-brasil/open-banking/consents/v1/consents/{consentID}:
    delete:
      deprecated: true
      description: |-
        Delete a previously created data access consent. Pass the required consent identifier in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: DeleteDataAccessConsentDeprecated
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "204":
          $ref: '#/responses/BrazilCustomerDataAccessConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Delete Data Access Consent v1
      tags:
      - OBBR
    get:
      deprecated: true
      description: |-
        Retrieve the information about a previously created data access consent. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: GetDataAccessConsentDeprecated
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilCustomerDataAccessConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Get Data Access Consent v1
      tags:
      - OBBR
  /open-banking-brasil/open-banking/consents/v1/consents/introspect:
    post:
      consumes:
      - application/x-www-form-urlencoded
      description: |-
        This endpoint accepts an OAuth 2.0 token and returns meta information surrounding the token along with the
        data access consent and the associated account identifiers.

        Pass the token as the bearer value with the `Authorization: Bearer` header. To obtain the token with all the required
        scopes, apply the
        [Authorization Code](https://cloudentity.com/developers/basics/oauth-grant-types/authorization-code-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: obbrDataAccessConsentIntrospect
      parameters:
      - in: formData
        name: token
        type: string
      responses:
        "200":
          $ref: '#/responses/IntrospectOBBRDataAccessConsentResponse'
        "401":
          $ref: '#/responses/genericError'
        "404":
          $ref: '#/responses/genericError'
        "429":
          $ref: '#/responses/genericError'
      security:
      - default:
        - introspect_openbanking_tokens
      summary: Introspect Open Banking Brazil Data Access Consent v1
      tags:
      - OBBR
  /open-banking-brasil/open-banking/consents/v2/consents/introspect:
    post:
      consumes:
      - application/x-www-form-urlencoded
      description: |-
        This endpoint accepts an OAuth 2.0 token bound to v2 data access consents and returns meta information surrounding the token
        along with the data access consent and the associated account identifiers. It's also backwards compatible with v1
        data access consent tokens.

        Pass the token as the bearer value with the `Authorization: Bearer` header. To obtain the token with all the required
        scopes, apply the
        [Authorization Code](https://cloudentity.com/developers/basics/oauth-grant-types/authorization-code-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: obbrDataAccessConsentV2Introspect
      parameters:
      - in: formData
        name: token
        type: string
      responses:
        "200":
          $ref: '#/responses/IntrospectOBBRDataAccessConsentV2Response'
        "401":
          $ref: '#/responses/genericError'
        "404":
          $ref: '#/responses/genericError'
        "429":
          $ref: '#/responses/genericError'
      security:
      - default:
        - introspect_openbanking_tokens
      summary: Introspect Open Banking Brazil Data Access Consent v2
      tags:
      - OBBR
  /open-banking-brasil/open-banking/payments/v1/consents:
    post:
      consumes:
      - application/jwt
      deprecated: true
      description: |-
        This API allows an Account Information Service Provider (AISP) to create a consent to initiate payments between
        banks and financial institutions on behalf of a Payment Service User (PSU).

        For authorization, pass the `Authorization: Bearer` header with an access token as the bearer value. To obtain the
        access token with all the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: CreatePaymentConsentDeprecated
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: |-
          Every request will be processed only once per x-idempotency-key.
          The Idempotency Key will be valid for 24 hours
        in: header
        name: x-idempotency-key
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilCustomerCreatePaymentConsentRequest'
      produces:
      - application/jwt
      responses:
        "201":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Create Payment Consent v1
      tags:
      - OBBR
  /open-banking-brasil/open-banking/payments/v1/consents/{consentID}:
    get:
      deprecated: true
      description: |-
        Retrieve the information about a previously created payment consent. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: GetPaymentConsentDeprecated
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Get Payment Consent v1
      tags:
      - OBBR
  /open-banking-brasil/open-banking/payments/v1/consents/introspect:
    post:
      consumes:
      - application/x-www-form-urlencoded
      deprecated: true
      description: |-
        This endpoint accepts an Oauth 2.0 token and returns meta information surrounding the token along with the
        payment consent and the associated account identifiers.

        Pass the token as the bearer value with the `Authorization: Bearer` header. To obtain the token with all the required
        scopes, apply the
        [Authorization Code](https://cloudentity.com/developers/basics/oauth-grant-types/authorization-code-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: obbrPaymentConsentIntrospect
      parameters:
      - in: formData
        name: token
        type: string
      responses:
        "200":
          $ref: '#/responses/IntrospectOBBRPaymentConsentResponse'
        "401":
          $ref: '#/responses/genericError'
        "404":
          $ref: '#/responses/genericError'
        "429":
          $ref: '#/responses/genericError'
      security:
      - default:
        - introspect_openbanking_tokens
      summary: Introspect Open Banking Brazil Payment Consent v1
      tags:
      - OBBR
  /open-banking-brasil/open-banking/payments/v2/consents/introspect:
    post:
      consumes:
      - application/x-www-form-urlencoded
      deprecated: true
      description: |-
        This endpoint accepts an OAuth 2.0 token bound to v2 payment consents and returns meta information surrounding the
        token along with the payment consent and the associated account identifiers. It's also backwards compatible with v1
        payment consent tokens.

        Pass the token as the bearer value with the `Authorization: Bearer` header. To obtain the token with all the required
        scopes, apply the
        [Authorization Code](https://cloudentity.com/developers/basics/oauth-grant-types/authorization-code-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: obbrPaymentConsentIntrospectV2
      parameters:
      - in: formData
        name: token
        type: string
      responses:
        "200":
          $ref: '#/responses/IntrospectOBBRPaymentConsentResponseV2'
        "401":
          $ref: '#/responses/genericError'
        "404":
          $ref: '#/responses/genericError'
        "429":
          $ref: '#/responses/genericError'
      security:
      - default:
        - introspect_openbanking_tokens
      summary: Introspect Open Banking Brazil Payment Consent v2
      tags:
      - OBBR
  /open-banking-brasil/open-banking/payments/v3/consents/introspect:
    post:
      consumes:
      - application/x-www-form-urlencoded
      deprecated: true
      description: |-
        This endpoint accepts an OAuth 2.0 token bound to v3 payment consents and returns meta information surrounding the
        token along with the payment consent and the associated account identifiers. It's also backwards compatible with v1 and v2
        payment consent tokens.

        Pass the token as the bearer value with the `Authorization: Bearer` header. To obtain the token with all the required
        scopes, apply the
        [Authorization Code](https://cloudentity.com/developers/basics/oauth-grant-types/authorization-code-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: obbrPaymentConsentIntrospectV3
      parameters:
      - in: formData
        name: token
        type: string
      responses:
        "200":
          $ref: '#/responses/IntrospectOBBRPaymentConsentResponseV3'
        "401":
          $ref: '#/responses/genericError'
        "404":
          $ref: '#/responses/genericError'
        "429":
          $ref: '#/responses/genericError'
      security:
      - default:
        - introspect_openbanking_tokens
      summary: Introspect Open Banking Brazil Payment Consent v3
      tags:
      - OBBR
  /open-banking-brasil/payment/{login}:
    get:
      description: |-
        Retrieve information about payment initiation consents per client application. This endpoint is used by a custom
        Open Banking consent page during the authorization process. It returns consents previously created with the
        `POST` **Create Payment Consent** endpoints.

        For authorization, pass the `Authorization: Bearer` with a token as the bearer value. To obtain the token, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/)
        grant type.
      operationId: getOBBRCustomerPaymentConsentSystem
      parameters:
      - in: path
        name: login
        required: true
        type: string
      - in: query
        name: login_state
        type: string
      responses:
        "200":
          $ref: '#/responses/GetOBBRCustomerPaymentConsentResponse'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Get Customer Payment Initiation Consent
      tags:
      - CONSENT_PAGE
  /open-banking-brasil/payment/{login}/accept:
    post:
      description: |-
        Notify Cloudentity that a user allowed payment initiation.

        Used by a custom Open Banking consent page.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain
        the token, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/)
        grant type.
      operationId: acceptOBBRCustomerPaymentConsentSystem
      parameters:
      - in: path
        name: login
        required: true
        type: string
      - in: body
        name: AcceptConsent
        schema:
          $ref: '#/definitions/AcceptConsentRequest'
      responses:
        "200":
          $ref: '#/responses/ConsentAccepted'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Accept Customer Payment Initiation Consent
      tags:
      - CONSENT_PAGE
  /open-banking-brasil/payment/{login}/reject:
    post:
      description: |-
        Notify Cloudentity that a user rejected payment initiation.

        Used by a custom Open Banking consent page.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain
        the token, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/)
        grant type.
      operationId: rejectOBBRCustomerPaymentConsentSystem
      parameters:
      - in: path
        name: login
        required: true
        type: string
      - in: body
        name: RejectConsent
        schema:
          $ref: '#/definitions/RejectConsentRequest'
      responses:
        "200":
          $ref: '#/responses/ConsentRejected'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Reject Customer Payment Initiation Consent
      tags:
      - CONSENT_PAGE
  /open-banking-brasil/payment/v2/{login}:
    get:
      description: |-
        Retrieve information about payment initiation consents per client application. This endpoint is used by a custom
        Open Banking consent page during the authorization process. It returns consents previously created with the
        `POST` **Create Payment Consent** endpoints.

        This endpoint is compatible with version 2.0 of the Open Banking Brazil standard.

        For authorization, pass the `Authorization: Bearer` with a token as the bearer value. To obtain the token, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/)
        grant type.
      operationId: getOBBRCustomerPaymentConsentSystemV2
      parameters:
      - in: path
        name: login
        required: true
        type: string
      - in: query
        name: login_state
        type: string
      responses:
        "200":
          $ref: '#/responses/GetOBBRCustomerPaymentConsentResponseV2'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Get Customer Payment Initiation Consent v2
      tags:
      - CONSENT_PAGE
  /open-banking-brasil/payment/v3/{login}:
    get:
      description: |-
        Retrieve information about payment initiation consents per client application. This endpoint is used by a custom
        Open Banking consent page during the authorization process. It returns consents previously created with the
        `POST` **Create Payment Consent** endpoints.

        This endpoint is compatible with version 3.0 of the Open Banking Brazil payment initiation standard.

        For authorization, pass the `Authorization: Bearer` with a token as the bearer value. To obtain the token, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/)
        grant type.
      operationId: getOBBRCustomerPaymentConsentSystemV3
      parameters:
      - in: path
        name: login
        required: true
        type: string
      - in: query
        name: login_state
        type: string
      responses:
        "200":
          $ref: '#/responses/GetOBBRCustomerPaymentConsentResponseV3'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Get Customer Payment Initiation Consent v3
      tags:
      - CONSENT_PAGE
  /open-banking/consents/v1/consents:
    post:
      description: |-
        This API allows an Account Information Service Provider (AISP) to create a consent to access the registration data
        of a Payment Service User (PSU), along with information about transactions in PSU's accounts, credit cards, and
        contracted credit products.

        For authorization, pass the `Authorization: Bearer` header with an access token as the bearer value. To obtain the
        access token with all the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: CreateDataAccessConsent
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: |-
          Every request will be processed only once per x-idempotency-key.
          The Idempotency Key will be valid for 24 hours
        in: header
        name: x-idempotency-key
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilCustomerDataAccessConsentRequestV1'
      responses:
        "201":
          $ref: '#/responses/BrazilCustomerDataAccessConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Create Data Access Consent v1
      tags:
      - OBBR
  /open-banking/consents/v1/consents/{consentID}:
    delete:
      description: |-
        Delete a previously created data access consent. Pass the required consent identifier in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: DeleteDataAccessConsent
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "204":
          $ref: '#/responses/BrazilCustomerDataAccessConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Delete Data Access Consent v1
      tags:
      - OBBR
    get:
      description: |-
        Retrieve the information about a previously created data access consent. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: GetDataAccessConsent
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilCustomerDataAccessConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Get Data Access Consent v1
      tags:
      - OBBR
  /open-banking/consents/v2/consents:
    post:
      description: |-
        This API allows an Account Information Service Provider (AISP) to create a consent to access the registration data
        of a Payment Service User (PSU), along with information about transactions in PSU's accounts, credit cards, and
        contracted credit products.

        For authorization, pass the `Authorization: Bearer` header with an access token as the bearer value. To obtain the
        access token with all the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: CreateDataAccessConsentV2
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: |-
          Every request will be processed only once per x-idempotency-key.
          The Idempotency Key will be valid for 24 hours
        in: header
        name: x-idempotency-key
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilCustomerDataAccessConsentRequestV2'
      responses:
        "201":
          $ref: '#/responses/BrazilCreateCustomerDataAccessConsentV2Response'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Create Data Access Consent v2
      tags:
      - OBBR
  /open-banking/consents/v2/consents/{consentID}:
    delete:
      description: |-
        Delete a previously created data access consent. Pass the required consent identifier in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: DeleteDataAccessConsentV2
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "204":
          description: ' consent deleted'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Delete Data Access Consent v2
      tags:
      - OBBR
    get:
      description: |-
        Retrieve the information about a previously created data access consent. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: GetDataAccessConsentV2
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilGetCustomerDataAccessConsentV2Response'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Get Data Access Consent v2
      tags:
      - OBBR
  /open-banking/consents/v2/consents/{consentID}/extends:
    get:
      description: |-
        Retrieve the details of consent extension. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: GetDataAccessConsentExtensions
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilGetConsentExtensionsResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Get Data Access Consent Extensions
      tags:
      - OBBR
    post:
      description: |-
        Extend the expiration time on an already authorised data access v2 consent. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: CreateDataAccessConsentExtension
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: |-
          Every request will be processed only once per x-idempotency-key.
          The Idempotency Key will be valid for 24 hours
        in: header
        name: x-idempotency-key
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilExtensionConsentRequest'
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilCreateConsentExtensionResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - consents
      summary: Extend a Data Access V2 Consent
      tags:
      - OBBR
  /open-banking/payments/v1/consents:
    post:
      consumes:
      - application/jwt
      deprecated: true
      description: |-
        This API allows an Account Information Service Provider (AISP) to create a consent to initiate payments between
        banks and financial institutions on behalf of a Payment Service User (PSU).

        For authorization, pass the `Authorization: Bearer` header with an access token as the bearer value. To obtain the
        access token with all the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: CreatePaymentConsent
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: |-
          Every request will be processed only once per x-idempotency-key.
          The Idempotency Key will be valid for 24 hours
        in: header
        name: x-idempotency-key
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilCustomerCreatePaymentConsentRequest'
      produces:
      - application/jwt
      responses:
        "201":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Create Payment Consent v1
      tags:
      - OBBR
  /open-banking/payments/v1/consents/{consentID}:
    get:
      deprecated: true
      description: |-
        Retrieve the information about a previously created payment consent. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: GetPaymentConsent
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Get Payment Consent v1
      tags:
      - OBBR
    patch:
      deprecated: true
      description: |-
        Revoke a previously granted payment consent. Pass the required consent identifier in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: PatchPaymentConsent
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilCustomerPatchPaymentConsentRequest'
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponse'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Revoke Payment Consent
      tags:
      - OBBR
  /open-banking/payments/v2/consents:
    post:
      consumes:
      - application/jwt
      description: |-
        This API allows an Account Information Service Provider (AISP) to create a consent to initiate payments between
        banks and financial institutions on behalf of a Payment Service User (PSU).

        For authorization, pass the `Authorization: Bearer` header with an access token as the bearer value. To obtain the
        access token with all the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: CreatePaymentConsentV2
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: |-
          Every request will be processed only once per x-idempotency-key.
          The Idempotency Key will be valid for 24 hours
        in: header
        name: x-idempotency-key
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilCustomerCreatePaymentConsentRequestV2'
      produces:
      - application/jwt
      responses:
        "201":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponseV2'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Create Payment Consent v2
      tags:
      - OBBR
  /open-banking/payments/v2/consents/{consentID}:
    get:
      description: |-
        Retrieve the information about a previously created payment consent. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: GetPaymentConsentV2
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponseV2'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Get Payment Consent v2
      tags:
      - OBBR
  /open-banking/payments/v3/consents:
    post:
      consumes:
      - application/jwt
      description: |-
        This API allows an Account Information Service Provider (AISP) to create a consent to initiate payments between
        banks and financial institutions on behalf of a Payment Service User (PSU).

        For authorization, pass the `Authorization: Bearer` header with an access token as the bearer value. To obtain the
        access token with all the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scopes list in the AUTHORIZATIONS block.
      operationId: CreatePaymentConsentV3
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: |-
          Every request will be processed only once per x-idempotency-key.
          The Idempotency Key will be valid for 24 hours
        in: header
        name: x-idempotency-key
        type: string
      - description: Request
        in: body
        name: Request
        schema:
          $ref: '#/definitions/BrazilCustomerCreatePaymentConsentRequestV3'
      produces:
      - application/jwt
      responses:
        "201":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponseV3'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Create Payment Consent v3
      tags:
      - OBBR
  /open-banking/payments/v3/consents/{consentID}:
    get:
      description: |-
        Retrieve the information about a previously created payment consent. Pass the required consent identifier
        in the path.

        This endpoint is used by a Payment Initiation Service Provider (PISP).

        For authorization, pass the `Authorization: Bearer` with a token as a bearer value. To obtain the token and include
        the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: GetPaymentConsentV3
      parameters:
      - description: |-
          The time when the PSU last logged in with the TPP.

          The value is supplied as a HTTP-date as in section 7.1.1.1 of [RFC7231]
        in: header
        name: x-fapi-auth-date
        type: string
      - description: The PSU's IP address if the PSU is currently logged in with the
          TPP.
        in: header
        name: x-fapi-customer-ip-address
        type: string
      - description: |-
          An RFC4122 UID used as a correlation Id.

          If provided, the ASPSP must "play back" this value
          in the x-fapi-interaction-id response header.
        in: header
        name: x-fapi-interaction-id
        type: string
      - description: |-
          The header indicates the user-agent that the PSU is using.

          The TPP may populate this field with the user-agent indicated by the PSU.
          If the PSU is using a TPP mobile app, the TPP must ensure that the user-agent string
          is different from browser based user-agent strings.
        in: header
        name: x-customer-user-agent
        type: string
      - description: |-
          Header containing a detached JWS signature of the body of the payload.

          Refer to resource specific documentation on when this header must be specified.
        in: header
        name: x-jws-signature
        type: string
      - description: Consent id
        format: consentID
        in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilCustomerPaymentConsentResponseV3'
        "400":
          $ref: '#/responses/OBBRErrorResponse'
        "401":
          $ref: '#/responses/OBBRErrorResponse'
        "403":
          $ref: '#/responses/OBBRErrorResponse'
        "405":
          $ref: '#/responses/OBBRErrorResponse'
        "406":
          $ref: '#/responses/OBBRErrorResponse'
        "415":
          $ref: '#/responses/OBBRErrorResponse'
        "422":
          $ref: '#/responses/OBBRErrorResponse'
        "429":
          $ref: '#/responses/OBBRErrorResponse'
        "500":
          $ref: '#/responses/OBBRErrorResponse'
      security:
      - defaultcc:
        - payments
      summary: Get Payment Consent v3
      tags:
      - OBBR
  /servers/{wid}/open-banking-brasil/consents:
    delete:
      description: |-
        Revoke Open Banking Brazil consents by a client ID or combination of client ID and consent types.
        For this, pass the `client_id` query parameter with the value set to the required client identifier. It revokes
        all consents for the given client application.

        Currently, v1 and v2 consents are supported.

        For authorization, pass the `Authorization: Bearer` with a token as the bearer value. To obtain the token with the
        necessary scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: revokeOBBRConsents
      parameters:
      - default: default
        description: Workspace id
        in: path
        name: wid
        required: true
        type: string
      - description: Client ID
        in: query
        name: client_id
        type: string
      - description: A string of consent types you can pass along with the client
          identifier to detail your request.
        in: query
        items:
          type: string
        name: consent_types
        type: array
      responses:
        "200":
          $ref: '#/responses/ConsentsRemovedResponse'
        "400":
          $ref: '#/responses/badRequestHTTPError'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Revoke Open Banking Brazil Consents
      tags:
      - MANAGEMENT
    get:
      deprecated: true
      description: |-
        Retrieve the list of Open Banking Brazil consents per workspace. Pass the required workspace identifier in the path.
        To narrow the list, use the query parameters. Currently, this endpoint supports v1 and v2 consents.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain the token
        with the required scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: getOBBRConsents
      parameters:
      - description: |-
          Optional. A
          [14-digit identification number](https://openbanking-brasil.github.io/specs-seguranca/open-banking-brasil-financial-api-1_ID3.html#section-5.2.2.4)
          of a company in Cadastro Nacional da Pessoa Jurídica (Brazilian National Registry of Legal Entities).
        in: query
        name: cnpj
        type: string
      - description: |-
          Optional. An
          [11-digit taxpayer identification number](https://openbanking-brasil.github.io/specs-seguranca/open-banking-brasil-financial-api-1_ID3.html#name-requesting-the-cpf-claim)
          in Cadastro de Pessoas Físicas (Natural Persons Register).
        in: query
        name: cpf
        type: string
      - description: |-
          Optional. Consent creation date.

          Sets the start date of the consent retrieval date range.
        in: query
        name: date_from
        type: string
      - description: |-
          Optional. Consent creation date.

          Sets the end date of the consent retrieval date range.
        in: query
        name: date_to
        type: string
      - description: |-
          List of accounts.

          It can refer to user bank accounts the client application is allowed to access.
        in: query
        items:
          type: string
        name: accounts
        type: array
      - description: A client application identifier.
        in: query
        name: client_id
        type: string
      - description: List of the consent statuses.
        in: query
        items:
          type: string
        name: status
        type: array
      - description: Consent types.
        in: query
        items:
          type: string
        name: types
        type: array
      - description: |-
          A consent identifier.

          Use it to navigate through the request pagination when the number of consents is greater than
          the `limit` set for results in the response.

          With `after_consent_id`, the list you obtain starts from the subsequent consent after the specified one. Also,
          the response depends on the `sort` and `order` parameters, if any are passed.
        in: query
        name: after_consent_id
        type: string
      - description: |-
          A consent identifier.

          Use it to navigate through the request pagination when the number of consents is greater than
          the limit set for results in the response.

          With `before_consent_id`, the list you obtain comprises consents up to the specified one. The specified consent
          isn't included. Also, the response depends on the `sort` and `order` parameters, if any are passed.
        in: query
        name: before_consent_id
        type: string
      - description: Sort results returned in the response.
        in: query
        name: sort
        type: string
      - default: desc
        description: |-
          Input: `acs` or `desc`.

          Set the order of results returned in the response.
        in: query
        name: order
        type: string
      - default: 20
        description: Limit the number of results returned in the response.
        format: int64
        in: query
        maximum: 100
        minimum: 1
        name: limit
        type: integer
      - default: default
        description: Workspace id
        in: path
        name: wid
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/OBBRConsents'
        "400":
          $ref: '#/responses/badRequestHTTPError'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Get Open Banking Brazil Consents
      tags:
      - MANAGEMENT
    post:
      description: |-
        Retrieve the list of Open Banking Brazil consents per workplace. Pass the required workplace identifier in the path.
        This endpoint currently supports v1 and v2 consents. To narrow the list, filter the response with the request body
        parameters.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain the token with
        the necessary scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: listOBBRConsents
      parameters:
      - default: default
        description: Workspace id
        in: path
        name: wid
        required: true
        type: string
      - in: body
        name: ConsentsRequest
        schema:
          $ref: '#/definitions/OBBRConsentsRequest'
      responses:
        "200":
          $ref: '#/responses/OBBRConsents'
        "400":
          $ref: '#/responses/badRequestHTTPError'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "422":
          $ref: '#/responses/unprocessableEntityHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: List Open Banking Brazil Consents
      tags:
      - MANAGEMENT
  /servers/{wid}/open-banking-brasil/consents/{consentID}:
    delete:
      description: |-
        Revoke an Open Banking Brazil consent by ID. Pass the identifiers of the required consent and the related workspace
        in the path.

        Currently, v1 and v2 consents are supported.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain the token
        and the necessary scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: revokeOBBRConsent
      parameters:
      - default: default
        description: Workspace id
        in: path
        name: wid
        required: true
        type: string
      - in: path
        name: consentID
        required: true
        type: string
      responses:
        "204":
          description: ' Consent Revoked'
        "400":
          $ref: '#/responses/badRequestHTTPError'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Revoke Open Banking Brazil Consent by ID
      tags:
      - MANAGEMENT
    get:
      description: |-
        Call this endpoint to get the details of a consent within the specified workspace.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain the token
        and the necessary scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: getOBBRConsent
      parameters:
      - default: default
        description: Workspace id
        in: path
        name: wid
        required: true
        type: string
      - in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilConsent'
        "400":
          $ref: '#/responses/badRequestHTTPError'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Get Open Banking Brazil Consent by ID
      tags:
      - MANAGEMENT
  /servers/{wid}/open-banking-brasil/consents/{consentID}/consume:
    post:
      description: |-
        Retrieve the details of an Open Banking Brazil data access or payment initiation consent. Specify the required
        consent identifier along with the identifier of the related workspace in the path.

        The response contains detailed data about the consent, including both versions of `customer_data_access_consent`,
        `customer_payment_consent`, and `customer_insurance_data_access_consent`.

        For authorization, pass the `Authorization: Bearer` header with a token as the bearer value. To obtain the token with
        the necessary scopes, apply the
        [Client Credentials](https://cloudentity.com/developers/basics/oauth-grant-types/client-credentials-flow/) grant type.

        Find the scope list in the AUTHORIZATIONS block.
      operationId: consumeOBBRConsent
      parameters:
      - default: default
        description: Workspace id
        in: path
        name: wid
        required: true
        type: string
      - in: path
        name: consentID
        required: true
        type: string
      responses:
        "200":
          $ref: '#/responses/BrazilConsent'
        "400":
          $ref: '#/responses/badRequestHTTPError'
        "401":
          $ref: '#/responses/unauthorizedHTTPError'
        "403":
          $ref: '#/responses/forbiddenHTTPError'
        "404":
          $ref: '#/responses/notFoundHTTPError'
        "429":
          $ref: '#/responses/tooManyRequestsHTTPError'
      security:
      - system:
        - manage_openbanking_consents
      summary: Consume Open Banking Brazil Consent by ID
      tags:
      - MANAGEMENT
produces:
- application/json
responses:
  BrazilConsent:
    description: Open Banking Brazil Consent
    schema:
      $ref: '#/definitions/BrazilConsent'
  BrazilCreateConsentExtensionResponse:
    description: Create Customer data access v2 consent extension
    schema:
      $ref: '#/definitions/BrazilCreateConsentExtensionResponse'
  BrazilCreateCustomerDataAccessConsentV2Response:
    description: Create Customer data access v2 consent
    schema:
      $ref: '#/definitions/BrazilCreateCustomerDataAccessConsentV2Response'
  BrazilCustomerDataAccessConsentResponse:
    description: Customer data access consent
    schema:
      $ref: '#/definitions/BrazilCustomerDataAccessConsentResponse'
  BrazilCustomerPaymentConsentResponse:
    description: Customer payment consent
    schema:
      $ref: '#/definitions/BrazilCustomerPaymentConsentResponse'
  BrazilCustomerPaymentConsentResponseV2:
    description: Customer payment consent
    schema:
      $ref: '#/definitions/BrazilCustomerPaymentConsentResponseV2'
  BrazilCustomerPaymentConsentResponseV3:
    description: Customer payment consent
    schema:
      $ref: '#/definitions/BrazilCustomerPaymentConsentResponseV3'
  BrazilGetConsentExtensionsResponse:
    description: Get Customer data access v2 consent extension
    schema:
      $ref: '#/definitions/BrazilGetConsentExtensionsResponse'
  BrazilGetCustomerDataAccessConsentV2Response:
    description: Get Customer data access v2 consent
    schema:
      $ref: '#/definitions/BrazilGetCustomerDataAccessConsentV2Response'
  BrazilInsuranceCustomerDataAccessConsentResponse:
    description: Customer data access consent
    schema:
      $ref: '#/definitions/BrazilInsuranceCustomerDataAccessConsentResponse'
  ConsentAccepted:
    description: Consent Accepted
    schema:
      $ref: '#/definitions/ConsentAccepted'
  ConsentRejected:
    description: Consent rejected
    schema:
      $ref: '#/definitions/ConsentRejected'
  ConsentsRemovedResponse:
    description: Consents Revoked
    schema:
      $ref: '#/definitions/ConsentsRemovedResponse'
  DynamicClientRegistrationResponse:
    description: Dynamic client registration response
    schema:
      $ref: '#/definitions/DynamicClientRegistrationResponse'
  GetOBBRCustomerDataAccessConsentResponse:
    description: GetOBBRCustomerDataAccessConsentResponse
    schema:
      $ref: '#/definitions/GetOBBRCustomerDataAccessConsentResponse'
  GetOBBRCustomerPaymentConsentResponse:
    description: GetOBBRCustomerPaymentConsentResponse
    schema:
      $ref: '#/definitions/GetOBBRCustomerPaymentConsentResponse'
  GetOBBRCustomerPaymentConsentResponseV2:
    description: GetOBBRCustomerPaymentConsentResponseV2
    schema:
      $ref: '#/definitions/GetOBBRCustomerPaymentConsentResponseV2'
  GetOBBRCustomerPaymentConsentResponseV3:
    description: GetOBBRCustomerPaymentConsentResponseV3
    schema:
      $ref: '#/definitions/GetOBBRCustomerPaymentConsentResponseV3'
  IntrospectOBBRDataAccessConsentResponse:
    description: Introspect Openbanking Brazil Data Access Consent Response
    schema:
      $ref: '#/definitions/IntrospectOBBRDataAccessConsentResponse'
  IntrospectOBBRDataAccessConsentV2Response:
    description: Introspect Openbanking Brazil Data Access Consent V2 Response
    schema:
      $ref: '#/definitions/IntrospectOBBRDataAccessConsentV2Response'
  IntrospectOBBRPaymentConsentResponse:
    description: Introspect Openbanking Brazil Payment Consent Response
    schema:
      $ref: '#/definitions/IntrospectOBBRPaymentConsentResponse'
  IntrospectOBBRPaymentConsentResponseV2:
    description: Introspect Openbanking Brazil Payment Consent V2 Response
    schema:
      $ref: '#/definitions/IntrospectOBBRPaymentConsentResponseV2'
  IntrospectOBBRPaymentConsentResponseV3:
    description: Introspect Openbanking Brazil Payment Consent V3 Response
    schema:
      $ref: '#/definitions/IntrospectOBBRPaymentConsentResponseV3'
  OBBRConsents:
    description: Open Banking Brazil Consents
    schema:
      $ref: '#/definitions/OBBRConsents'
  OBBRErrorResponse:
    description: Error
    schema:
      $ref: '#/definitions/OBBRErrorResponse'
  RFC6749Error:
    description: RFC6749 error
    schema:
      $ref: '#/definitions/RFC6749Error'
  badRequestHTTPError:
    description: Bad request
    schema:
      $ref: '#/definitions/Error'
  conflictHTTPError:
    description: Conflict
    schema:
      $ref: '#/definitions/Error'
  forbiddenHTTPError:
    description: Forbidden
    schema:
      $ref: '#/definitions/Error'
  genericError:
    description: ErrorResponse
    schema:
      $ref: '#/definitions/genericError'
  internalServerErrorHTTPError:
    description: Internal server error
    schema:
      $ref: '#/definitions/Error'
  methodNotAllowedHTTPError:
    description: Method not allowed
    schema:
      $ref: '#/definitions/Error'
  notFoundHTTPError:
    description: Not found
    schema:
      $ref: '#/definitions/Error'
  payloadTooLargeHTTPError:
    description: Payload too large
    schema:
      $ref: '#/definitions/Error'
  preconditionFailedHTTPError:
    description: Precondition failed
    schema:
      $ref: '#/definitions/Error'
  tooManyRequestsHTTPError:
    description: Too many requests
    schema:
      $ref: '#/definitions/Error'
  unauthorizedHTTPError:
    description: Unauthorized
    schema:
      $ref: '#/definitions/Error'
  unprocessableEntityHTTPError:
    description: Unprocessable entity
    schema:
      $ref: '#/definitions/Error'
schemes:
- https
securityDefinitions:
  default:
    authorizationUrl: /default/default/oauth2/authorize
    flow: accessCode
    scopes:
      dcr_register: dcr_register
      introspect_openbanking_tokens: introspect_openbanking_tokens
    tokenUrl: /default/default/oauth2/token
    type: oauth2
  defaultcc:
    authorizationUrl: /default/default/oauth2/authorize
    flow: accessCode
    scopes:
      accounts: accounts
      consents: consents
      payments: payments
    tokenUrl: /default/default/oauth2/token
    type: oauth2
  system:
    authorizationUrl: /default/system/oauth2/authorize
    flow: accessCode
    scopes:
      manage_openbanking_consents: manage_openbanking_consents
    tokenUrl: /default/system/oauth2/token
    type: oauth2
swagger: "2.0"
